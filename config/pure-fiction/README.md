# Pure Fiction isolated authoring project

This directory is the mode-owned Sora 0.3.0 project and contains the three
complete `openpyxl`-authored workbooks. It is isolated from production
`config/generated`, Standard Universe, and all concurrent mode projects.

