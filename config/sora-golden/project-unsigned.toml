project = { id = "starclock_sora_unsupported_unsigned" }
groups = { common = { default = true } }
views = { default = { contract = "starclock_sora_unsupported_unsigned/default", groups = ["common"] } }
includes = ["schema/unsigned.toml"]

[codegen.rust]
runtime_format = "sora"
