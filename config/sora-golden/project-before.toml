project = { id = "starclock_sora_capability_golden" }
groups = { common = { default = true } }
views = { default = { contract = "starclock_sora_capability_golden/default", groups = ["common"] } }
includes = ["schema/domain.toml", "schema/excel-before.toml"]
