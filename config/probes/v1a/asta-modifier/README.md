# Asta V1a modifier probe

This isolated, non-production probe binds `character.asta.ability.astrometry.skillp01`
to the frozen Version 4.4 structured record. Its `.xlsx` workbooks are materialized
from the production templates and these reviewed rows, exported by pinned Sora
0.3.0, loaded by the production generated reader, and lowered into Starclock Rule
IR and modifier definitions.

The probe proves one Replace-by-caster aura instance whose ATK ratio is the
current Charging slot multiplied by the exact level-10 ratio `0.14`. Probe
identities remain `ProjectFixture`, disabled, and absent from production catalogs
and coverage.
