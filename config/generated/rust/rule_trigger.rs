// This file was generated by Sora. Do not edit manually.
use super::SoraMap;
use super::event_pattern::EventPattern;
use super::once_scope::OnceScope;
use super::trigger_phase::TriggerPhase;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct RuleTrigger {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "stable_key")]
    pub stable_key: String,
    #[serde(rename = "rule_id")]
    pub rule_id: i32,
    #[serde(rename = "sequence")]
    pub sequence: i32,
    #[serde(rename = "event")]
    pub event: EventPattern,
    #[serde(rename = "phase")]
    pub phase: TriggerPhase,
    #[serde(rename = "filter_id")]
    pub filter_id: i32,
    #[serde(rename = "condition_id")]
    pub condition_id: i32,
    #[serde(rename = "once_scope")]
    pub once_scope: OnceScope,
    #[serde(rename = "priority")]
    pub priority: i32,
    #[serde(rename = "program_id")]
    pub program_id: i32,
}

impl super::runtime::SoraDecode for RuleTrigger {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            stable_key: <String as super::runtime::SoraDecode>::decode(reader)?,
            rule_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            sequence: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            event: <EventPattern as super::runtime::SoraDecode>::decode(reader)?,
            phase: <TriggerPhase as super::runtime::SoraDecode>::decode(reader)?,
            filter_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            condition_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            once_scope: <OnceScope as super::runtime::SoraDecode>::decode(reader)?,
            priority: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            program_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl RuleTrigger {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
        self.event.collect_text_keys(out);
    }
}

#[derive(Debug, Clone)]
pub struct RuleTriggerTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, RuleTrigger>,
    by_stable_key: SoraMap<String, i32>,
}

impl RuleTriggerTable {
    pub const NAME: &'static str = "RuleTrigger";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "RuleTrigger",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[super::SoraIndexInfo {
            name: "by_stable_key",
            unique: true,
            fields: &["stable_key"],
        }],
    };

    pub(super) fn from_rows(rows: Vec<RuleTrigger>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        let by_stable_key =
            super::build_unique_map_index(rows.iter(), |row| row.stable_key.clone(), |row| row.id);
        let rows = super::decode_map_table(rows, |row| row.id);
        Ok(Self {
            keys,
            rows,
            by_stable_key,
        })
    }

    pub fn get(&self, key: &i32) -> Option<&RuleTrigger> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &RuleTrigger> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
    pub fn get_by_stable_key(&self, stable_key: &str) -> Option<&RuleTrigger> {
        self.by_stable_key
            .get(stable_key)
            .and_then(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for RuleTriggerTable {
    type Target = SoraMap<i32, RuleTrigger>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for RuleTriggerTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for RuleTriggerTable {
    type Key = i32;
    type Row = RuleTrigger;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
