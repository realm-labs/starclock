// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct UniversePackFile {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "relative_path")]
    pub relative_path: String,
    #[serde(rename = "bytes")]
    pub bytes: i64,
    #[serde(rename = "rows")]
    pub rows: i32,
    #[serde(rename = "sha256")]
    pub sha256: String,
}

impl super::runtime::SoraDecode for UniversePackFile {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            relative_path: <String as super::runtime::SoraDecode>::decode(reader)?,
            bytes: <i64 as super::runtime::SoraDecode>::decode(reader)?,
            rows: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            sha256: <String as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl UniversePackFile {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct UniversePackFileTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, UniversePackFile>,
    by_relative_path: SoraMap<String, i32>,
}

impl UniversePackFileTable {
    pub const NAME: &'static str = "UniversePackFile";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "UniversePackFile",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[super::SoraIndexInfo {
            name: "by_relative_path",
            unique: true,
            fields: &["relative_path"],
        }],
    };

    pub(super) fn from_rows(
        rows: Vec<UniversePackFile>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        let by_relative_path = super::build_unique_map_index(
            rows.iter(),
            |row| row.relative_path.clone(),
            |row| row.id,
        );
        let rows = super::decode_map_table(rows, |row| row.id);
        Ok(Self {
            keys,
            rows,
            by_relative_path,
        })
    }

    pub fn get(&self, key: &i32) -> Option<&UniversePackFile> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &UniversePackFile> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
    pub fn get_by_relative_path(&self, relative_path: &str) -> Option<&UniversePackFile> {
        self.by_relative_path
            .get(relative_path)
            .and_then(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for UniversePackFileTable {
    type Target = SoraMap<i32, UniversePackFile>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for UniversePackFileTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for UniversePackFileTable {
    type Key = i32;
    type Row = UniversePackFile;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
