// This file was generated by Sora. Do not edit manually.
use super::SoraMap;
use super::combat_path::CombatPath;
use super::light_cone_applicability::LightConeApplicability;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct LightCone {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "rarity")]
    pub rarity: i32,
    #[serde(rename = "path")]
    pub path: CombatPath,
    #[serde(rename = "applicability")]
    pub applicability: LightConeApplicability,
    #[serde(rename = "passive_rule_identity_id")]
    pub passive_rule_identity_id: i32,
}

impl super::runtime::SoraDecode for LightCone {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            rarity: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            path: <CombatPath as super::runtime::SoraDecode>::decode(reader)?,
            applicability: <LightConeApplicability as super::runtime::SoraDecode>::decode(reader)?,
            passive_rule_identity_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl LightCone {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct LightConeTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, LightCone>,
}

impl LightConeTable {
    pub const NAME: &'static str = "LightCone";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "LightCone",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[],
    };

    pub(super) fn from_rows(rows: Vec<LightCone>) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &i32) -> Option<&LightCone> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &LightCone> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for LightConeTable {
    type Target = SoraMap<i32, LightCone>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for LightConeTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for LightConeTable {
    type Key = i32;
    type Row = LightCone;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
