// This file was generated by Sora. Do not edit manually.
use super::SoraMap;
use super::universe_difficulty_kind::UniverseDifficultyKind;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct UniverseDifficulty {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "stable_key")]
    pub stable_key: String,
    #[serde(rename = "world_id")]
    pub world_id: i32,
    #[serde(rename = "source_area_id")]
    pub source_area_id: String,
    #[serde(rename = "difficulty")]
    pub difficulty: i32,
    #[serde(rename = "kind")]
    pub kind: UniverseDifficultyKind,
    #[serde(rename = "recommended_level")]
    pub recommended_level: i32,
    #[serde(rename = "recommended_elements")]
    pub recommended_elements: Vec<String>,
    #[serde(rename = "score_curve_json")]
    pub score_curve_json: String,
    #[serde(rename = "unlock_source_id")]
    pub unlock_source_id: Option<String>,
}

impl super::runtime::SoraDecode for UniverseDifficulty {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            stable_key: <String as super::runtime::SoraDecode>::decode(reader)?,
            world_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            source_area_id: <String as super::runtime::SoraDecode>::decode(reader)?,
            difficulty: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            kind: <UniverseDifficultyKind as super::runtime::SoraDecode>::decode(reader)?,
            recommended_level: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            recommended_elements: <Vec<String> as super::runtime::SoraDecode>::decode(reader)?,
            score_curve_json: <String as super::runtime::SoraDecode>::decode(reader)?,
            unlock_source_id: <Option<String> as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl UniverseDifficulty {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct UniverseDifficultyTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, UniverseDifficulty>,
    by_stable_key: SoraMap<String, i32>,
    by_source_area: SoraMap<String, i32>,
}

impl UniverseDifficultyTable {
    pub const NAME: &'static str = "UniverseDifficulty";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "UniverseDifficulty",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[
            super::SoraIndexInfo {
                name: "by_stable_key",
                unique: true,
                fields: &["stable_key"],
            },
            super::SoraIndexInfo {
                name: "by_source_area",
                unique: true,
                fields: &["source_area_id"],
            },
        ],
    };

    pub(super) fn from_rows(
        rows: Vec<UniverseDifficulty>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        let by_stable_key =
            super::build_unique_map_index(rows.iter(), |row| row.stable_key.clone(), |row| row.id);
        let by_source_area = super::build_unique_map_index(
            rows.iter(),
            |row| row.source_area_id.clone(),
            |row| row.id,
        );
        let rows = super::decode_map_table(rows, |row| row.id);
        Ok(Self {
            keys,
            rows,
            by_stable_key,
            by_source_area,
        })
    }

    pub fn get(&self, key: &i32) -> Option<&UniverseDifficulty> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &UniverseDifficulty> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
    pub fn get_by_stable_key(&self, stable_key: &str) -> Option<&UniverseDifficulty> {
        self.by_stable_key
            .get(stable_key)
            .and_then(|key| self.rows.get(key))
    }
    pub fn get_by_source_area(&self, source_area_id: &str) -> Option<&UniverseDifficulty> {
        self.by_source_area
            .get(source_area_id)
            .and_then(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for UniverseDifficultyTable {
    type Target = SoraMap<i32, UniverseDifficulty>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for UniverseDifficultyTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for UniverseDifficultyTable {
    type Key = i32;
    type Row = UniverseDifficulty;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
