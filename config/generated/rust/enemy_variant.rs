// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct EnemyVariant {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "template_id")]
    pub template_id: i32,
    #[serde(rename = "ai_graph_id")]
    pub ai_graph_id: i32,
    #[serde(rename = "mechanically_distinct_key")]
    pub mechanically_distinct_key: String,
}

impl super::runtime::SoraDecode for EnemyVariant {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            template_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            ai_graph_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            mechanically_distinct_key: <String as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl EnemyVariant {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct EnemyVariantTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, EnemyVariant>,
    by_mechanically_distinct_key: SoraMap<String, i32>,
}

impl EnemyVariantTable {
    pub const NAME: &'static str = "EnemyVariant";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "EnemyVariant",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[super::SoraIndexInfo {
            name: "by_mechanically_distinct_key",
            unique: true,
            fields: &["mechanically_distinct_key"],
        }],
    };

    pub(super) fn from_rows(
        rows: Vec<EnemyVariant>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        let by_mechanically_distinct_key = super::build_unique_map_index(
            rows.iter(),
            |row| row.mechanically_distinct_key.clone(),
            |row| row.id,
        );
        let rows = super::decode_map_table(rows, |row| row.id);
        Ok(Self {
            keys,
            rows,
            by_mechanically_distinct_key,
        })
    }

    pub fn get(&self, key: &i32) -> Option<&EnemyVariant> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &EnemyVariant> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
    pub fn get_by_mechanically_distinct_key(
        &self,
        mechanically_distinct_key: &str,
    ) -> Option<&EnemyVariant> {
        self.by_mechanically_distinct_key
            .get(mechanically_distinct_key)
            .and_then(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for EnemyVariantTable {
    type Target = SoraMap<i32, EnemyVariant>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for EnemyVariantTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for EnemyVariantTable {
    type Key = i32;
    type Row = EnemyVariant;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
