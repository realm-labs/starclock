// This file was generated by Sora. Do not edit manually.
use super::SoraMap;
use super::universe_data_quality::UniverseDataQuality;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct UniverseReviewFixture {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "stable_key")]
    pub stable_key: String,
    #[serde(rename = "mechanic_family")]
    pub mechanic_family: String,
    #[serde(rename = "input_stable_keys")]
    pub input_stable_keys: Vec<String>,
    #[serde(rename = "initial_state_json")]
    pub initial_state_json: String,
    #[serde(rename = "commands_json")]
    pub commands_json: String,
    #[serde(rename = "expected_facts_json")]
    pub expected_facts_json: String,
    #[serde(rename = "quality_floor")]
    pub quality_floor: UniverseDataQuality,
    #[serde(rename = "provenance_ids")]
    pub provenance_ids: Vec<i32>,
}

impl super::runtime::SoraDecode for UniverseReviewFixture {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            stable_key: <String as super::runtime::SoraDecode>::decode(reader)?,
            mechanic_family: <String as super::runtime::SoraDecode>::decode(reader)?,
            input_stable_keys: <Vec<String> as super::runtime::SoraDecode>::decode(reader)?,
            initial_state_json: <String as super::runtime::SoraDecode>::decode(reader)?,
            commands_json: <String as super::runtime::SoraDecode>::decode(reader)?,
            expected_facts_json: <String as super::runtime::SoraDecode>::decode(reader)?,
            quality_floor: <UniverseDataQuality as super::runtime::SoraDecode>::decode(reader)?,
            provenance_ids: <Vec<i32> as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl UniverseReviewFixture {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct UniverseReviewFixtureTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, UniverseReviewFixture>,
    by_stable_key: SoraMap<String, i32>,
    by_mechanic_family: SoraMap<String, i32>,
}

impl UniverseReviewFixtureTable {
    pub const NAME: &'static str = "UniverseReviewFixture";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "UniverseReviewFixture",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[
            super::SoraIndexInfo {
                name: "by_stable_key",
                unique: true,
                fields: &["stable_key"],
            },
            super::SoraIndexInfo {
                name: "by_mechanic_family",
                unique: true,
                fields: &["mechanic_family"],
            },
        ],
    };

    pub(super) fn from_rows(
        rows: Vec<UniverseReviewFixture>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        let by_stable_key =
            super::build_unique_map_index(rows.iter(), |row| row.stable_key.clone(), |row| row.id);
        let by_mechanic_family = super::build_unique_map_index(
            rows.iter(),
            |row| row.mechanic_family.clone(),
            |row| row.id,
        );
        let rows = super::decode_map_table(rows, |row| row.id);
        Ok(Self {
            keys,
            rows,
            by_stable_key,
            by_mechanic_family,
        })
    }

    pub fn get(&self, key: &i32) -> Option<&UniverseReviewFixture> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &UniverseReviewFixture> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
    pub fn get_by_stable_key(&self, stable_key: &str) -> Option<&UniverseReviewFixture> {
        self.by_stable_key
            .get(stable_key)
            .and_then(|key| self.rows.get(key))
    }
    pub fn get_by_mechanic_family(&self, mechanic_family: &str) -> Option<&UniverseReviewFixture> {
        self.by_mechanic_family
            .get(mechanic_family)
            .and_then(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for UniverseReviewFixtureTable {
    type Target = SoraMap<i32, UniverseReviewFixture>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for UniverseReviewFixtureTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for UniverseReviewFixtureTable {
    type Key = i32;
    type Row = UniverseReviewFixture;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
