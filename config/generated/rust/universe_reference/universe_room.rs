// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct UniverseRoom {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "stable_key")]
    pub stable_key: String,
    #[serde(rename = "domain_id")]
    pub domain_id: i32,
    #[serde(rename = "source_room_id")]
    pub source_room_id: String,
    #[serde(rename = "map_entrance")]
    pub map_entrance: String,
    #[serde(rename = "source_group_id")]
    pub source_group_id: String,
    #[serde(rename = "section_ids")]
    pub section_ids: Vec<i32>,
}

impl super::runtime::SoraDecode for UniverseRoom {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            stable_key: <String as super::runtime::SoraDecode>::decode(reader)?,
            domain_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            source_room_id: <String as super::runtime::SoraDecode>::decode(reader)?,
            map_entrance: <String as super::runtime::SoraDecode>::decode(reader)?,
            source_group_id: <String as super::runtime::SoraDecode>::decode(reader)?,
            section_ids: <Vec<i32> as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl UniverseRoom {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct UniverseRoomTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, UniverseRoom>,
    by_stable_key: SoraMap<String, i32>,
    by_source_room: SoraMap<String, i32>,
}

impl UniverseRoomTable {
    pub const NAME: &'static str = "UniverseRoom";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "UniverseRoom",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[
            super::SoraIndexInfo {
                name: "by_stable_key",
                unique: true,
                fields: &["stable_key"],
            },
            super::SoraIndexInfo {
                name: "by_source_room",
                unique: true,
                fields: &["source_room_id"],
            },
        ],
    };

    pub(super) fn from_rows(
        rows: Vec<UniverseRoom>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        let by_stable_key =
            super::build_unique_map_index(rows.iter(), |row| row.stable_key.clone(), |row| row.id);
        let by_source_room = super::build_unique_map_index(
            rows.iter(),
            |row| row.source_room_id.clone(),
            |row| row.id,
        );
        let rows = super::decode_map_table(rows, |row| row.id);
        Ok(Self {
            keys,
            rows,
            by_stable_key,
            by_source_room,
        })
    }

    pub fn get(&self, key: &i32) -> Option<&UniverseRoom> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &UniverseRoom> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
    pub fn get_by_stable_key(&self, stable_key: &str) -> Option<&UniverseRoom> {
        self.by_stable_key
            .get(stable_key)
            .and_then(|key| self.rows.get(key))
    }
    pub fn get_by_source_room(&self, source_room_id: &str) -> Option<&UniverseRoom> {
        self.by_source_room
            .get(source_room_id)
            .and_then(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for UniverseRoomTable {
    type Target = SoraMap<i32, UniverseRoom>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for UniverseRoomTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for UniverseRoomTable {
    type Key = i32;
    type Row = UniverseRoom;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
