// This file was generated by Sora. Do not edit manually.
use super::SoraMap;
use super::universe_decision_policy::UniverseDecisionPolicy;
use super::universe_domain_kind::UniverseDomainKind;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct UniverseDomain {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "stable_key")]
    pub stable_key: String,
    #[serde(rename = "source_type")]
    pub source_type: i32,
    #[serde(rename = "kind")]
    pub kind: UniverseDomainKind,
    #[serde(rename = "decision_policy")]
    pub decision_policy: UniverseDecisionPolicy,
    #[serde(rename = "terminal")]
    pub terminal: bool,
    #[serde(rename = "name_en")]
    pub name_en: String,
    #[serde(rename = "name_zh_cn")]
    pub name_zh_cn: String,
    #[serde(rename = "summary_en")]
    pub summary_en: String,
    #[serde(rename = "summary_zh_cn")]
    pub summary_zh_cn: String,
}

impl super::runtime::SoraDecode for UniverseDomain {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            stable_key: <String as super::runtime::SoraDecode>::decode(reader)?,
            source_type: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            kind: <UniverseDomainKind as super::runtime::SoraDecode>::decode(reader)?,
            decision_policy: <UniverseDecisionPolicy as super::runtime::SoraDecode>::decode(
                reader,
            )?,
            terminal: <bool as super::runtime::SoraDecode>::decode(reader)?,
            name_en: <String as super::runtime::SoraDecode>::decode(reader)?,
            name_zh_cn: <String as super::runtime::SoraDecode>::decode(reader)?,
            summary_en: <String as super::runtime::SoraDecode>::decode(reader)?,
            summary_zh_cn: <String as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl UniverseDomain {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct UniverseDomainTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, UniverseDomain>,
    by_stable_key: SoraMap<String, i32>,
    by_source_type: SoraMap<i32, i32>,
}

impl UniverseDomainTable {
    pub const NAME: &'static str = "UniverseDomain";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "UniverseDomain",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[
            super::SoraIndexInfo {
                name: "by_stable_key",
                unique: true,
                fields: &["stable_key"],
            },
            super::SoraIndexInfo {
                name: "by_source_type",
                unique: true,
                fields: &["source_type"],
            },
        ],
    };

    pub(super) fn from_rows(
        rows: Vec<UniverseDomain>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        let by_stable_key =
            super::build_unique_map_index(rows.iter(), |row| row.stable_key.clone(), |row| row.id);
        let by_source_type =
            super::build_unique_map_index(rows.iter(), |row| row.source_type, |row| row.id);
        let rows = super::decode_map_table(rows, |row| row.id);
        Ok(Self {
            keys,
            rows,
            by_stable_key,
            by_source_type,
        })
    }

    pub fn get(&self, key: &i32) -> Option<&UniverseDomain> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &UniverseDomain> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
    pub fn get_by_stable_key(&self, stable_key: &str) -> Option<&UniverseDomain> {
        self.by_stable_key
            .get(stable_key)
            .and_then(|key| self.rows.get(key))
    }
    pub fn get_by_source_type(&self, source_type: i32) -> Option<&UniverseDomain> {
        self.by_source_type
            .get(&source_type)
            .and_then(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for UniverseDomainTable {
    type Target = SoraMap<i32, UniverseDomain>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for UniverseDomainTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for UniverseDomainTable {
    type Key = i32;
    type Row = UniverseDomain;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
