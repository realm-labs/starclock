// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct UniverseProfile {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "stable_key")]
    pub stable_key: String,
    #[serde(rename = "game_version")]
    pub game_version: String,
    #[serde(rename = "snapshot_date")]
    pub snapshot_date: String,
    #[serde(rename = "content_manifest_sha256")]
    pub content_manifest_sha256: String,
    #[serde(rename = "pack_sha256")]
    pub pack_sha256: String,
    #[serde(rename = "world_count")]
    pub world_count: i32,
    #[serde(rename = "path_count")]
    pub path_count: i32,
    #[serde(rename = "runtime_loading")]
    pub runtime_loading: String,
}

impl super::runtime::SoraDecode for UniverseProfile {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            stable_key: <String as super::runtime::SoraDecode>::decode(reader)?,
            game_version: <String as super::runtime::SoraDecode>::decode(reader)?,
            snapshot_date: <String as super::runtime::SoraDecode>::decode(reader)?,
            content_manifest_sha256: <String as super::runtime::SoraDecode>::decode(reader)?,
            pack_sha256: <String as super::runtime::SoraDecode>::decode(reader)?,
            world_count: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            path_count: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            runtime_loading: <String as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl UniverseProfile {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct UniverseProfileTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, UniverseProfile>,
    by_stable_key: SoraMap<String, i32>,
}

impl UniverseProfileTable {
    pub const NAME: &'static str = "UniverseProfile";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "UniverseProfile",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[super::SoraIndexInfo {
            name: "by_stable_key",
            unique: true,
            fields: &["stable_key"],
        }],
    };

    pub(super) fn from_rows(
        rows: Vec<UniverseProfile>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        let by_stable_key =
            super::build_unique_map_index(rows.iter(), |row| row.stable_key.clone(), |row| row.id);
        let rows = super::decode_map_table(rows, |row| row.id);
        Ok(Self {
            keys,
            rows,
            by_stable_key,
        })
    }

    pub fn get(&self, key: &i32) -> Option<&UniverseProfile> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &UniverseProfile> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
    pub fn get_by_stable_key(&self, stable_key: &str) -> Option<&UniverseProfile> {
        self.by_stable_key
            .get(stable_key)
            .and_then(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for UniverseProfileTable {
    type Target = SoraMap<i32, UniverseProfile>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for UniverseProfileTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for UniverseProfileTable {
    type Key = i32;
    type Row = UniverseProfile;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
