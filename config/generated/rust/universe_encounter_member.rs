// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct UniverseEncounterMember {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "group_id")]
    pub group_id: i32,
    #[serde(rename = "sequence")]
    pub sequence: i32,
    #[serde(rename = "source_rogue_monster_id")]
    pub source_rogue_monster_id: String,
    #[serde(rename = "source_primary_monster_id")]
    pub source_primary_monster_id: String,
    #[serde(rename = "source_stage_id")]
    pub source_stage_id: String,
    #[serde(rename = "weight_decimal")]
    pub weight_decimal: String,
    #[serde(rename = "stage_level")]
    pub stage_level: i32,
    #[serde(rename = "hard_level_group")]
    pub hard_level_group: i32,
    #[serde(rename = "stage_ability_ids")]
    pub stage_ability_ids: Vec<String>,
    #[serde(rename = "drop_type")]
    pub drop_type: Option<String>,
}

impl super::runtime::SoraDecode for UniverseEncounterMember {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            group_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            sequence: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            source_rogue_monster_id: <String as super::runtime::SoraDecode>::decode(reader)?,
            source_primary_monster_id: <String as super::runtime::SoraDecode>::decode(reader)?,
            source_stage_id: <String as super::runtime::SoraDecode>::decode(reader)?,
            weight_decimal: <String as super::runtime::SoraDecode>::decode(reader)?,
            stage_level: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            hard_level_group: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            stage_ability_ids: <Vec<String> as super::runtime::SoraDecode>::decode(reader)?,
            drop_type: <Option<String> as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl UniverseEncounterMember {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct UniverseEncounterMemberTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, UniverseEncounterMember>,
}

impl UniverseEncounterMemberTable {
    pub const NAME: &'static str = "UniverseEncounterMember";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "UniverseEncounterMember",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[],
    };

    pub(super) fn from_rows(
        rows: Vec<UniverseEncounterMember>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &i32) -> Option<&UniverseEncounterMember> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &UniverseEncounterMember> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for UniverseEncounterMemberTable {
    type Target = SoraMap<i32, UniverseEncounterMember>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for UniverseEncounterMemberTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for UniverseEncounterMemberTable {
    type Key = i32;
    type Row = UniverseEncounterMember;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
