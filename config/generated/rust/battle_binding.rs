// This file was generated by Sora. Do not edit manually.
use super::SoraMap;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct BattleBinding {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "stable_key")]
    pub stable_key: String,
    #[serde(rename = "node_id")]
    pub node_id: i32,
    #[serde(rename = "encounter_id")]
    pub encounter_id: i32,
    #[serde(rename = "participant_policy_id")]
    pub participant_policy_id: i32,
    #[serde(rename = "projection_id")]
    pub projection_id: i32,
    #[serde(rename = "seed_stream_label")]
    pub seed_stream_label: String,
    #[serde(rename = "participant_lock_sha256")]
    pub participant_lock_sha256: String,
    #[serde(rename = "battle_spec_policy_revision")]
    pub battle_spec_policy_revision: String,
}

impl super::runtime::SoraDecode for BattleBinding {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            stable_key: <String as super::runtime::SoraDecode>::decode(reader)?,
            node_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            encounter_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            participant_policy_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            projection_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            seed_stream_label: <String as super::runtime::SoraDecode>::decode(reader)?,
            participant_lock_sha256: <String as super::runtime::SoraDecode>::decode(reader)?,
            battle_spec_policy_revision: <String as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl BattleBinding {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct BattleBindingTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, BattleBinding>,
    by_stable_key: SoraMap<String, i32>,
    by_node: SoraMap<i32, Vec<i32>>,
}

impl BattleBindingTable {
    pub const NAME: &'static str = "BattleBinding";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "BattleBinding",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[
            super::SoraIndexInfo {
                name: "by_stable_key",
                unique: true,
                fields: &["stable_key"],
            },
            super::SoraIndexInfo {
                name: "by_node",
                unique: false,
                fields: &["node_id"],
            },
        ],
    };

    pub(super) fn from_rows(
        rows: Vec<BattleBinding>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        let by_stable_key =
            super::build_unique_map_index(rows.iter(), |row| row.stable_key.clone(), |row| row.id);
        let by_node = super::build_map_index(rows.iter(), |row| row.node_id, |row| row.id);
        let rows = super::decode_map_table(rows, |row| row.id);
        Ok(Self {
            keys,
            rows,
            by_stable_key,
            by_node,
        })
    }

    pub fn get(&self, key: &i32) -> Option<&BattleBinding> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &BattleBinding> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
    pub fn get_by_stable_key(&self, stable_key: &str) -> Option<&BattleBinding> {
        self.by_stable_key
            .get(stable_key)
            .and_then(|key| self.rows.get(key))
    }
    pub fn find_by_node(&self, node_id: i32) -> impl Iterator<Item = &BattleBinding> {
        self.by_node
            .get(&node_id)
            .into_iter()
            .flat_map(|keys| keys.iter())
            .filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for BattleBindingTable {
    type Target = SoraMap<i32, BattleBinding>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for BattleBindingTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for BattleBindingTable {
    type Key = i32;
    type Row = BattleBinding;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
