// This file was generated by Sora. Do not edit manually.
use super::SoraMap;
use super::universe_domain_kind::UniverseDomainKind;
use super::universe_selection_policy::UniverseSelectionPolicy;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct UniverseEncounterPool {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "stable_key")]
    pub stable_key: String,
    #[serde(rename = "room_id")]
    pub room_id: i32,
    #[serde(rename = "domain_kind")]
    pub domain_kind: UniverseDomainKind,
    #[serde(rename = "map_entrance")]
    pub map_entrance: String,
    #[serde(rename = "selection_policy")]
    pub selection_policy: UniverseSelectionPolicy,
    #[serde(rename = "source_primary_condition_key")]
    pub source_primary_condition_key: String,
    #[serde(rename = "name_en")]
    pub name_en: String,
    #[serde(rename = "name_zh_cn")]
    pub name_zh_cn: String,
    #[serde(rename = "summary_en")]
    pub summary_en: String,
    #[serde(rename = "summary_zh_cn")]
    pub summary_zh_cn: String,
}

impl super::runtime::SoraDecode for UniverseEncounterPool {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            stable_key: <String as super::runtime::SoraDecode>::decode(reader)?,
            room_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            domain_kind: <UniverseDomainKind as super::runtime::SoraDecode>::decode(reader)?,
            map_entrance: <String as super::runtime::SoraDecode>::decode(reader)?,
            selection_policy: <UniverseSelectionPolicy as super::runtime::SoraDecode>::decode(
                reader,
            )?,
            source_primary_condition_key: <String as super::runtime::SoraDecode>::decode(reader)?,
            name_en: <String as super::runtime::SoraDecode>::decode(reader)?,
            name_zh_cn: <String as super::runtime::SoraDecode>::decode(reader)?,
            summary_en: <String as super::runtime::SoraDecode>::decode(reader)?,
            summary_zh_cn: <String as super::runtime::SoraDecode>::decode(reader)?,
        })
    }
}

impl UniverseEncounterPool {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct UniverseEncounterPoolTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, UniverseEncounterPool>,
    by_stable_key: SoraMap<String, i32>,
    by_room: SoraMap<i32, i32>,
}

impl UniverseEncounterPoolTable {
    pub const NAME: &'static str = "UniverseEncounterPool";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "UniverseEncounterPool",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[
            super::SoraIndexInfo {
                name: "by_stable_key",
                unique: true,
                fields: &["stable_key"],
            },
            super::SoraIndexInfo {
                name: "by_room",
                unique: true,
                fields: &["room_id"],
            },
        ],
    };

    pub(super) fn from_rows(
        rows: Vec<UniverseEncounterPool>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        let by_stable_key =
            super::build_unique_map_index(rows.iter(), |row| row.stable_key.clone(), |row| row.id);
        let by_room = super::build_unique_map_index(rows.iter(), |row| row.room_id, |row| row.id);
        let rows = super::decode_map_table(rows, |row| row.id);
        Ok(Self {
            keys,
            rows,
            by_stable_key,
            by_room,
        })
    }

    pub fn get(&self, key: &i32) -> Option<&UniverseEncounterPool> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &UniverseEncounterPool> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
    pub fn get_by_stable_key(&self, stable_key: &str) -> Option<&UniverseEncounterPool> {
        self.by_stable_key
            .get(stable_key)
            .and_then(|key| self.rows.get(key))
    }
    pub fn get_by_room(&self, room_id: i32) -> Option<&UniverseEncounterPool> {
        self.by_room
            .get(&room_id)
            .and_then(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for UniverseEncounterPoolTable {
    type Target = SoraMap<i32, UniverseEncounterPool>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for UniverseEncounterPoolTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for UniverseEncounterPoolTable {
    type Key = i32;
    type Row = UniverseEncounterPool;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
