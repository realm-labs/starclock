// This file was generated by Sora. Do not edit manually.
use super::SoraMap;
use super::standard_expected_outcome::StandardExpectedOutcome;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct StandardScenario {
    #[serde(rename = "id")]
    pub id: i32,
    #[serde(rename = "profile_id")]
    pub profile_id: i32,
    #[serde(rename = "activity_id")]
    pub activity_id: i32,
    #[serde(rename = "battle_binding_id")]
    pub battle_binding_id: i32,
    #[serde(rename = "master_seed_hex")]
    pub master_seed_hex: String,
    #[serde(rename = "expected_outcome")]
    pub expected_outcome: StandardExpectedOutcome,
}

impl super::runtime::SoraDecode for StandardScenario {
    fn decode(
        reader: &mut super::runtime::SoraReader<'_>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        Ok(Self {
            id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            profile_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            activity_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            battle_binding_id: <i32 as super::runtime::SoraDecode>::decode(reader)?,
            master_seed_hex: <String as super::runtime::SoraDecode>::decode(reader)?,
            expected_outcome: <StandardExpectedOutcome as super::runtime::SoraDecode>::decode(
                reader,
            )?,
        })
    }
}

impl StandardScenario {
    pub(super) fn collect_text_keys<'a>(&'a self, out: &mut Vec<&'a super::runtime::TextKey>) {
        let _ = &out;
    }
}

#[derive(Debug, Clone)]
pub struct StandardScenarioTable {
    keys: Vec<i32>,
    rows: SoraMap<i32, StandardScenario>,
}

impl StandardScenarioTable {
    pub const NAME: &'static str = "StandardScenario";
    pub const INFO: super::SoraTableInfo = super::SoraTableInfo {
        name: Self::NAME,
        row_type: "StandardScenario",
        shape: super::SoraTableShape::Keyed,
        primary_key: Some(super::SoraKeyInfo {
            name: "id",
            ty: "i32",
        }),
        indexes: &[],
    };

    pub(super) fn from_rows(
        rows: Vec<StandardScenario>,
    ) -> Result<Self, super::runtime::SoraReadError> {
        let keys = rows.iter().map(|row| row.id).collect::<Vec<_>>();
        Ok(Self {
            keys,
            rows: super::decode_map_table(rows, |row| row.id),
        })
    }

    pub fn get(&self, key: &i32) -> Option<&StandardScenario> {
        self.rows.get(key)
    }

    pub fn keys(&self) -> &[i32] {
        &self.keys
    }

    pub fn ordered_rows(&self) -> impl Iterator<Item = &StandardScenario> {
        self.keys.iter().filter_map(|key| self.rows.get(key))
    }
}

impl std::ops::Deref for StandardScenarioTable {
    type Target = SoraMap<i32, StandardScenario>;

    fn deref(&self) -> &Self::Target {
        &self.rows
    }
}

impl super::ErasedSoraTable for StandardScenarioTable {
    fn info(&self) -> &'static super::SoraTableInfo {
        &Self::INFO
    }

    fn len(&self) -> usize {
        self.rows.len()
    }
}

impl super::SoraKeyedTable for StandardScenarioTable {
    type Key = i32;
    type Row = StandardScenario;

    fn get(&self, key: &Self::Key) -> Option<&Self::Row> {
        self.rows.get(key)
    }

    fn keys(&self) -> &[Self::Key] {
        &self.keys
    }
}
