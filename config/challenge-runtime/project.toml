package = "starclock_challenge_runtime_config"
includes = ["schema/memory-of-chaos.toml", "schema/apocalyptic-shadow.toml", "schema/pure-fiction.toml"]

[build]
default_source_format = "xlsx"
data_root = "data"

[codegen.rust]
runtime_format = "sora"
