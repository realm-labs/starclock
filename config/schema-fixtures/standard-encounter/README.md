# Standard encounter schema fixture

This disabled synthetic fixture composes the character/build and typed Rule IR
goldens with one enemy family, deterministic AI graph, encounter wave and
one-battle Standard Activity handoff. It is schema evidence only: it is not
production content, a runtime data path or coverage credit.
