# Goal 07 Service Partition S05

`G07-P4-M14-S05` closes Divergent Universe Trailblaze Bonus source IDs
`502`–`517`: 16 service records and 16 mechanic-rule bindings retained by
Goal 07's frozen exact-once denominator.

Every assigned row is formally owned by `DivergentUniverse`, marked
`EvidenceOnly`, omitted from Standard offer pools and authored without a
Standard effect payload. The service compiler lowers each record and rule to
the generic `ProfileExcluded` boundary.

Submitting any assigned ID to the Standard service definition or compiled
interaction runtime returns `ProfileUnavailable` before payload validation,
RNG use, currency/inventory mutation or Activity events. Tests enumerate the
complete expansion-owned Trailblaze Bonus set so a later workbook or bundle
regeneration cannot leak one of these rows into Standard eligibility.

All 16 records and rules finish with `ProfileExcluded`, `ExactPublic` and
explicit `DivergentUniverse` ownership. This partition does not implement or
approximate Divergent Universe behavior, admit a native handler or change any
Standard runtime identity.
