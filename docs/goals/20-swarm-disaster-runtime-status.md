# Goal 20 Status — Swarm Disaster Runtime

## Goal state

| Field | Value |
|---|---|
| Goal ID | `swarm-disaster-runtime-v1` |
| State | `InProgress` |
| Active phase | Phase 6 — Encounters and full-run integration |
| Active batch | None |
| Next unblocked batch | `G20-P6-B4` |
| Snapshot | Version 4.4 / Goal 09 reference release dated 2026-07-29 |
| Profile | `swarm-disaster.profile.v1` |
| Candidate bundle | `385727a8a5875795b29c996102040f7f4419c6adac7b5e10ee6b09c084409362` |
| Normalized pack | `82f3ffc444a1cdcd8bcba5a946bee3a3c8d58527b93a1c9d77f285697401b2d8` |
| Runtime denominator | 6,963 source obligations / 23 rules / 23 fixture families |
| Inherited policy boundaries | 31; 31 terminal / 0 pending |
| Content lane | Candidate reference input; target `Released` runtime component |
| Branch | `codex/goal20-swarm-disaster-runtime` in the current worktree by user direction |
| Blocking condition | None |

## Phase ledger

| Phase | State | Exit evidence |
|---|---|---|
| Phase 0 — Contract, audit and execution plan | `Complete` | P0-B1–B4 freeze prerequisites/runtime baseline, exact 6,963/23/23/31 assignments, minimal APIs/state/identity, a 16-run matrix covering 5 difficulties/8 Paths and dice/42 faces/8 Countdown-Disarray cases, 31 policy owners, seven workloads, CI and 13 release gates. |
| Phase 1 — Bundle and catalogs | `Complete` | All 65 tables/33,380 rows lower privately with exact 12/6,716 structural, 24/772 unique-system and 29/25,892 content/rule/coverage closure; component identities remain isolated. |
| Phase 2 — Entry, topology, Countdown and Disarray | `Complete` | Five batches compile all 40 formal entry combinations into one 16-slot Activity profile and a 48-node/61-edge bounded three-plane graph; execute 2,438 map-overlay, 61 Countdown/Disarray and two boss-choice obligations; preserve Countdown/Disarray across section resets; terminate through the graph's single completion node; and freeze atomic rejection, Graph-stream isolation and state hash `e2275ed8…14a30f`. |
| Phase 3 — Audience Dice and Communing Device | `Complete` | Five batches execute all eight Audience Dice and 42 faces through labeled roll/control/target policies, compile 21 Communing choices, seven persistent dimensions, 31 objective-gated cabinets and 55 ordered clamped point changes, then close movement/dice/map/Communing ordering in one five-tier Activity program with same-cause events, transactional RNG and atomic stale-state rejection. Four Phase 3 fixture families have pre-terminal production parity while their frozen formal execution remains owned by P5-B1. |
| Phase 4 — Progression, content and battle contributions | `Complete` | Communing Trails, Pathstrider progression, Paths/Resonance/Interplays, shared Blessings, Swarm Curios, Occurrences, Services, currency, beacons and external Adventure settlement execute through generic Activity/content owners with exact batch evidence. |
| Phase 5 — Mechanic partitions | `Complete` | All 12 frozen mechanic partitions, all 23 semantic fixture families and exact-once 6,963/23/23 coverage close with zero gaps, duplicate IDs, orphan rules, admitted native handlers or shared-domain Swarm stable-ID branches. Encounter remains truthfully catalog-bound until P6-B1 selection and P6-B2 BattleSpec materialization. |
| Phase 6 — Encounters and full-run integration | `InProgress` | Encounter selection and real immutable `BattleSpec` materialization now execute through the generic nested-battle handoff and executor with verified eight-field results, four-participant carry, explicit boss choice, defeat/revival, Curio repair and atomic post-battle/plane settlement. All 31 policy boundaries are terminal; the 16-run seeded matrix remains in P6-B4. |
| Phase 7 — Replay, controllers and external surfaces | `Pending` | — |
| Phase 8 — Hardening and release | `Pending` | — |

## Batch ledger

Only the earliest unblocked row may be `InProgress`. Replace `—` with exact
commands, counts, digests and executable evidence in the completing commit.

| Batch | State | Commit | Result/evidence |
|---|---|---|---|
| `G20-P0-B1` | `Complete` | This batch commit | `node tools/repository-check/verify-release-snapshots.mjs`, `node tools/reference-integration/verify.mjs`, `node tools/goal20/verify-foundation.mjs` and the quick gate pass. `policy/goal20-foundation.json` freezes 10 prerequisites, all 19 registered snapshot checks, the exact Goal 09 artifacts and 6,963/23/23/31 denominator, five immutable roots, nine runtime crate trees, 51 planned batches and current numeric/RNG/replay/adapter revisions. The first full attempt stopped because this shell exposed no `python`; an isolated temporary Python 3.14.6 environment with pinned `openpyxl==3.1.5` made no repository/global dependency change, then full passed in 154.0s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. No runtime source changed. |
| `G20-P0-B2` | `Complete` | This batch commit | `node tools/goal20/generate-dispositions.mjs --check` proves exact-once 6,963/23/23/31 assignment. Source targets are 6,282 `Integrated`, 652 `SharedIntegrated`, six `ExternalOutcome` and 23 `Metadata`; 23 rules split into three `ExactStructured` and 20 `VersionedProjectPolicy` assignments across 12 frozen P5 partitions. All 31 policy rows retain `InheritedPolicy`, exact affected-record digests and implementation owners; zero gaps, duplicates or native handlers. Runtime-disposition SHA-256 `c3e7e82a…dc00`; partition SHA-256 `20e5e240…4b10`. The final isolated-Python full gate passed in 116.2s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. |
| `G20-P0-B3` | `Complete` | This batch commit | `node tools/goal20/verify-runtime-contract.mjs` freezes four intentional public mode types, four physical/three logical scopes, 16 typed slot families, five existing Activity command kinds, nine transaction event families, 10 ordered components, eight existing RNG labels and seven failure boundaries. Candidate/generated inputs remain private; P0 admits zero native handlers; Standard and Gold replay revisions/bytes remain preserved. The isolated-Python full gate passed in 167.8s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. |
| `G20-P0-B4` | `Complete` | This batch commit | `node tools/goal20/generate-coverage-matrix.mjs --check` and `node tools/goal20/verify-phase0.mjs` freeze 16 valid three-plane runs covering five difficulties, eight paired Paths/Audience Dice, all 42 faces, eight Countdown/Disarray boundary cases and 31 policy probes. The first real-combat vertical slice, seven performance workloads, zero-clone/allocation structural budgets, three native plus three compile-only CI profiles and 13 release gates close. The isolated-Python Phase 0 full gate passed in 232.6s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. |
| `G20-P1-B1` | `Complete` | This batch commit | `cargo test -p starclock-test-kit --test universe_suite swarm_disaster_bundle --all-features` and `cargo test -p starclock-mode-universe --lib swarm_disaster_catalog --all-features` prove the exact Goal 09 bundle loads 65 private Sora tables/33,380 rows and preserves the 6,963/23/23/31 manifest denominators. Digest, format, schema fingerprint, table closure, manifest revision and row-denominator rejection families are covered before lowering. Generated rows and summary/error types remain private, no `pub use` is added, and the only new public surface is a generated-type-free validation function returning the existing catalog error. Clippy and `node tools/goal20/verify-phase1-b1.mjs` pass. The quick gate exhausted its 180-second budget while running selected workspace tests; the first full attempt exposed a legacy hard-coded `python` call, then an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5`, both on `PATH` and in `STARCLOCK_PYTHON`, passed `node tools/repository-check/run.mjs --full` in 244.5s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. |
| `G20-P1-B2` | `Complete` | This batch commit | The private catalog identity separates Candidate bundle, shared content, profile, Activity registry and composition digests. `swarm_disaster_component_set` returns the existing generic `ConfigurationComponentSet` with exactly 10 canonical components and no new public domain type or `pub use`. The static registry composes core plus one empty Swarm bundle with zero admitted handlers. Two new integration tests and two new unit tests freeze five digest vectors, controller sensitivity and the existing Gold fixture root; source-blob checks prove Standard and Gold handler/component composers remain byte-identical to the `G20-P1-B1` baseline. Clippy and `node tools/goal20/verify-phase1-b2.mjs` pass. The quick gate passed in 147.8s and deferred two generated/release/CI inputs; an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` then passed the full gate in 198.6s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. |
| `G20-P1-B3` | `Complete` | This batch commit | Twelve private structural tables lower into typed immutable definitions: 4 profiles, 8 areas, 20 difficulty segments, 11 planes, 101 chessboards, 1,109 columns, 1,991 nodes, 2,593 edges, 861 rooms, 12 domains, four beacons and two boss choices (6,716 rows total). Validation proves exact denominators and stable IDs, closed profile/difficulty/element/policy values, cross-table references, node exact-once membership, endpoint and adjacent-column closure, one start/end per board and start-to-terminal reachability for all 101 graphs. The derived edge set stays an explicitly labeled static `ProjectPolicy` superset; the `topology_policy` boundary remains `InheritedPolicy` until `G20-P2-B2`. Two structural unit tests, seven aggregate Swarm unit tests and four integration tests pass; identity construction now fails before composition if structural lowering fails. Clippy and `node tools/goal20/verify-phase1-b3.mjs` pass. The quick gate passed in 144.0s and deferred three generated/release/CI inputs; an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` then passed the full gate in 258.4s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. |
| `G20-P1-B4` | `Complete` | This batch commit | Twenty-four private unique-system tables lower 772 rows into typed immutable identities, references, canonical scalar strings and validated embedded programs. Closure covers one Countdown/Disarray policy, 42 boss-decay rows, eight Audience Path/Die pairs, 42 faces/targets, four controls, 21 choices, seven dimensions, 55 adjustments, 63 Trail nodes/effects and 56 prerequisites, 31 cabinets/objectives, 102 finish conditions, 110 unlocks, 13 chapters, six bonuses, eight Paths/boosts, 32 Resonances and 16 Interplays. All cross-table references and exact-once memberships validate; generated types remain private, authoritative floats are absent and all 31 inherited policies remain non-terminal because catalog validation is not execution. Two unique tests, nine aggregate Swarm unit tests and four integration tests pass; identity construction now rejects unique-catalog drift before composition. Clippy, dependency policy and `node tools/goal20/verify-phase1-b4.mjs` pass. The quick gate completed its three selected harnesses in 169.5s but exhausted the 180-second budget during the following `cargo check`; an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` then passed the full gate in 270.4s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. |
| `G20-P1-B5` | `Complete` | This batch commit | Twenty-nine remaining tables lower 25,892 rows into private typed topology-event, content, Curio, Occurrence, service, Adventure, encounter and mechanic-rule definitions plus a validated audit summary. Cross-catalog closure covers 349 events, 1,212 block rules, 13 consequences, 144/288 Blessings and levels, 184 pool members, three 66-row Curio tables, 75/57/308 Occurrence rows, 15 services, six Adventure outcomes, one currency, 19 service rules, 179/347/1,070/15 encounter rows and the exact 23/8,139/6,963/31/5,560/23/609/1/63 rule/evidence closure. Combined with P1-B3/B4 this proves all 65 tables/33,380 rows are accounted exactly once. Generated types remain private, authoritative floats and new `pub use` declarations are absent, and ReferenceOnly programs plus all 31 inherited policies remain non-terminal. Two content tests, 11 aggregate Swarm unit tests and four integration tests pass; identity construction now rejects content or cross-catalog drift before composition. Clippy, dependency policy and `node tools/goal20/verify-phase1-b5.mjs` pass. The quick gate passed in 117.9s and deferred three generated/release/CI inputs; an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` then passed the full gate in 216.7s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. |
| `G20-P2-B1` | `Complete` | This batch commit | The public `SwarmDisasterRuntimeFactory` validates the exact private Candidate catalogs and compiles `SwarmDisasterEntry` into an immutable `SwarmDisasterRuntimeInstance` backed by one generic `ActivityStateDefinition`. Entry validation locks one team/up to four participants for the Activity, accepts only five Formal difficulties and the eight released Path/Audience Die pairs, canonicalizes seven bounded Communing dimensions plus Trail/Cabinet/Interplay unlocks, validates Trailblaze bonuses, and initializes all 16 frozen slot families with Countdown 20 and 50 Cosmic Fragments without consuming RNG. A 40-case executable matrix covers five difficulties × eight pairs; typed rejections cover guide areas, mismatched dice, duplicate/overflow progression, unknown bonuses and participant-policy drift. Three entry tests, 14 aggregate Swarm unit tests and five generated-type-free integration tests pass. The 12 frozen P2-B1 source obligations are executed, while the profile-entry rule and semantic fixture remain truthfully Pending for their P5 owners. Clippy and `node tools/goal20/verify-phase2-b1.mjs` pass. The quick gate exhausted its 180-second budget while executing seven selected harnesses after their 102.6s build; an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` then passed the full gate in 311.9s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. |
| `G20-P2-B2` | `Complete` | This batch commit | All 40 formal entry combinations compile the authored `2011`/`2012`/`2013` plane order into deterministic root boards `20111`/`20121`/`20131`. The compiler retains the 47 nodes lying on a legal start-to-terminal route, excludes two unreachable authored nodes, adds two plane transitions and one completion edge, and produces a 48-node/61-edge immutable generic Activity graph with once-only node/edge budgets and digest `e371d5f7…f1c80`. Three bounded logical scope classes bind 47 route nodes plus the terminal; no RNG, floats or runtime JSON are used. The `topology_policy` boundary is terminal as an explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`; its semantic fixture remains pending for `G20-P5-B1`. Five entry/topology tests, 16 aggregate Swarm tests and five integration tests pass. Clippy and `node tools/goal20/verify-phase2-b2.mjs` pass. The first quick gate passed all seven selected harnesses in 148.3s before exhausting its 180-second budget during downstream `cargo check`; after the complete build, quick passed in 69.9s with two deferred inputs. An isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` passed the full gate in 242.9s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses. |
| `G20-P2-B3` | `Complete` | This batch commit | The factory lowers all 101 boards, 349 topology events, 1,212 block-creation rules, 12 domains, four beacons and 861 room bindings into private typed runtime inputs, executing the exact 2,438 assigned source obligations. Three formal-plane creation programs apply 33/84/24 ordered generic operations across 47 route nodes, select counts and beacons only from the labeled Graph stream, bind terminal domains `4/4/8`, preserve the immutable graph digest and define capacity exhaustion as an explicit no-legal-target no-op. Replacement, domain copy and blanking execute through generic Activity programs; copy and blanking preserve the target beacon, while blanking filters legal routes without mutating graph structure. Event descriptors execute before creation, and empty candidate sets consume zero draws. The domain and topology-consequence boundaries become explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`; beacons remain `InheritedPolicy` for `G20-P4-B5`, and all four semantic rule fixtures remain pending for `G20-P5-M02`/`G20-P5-B1`. Eight entry/overlay unit tests, 19 aggregate Swarm tests and five public integration tests pass; Clippy, dependency policy and the 72-re-export source policy pass. The final quick attempt built seven selected harnesses in 50.4s and passed them in 136.0s before its 180-second total budget expired during downstream `cargo check`; an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` passed the full gate in 312.9s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 214.1s. |
| `G20-P2-B4` | `Complete` | This batch commit | The private runtime catalog validates all 19 mode-constant bindings and all 42 Boss Decay rows, admits the 15 rows backed by released Swarm text and keeps 27 unproven shared-DLC rows fail-closed. Generic Activity programs carry Countdown `20` at Activity scope, apply movement `-1` before stable operation-ID-ordered adjustments, atomically reject stale or out-of-bounds programs, enter Planar Disarray only on an accepted move whose pre-move Countdown is zero, and retain an uncapped level while capping cumulative integer-percent contributions at level 20. Boundary vectors are level 1 `5/4/0`, level 5 `25/20/0`, level 6 `35/24/5`, level 20 `275/80/125`, and level 21 retains level-20 modifiers. Boss-decay selections use stable Starclock keys, canonical stable-ID order and at most one contribution per plane threshold; retained private contribution descriptors remain pending BattleSpec application in `G20-P6-B3`. The exact 61 P2-B4 source obligations execute; three rules and three semantic fixtures remain truthfully pending for `G20-P5-M03`/`G20-P5-B1`, and all three affected policies remain `InheritedPolicy` because `G20-P6-B3` is their remaining owner. Twelve entry/lifecycle tests, 23 aggregate Swarm tests and five generated-type-free integration tests pass; Clippy, dependency policy and the 72-re-export source policy pass. The cold quick attempt exhausted 180.1s after building seven harnesses in 99.3s; the isolated-Python full gate passed in 260.4s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 186.0s, then warm-cache quick passed in 77.6s with two deferred inputs. |
| `G20-P2-B5` | `Complete` | This batch commit | Two released displayed Boss choices lower into a private numeric-source-ordered runtime catalog retaining exact levels, enemy variants and intrinsic weaknesses. Caller-explicit selection and all three post-boss transitions compile only generic Activity conditions/operations, consume zero RNG and require the current plane end, a known same-layer selection and the typed released Boss Decay threshold (`PlaneOne`, `PlaneTwo`, then both). Plane 1/2 traversals reset all four Section-owned overlay slots while carrying Activity-owned Countdown/Disarray exactly; Plane 3 traverses to the graph's only terminal and records `Completed`. Stale boss programs, wrong layers, missing decay and bad causes leave canonical state/RNG bytes unchanged; transactional map rejection restores every RNG snapshot, accepted creation advances only `Graph`, and seed `0x20020005` freezes state hash `e2275ed8…14a30f`. The exact two P2-B5 source obligations execute. The Boss-choice boundary becomes explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`; encounter selection remains `InheritedPolicy` for `G20-P6-B1`/`G20-P6-B3`, and the 3-operation/4-fact semantic fixture remains pending for `G20-P5-B1`. Seventeen entry/topology/lifecycle tests, 28 aggregate Swarm tests and five public integration tests pass; Clippy, dependency policy and the 72-re-export source policy pass. The cold quick attempt exhausted 180.0s after a 99.5s seven-harness build; the isolated-Python Phase 2 full gate passed in 266.1s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 190.0s, then warm-cache quick passed in 88.9s with two deferred inputs. |
| `G20-P3-B1` | `Complete` | This batch commit | Eight Audience Paths and their paired Audience Dice plus three rarity ranks lower into a private immutable runtime catalog preserving authored Path sort and all 42 face memberships in authored-sort-then-stable-ID order. Entry authorization requires the exact seven authored unlock IDs while Destruction remains always available; unknown, duplicate and missing selected unlocks reject before state creation. Eight distinct initial `AddMazeBuff` programs and eight persistent passive descriptors retain 16/26 canonical parameter slots, compile a four-operation generic Activity initialization with `CarryExact` state and an Activity once-scope, and consume zero RNG; the complete mechanic semantics and 4-operation/5-fact fixture remain truthfully pending for `G20-P5-M04`/`G20-P5-B1`. The exact 19 assigned source obligations execute and the Audience Dice/Path policy rows become explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`; no public mode type or `pub use` is added. Audience state compilation changes the current pre-release state hash to `a1bcf8be…e8c`, while immutable P2 evidence retains `e2275ed8…14a30f`. Five Audience tests, 22 entry/lifecycle tests, 33 aggregate Swarm tests and five public integration tests pass; Clippy, dependency policy and the 72-re-export source policy pass. The cold quick attempt exhausted 180.0s after a 93.8s seven-harness build; the isolated-Python full gate passed in 276.6s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 200.2s, then the post-full quick gate passed in 85.6s with three deferred inputs. |
| `G20-P3-B2` | `Complete` | This batch commit | All four authored Audience Die controls lower into one private typed catalog with exact IDs, costs, fallbacks, stable face ordering and the optional `1000022` abandon unlock. Roll and reroll each consume exactly one `Spawn`-stream draw under independent purposes `0x5321`/`0x5322`; reroll samples the complete authored face set and atomically consumes one Activity-owned charge. Cheat selects one exact authored face, consumes one cheat charge and draws no RNG. Abandon requires a current face plus the authored unlock, clears that face, adds exactly 10 Cosmic Fragments, draws no RNG and closes the Attempt dice phase so its reward cannot repeat before reset. Empty candidates, missing charges/unlocks, invalid faces, unavailable lifecycle states and stale programs reject without authoritative state or RNG mutation; the default entry retains state hash `a1bcf8be…e8c`, while seed `0x20033205` freezes the unlocked roll/reroll/cheat/abandon hash `cb2265aa…b283`. The sole six-record dice-roll-controls boundary becomes explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`; its 4-operation/5-fact semantic fixture and mechanic rule remain truthfully pending for `G20-P5-B1`/`G20-P5-M04`. No source obligation, public mode type or `pub use` is added. Six control tests, 28 entry/lifecycle tests, 39 aggregate Swarm tests and five public integration tests pass; Clippy, dependency policy and the 72-re-export source policy pass. The cold quick attempt exhausted 180.0s after a 110.2s seven-harness build; the final isolated-Python full gate passed in 205.4s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 144.2s, then the post-full quick gate passed in 139.6s with three deferred inputs. |
| `G20-P3-B3` | `Complete` | This batch commit | All 42 authored faces and 42 target rules lower into a private immutable catalog with exactly one ordered effect per face, 33 closed operation variants, 59 canonical fixed-scale parameters, 23 description parameters and 63 ordered source-effect references. Typed activation coverage is 27 immediate, eight after-movement and seven battle-contribution faces; durations are 25 immediate, two current-movement, eight after-movement and seven next-battle descriptors, including five finite turn counts. Target contracts are 25 global/event-derived, 12 caller-explicit and five Spawn-random selectors over stable domain-then-node candidates. Random target selection uses only purpose `0x5323`; empty legal sets commit an explicit no-op with zero draws, while bad explicit targets, malformed candidate state, missing rolls and stale programs preserve state/RNG. Accepted activation closes the Attempt dice phase and records ordered Activity-owned graph/battle descriptors without a second state machine. Seed `0x20330500` freezes hash `f1e01b18…d308`; the default entry remains `a1bcf8be…e8c`. The exact 42 assigned source obligations execute and the 44-record dice-target boundary becomes explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`; the 4-operation/5-fact fixture and mechanic rule remain pending for `G20-P5-B1`/`G20-P5-M04`. Seven face tests, 35 entry/lifecycle tests, 46 aggregate Swarm tests and five public integration tests pass; Clippy, dependency policy and the unchanged 72-re-export source policy pass. The cold quick gate timed out at 180.0s after a 128.0s harness build; a later quick passed in 159.9s with two deferred inputs, then an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` passed full in 256.2s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 178.9s. |
| `G20-P3-B4` | `Complete` | This batch commit | All 21 Communing choices, seven dimensions, 55 supporting point-adjustment rows and 31 Pathstrider cabinets lower into a private immutable runtime catalog while executing the exact 59 assigned source obligations. The three released story stages expose seven choices each in stable Aeon order; one accepted choice increments only its per-Aeon counter, closes that stage for the Attempt, grants no permanent points without an authored row and consumes zero RNG. Seven Activity-owned dimension counters carry exactly across run scopes, remain bounded at 20 and apply cabinet rewards in authored source-list order with a clamp after each increment. Cabinet validation proves 24 Normal plus seven Hidden definitions, 31 unique objective authorities, 33 outgoing unlock edges exactly inverted into 33 prerequisites, one Normal root (`22`) and reachability of all 24 Normal cabinets. Exact objectives, missing prerequisites, repeated completions, corrupt completion state and stale programs fail closed or reject atomically. Seed `0x20033402` freezes hash `687b174b…f1f1`; the default pre-release entry remains `a1bcf8be…e8c`. The Communing-choice, Communing-dimension and Pathstrider-cabinet boundaries become explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`; their two mechanic rules and two 4-operation/5-fact fixtures remain truthfully pending for `G20-P5-M05`/`G20-P5-B1`. Seven Communing tests, 42 entry/lifecycle tests, 53 aggregate Swarm tests and five public integration tests pass; Clippy, the exact private `serde_json` owner list and the unchanged 72-re-export source policy pass. The cold quick attempt first exposed the missing private-owner registration, then exhausted 180.0s after a 100.7s harness build; an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` passed full in 295.6s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 210.3s, then warm-cache quick passed in 75.9s with two deferred inputs. |
| `G20-P3-B5` | `Complete` | This batch commit | A single ordinary `ActivityProgramDefinition` now closes five explicit tiers in versioned ProjectPolicy order: movement Countdown then authored-edge traversal, selected-face activation, caller-explicit map replacement, Communing choice, and cabinet completion with ordered point rewards. Five tier markers and every mutation share one cause; empty tiers remain explicit. Cross-section movement stays with the existing plane-completion owner, blanked/non-edge routes reject before RNG, random face selection advances exactly one Spawn draw, late map or cabinet validation restores every stream, and a stale face rejects earlier Countdown/traversal byte-identically. Seed `0x20033501` freezes the five-tier state hash `af167296…4701`, while the default pre-release entry remains `a1bcf8be…e8c`. The four Phase 3 fixture families bind to production contracts with 16 ordered operations, 20 expected facts and 10 source records; this is explicitly `PreTerminalProductionParity`, and formal fixture execution remains Pending for `G20-P5-B1`. This orchestration batch owns zero frozen source obligations, rules or policy boundaries, adds no public mode type and adds one generated-type-free instance method; the local tuple complexity rationale preserves Goal 20's frozen four-type public contract. Five focused tests, 47 entry/lifecycle tests, 58 aggregate Swarm tests and five public integration tests pass; Clippy and the unchanged 72-re-export source policy pass. Quick passed in 139.2s; an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` passed the Phase 3 full gate in 219.5s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 154.2s. |
| `G20-P4-B1` | `Complete` | This batch commit | All 63 Communing Trail nodes compile into seven exact nine-node threshold chains with seven roots and 56 versioned immediate-predecessor edges. Entry selection requires persistent dimension points at thresholds `1/3/5/7/9/11/13/16/20` plus predecessor closure. Five Activity-only and two Activity-and-battle effects execute through ordinary transactions: once-only run start grants 100 Cosmic Fragments, one cheat and two Countdown; abandon grants 10 additional fragments; each next-plane transition grants one reroll; and the previous-run conditional First Plane effect records at most four non-boss battle entries with stale rejection. Fifty-eight immutable BattleSpec-bound effect references retain exact scalar strings and source-node provenance with digest `9bf0490a…965f6`; seed `0x20410000` freezes the all-Trail run-start state hash `a344d30e…e7e25`. The exact 63 assigned source obligations execute and the 112-record prerequisite boundary becomes explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`; its exact mechanic rule and 4-operation/5-fact fixture remain pending for `G20-P5-M06`/`G20-P5-B1`. Six focused tests, 53 entry/lifecycle tests, 64 aggregate Swarm tests and five public integration tests pass; Clippy, dependency policy and the unchanged 72-re-export source policy pass. Quick passed in 130.8s with two deferred inputs; an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` passed full in 239.4s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 172.8s. |
| `G20-P4-B2` | `Complete` | This batch commit | A private Pathstrider catalog closes 31 exact external-quest-condition-to-cabinet bindings, all 102 released FinishWays, 110 unlock rows and 13 mechanical chapter locators while executing the exact 225 assigned source obligations. External objective completion routes directly to its unique cabinet and commits its existing ordered point/unlock transaction once. Fifteen explicitly Swarm-enabled FinishWays retain six released condition families and 15 non-revocable unlocks; caller-reported canonical progress is nondecreasing, below-threshold progress persists, threshold crossing sets each flag once, and stale/revoked progress rejects atomically. The remaining 87 FinishWays and 95 unlocks have no proven Swarm consumer and stay fail-closed. Chapter availability derives the current graph Section, checks 12 persistent Communing thresholds plus one unconditional final-plane locator, carries 13 exact flags at Activity scope, and keeps three declared-but-unpublished bonus payloads unresolved while still enabling chapter availability. Seed `0x20420001` freezes all-chapter state hash `0de89fb9…38eeb`. The 33-record objective policy becomes explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`; the 114-record unlock policy remains inherited for its `G20-P4-B3` owner, and the 4-operation/5-fact fixture plus mechanic rule remain pending for `G20-P5-B1`/`G20-P5-M06`. Six focused tests, 59 entry/lifecycle tests, 70 aggregate Swarm tests and five public integration tests pass; Clippy, dependency policy and the unchanged 72-re-export source policy pass. Quick passed in 132.3s with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 235.2s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 167.3s. |
| `G20-P4-B3` | `Complete` | This batch commit | A private Path runtime closes all 70 assigned obligations: six Trailblaze Bonuses, eight selectable Paths, eight Path boosts, 32 Resonance/Formation bindings and 16 Interplays. Bonus selection compiles one atomic run-start Activity program: fragment and Countdown effects commit immediately, while six blessing/Curio requests become stable P4-B4-owned deferred counters with zero premature RNG draws; unpayable costs reject before mutation and stale programs reject atomically. Every Path retains one `AddMazeBuff` boost, one threshold-3/100-Energy base Resonance, three threshold-0 Formations, two battle-event groups and all 13 extra-effect references. All non-Destruction account unlocks bind to released Pathstrider rows; Propagation uniquely requires `1000008` and exactly matches Audience authorization. Each Path owns two Interplays; distinct main/sub Path blessing counts must both reach three after an accepted inventory mutation, all newly eligible bindings commit together once, and immutable pre-character StageAbility contributions retain all 32 authored parameters. Runtime digest is `649f1d4c…d11b2`; seed `0x20430001` freezes the bonus-plus-two-Interplay state hash `dd4e0073…4bdbf`. Mechanical-chapter-locator and Pathstrider-unlock policies become explicit `VersionedExecutablePolicy`/`ProjectPolicy`; Path/Resonance and shared-pool policies remain inherited for P6-B3/P4-B5, and both formal fixtures/rules remain Pending for P5-B1/P5-M07. Five focused tests, 64 entry/lifecycle tests, 75 aggregate Swarm tests and five public integration tests pass; no public type or re-export was added. Clippy, dependency policy and the 894-file/72-re-export source policy pass. Final quick passed in 98.1s with two generated/release/CI inputs deferred to the already-passing full gate; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed the Phase 4 checkpoint full gate in 244.0s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 175.1s. |
| `G20-P4-B4` | `Complete` | This batch commit | The exact 564 assigned obligations execute: 144 reachable shared Blessings, 288 exact level links, 66 Swarm Curio mode copies and 66 initial states. The runtime loads the released Standard bundle once, validates every shared Blessing identity, Path, rarity, two-level parameter vector and binding, then delegates acquire/enhance/replace operations to the existing shared `BlessingRuntimeCatalog`; no second formula implementation exists. The 184 supporting pool rows close 144 equal-integer-weight Blessing members plus the previously compiled Path/Resonance membership. Two Activity-owned typed inventories preserve the frozen 16-slot profile: Blessings allow 144 entries with stack two and Curios allow 66 entries with stack one. Curio copies close 53 Normal, seven Negative and six Error Code pools, six numeric-charge lifecycles, three source-condition destructions, six Repairing-to-Fixed transitions and one replace-all rule. Candidate sets are mode-copy-ID ordered, exclude owned entries, use only Reward RNG for this boundary and fail closed when the owning offer is absent. P4-B3's six deferred random-content requests now settle through one atomic program; exhausted pools roll RNG back and stale settlement rejects without mutation. Runtime digest is `58403630…5f6eb`; seed 17 freezes Blessing offer `[35,36,88]` and Curio offer `[1019,1002]`. The 200-record Curio selection/lifecycle boundary becomes explicit `VersionedExecutablePolicy`/`ProjectPolicy`; its 3-operation/4-fact fixture and mechanic rule remain Pending for P5-B1/P5-M08. Six focused tests, 70 entry/lifecycle tests, 81 aggregate Swarm tests and five public integration tests pass; Clippy, dependency policy and the 897-file/72-re-export source policy pass. Final quick passed in 89.5s with two generated/release/CI inputs deferred; after the expected no-`python` first attempt, an isolated Python 3.14.6 environment with pinned `openpyxl==3.1.5` passed full in 260.1s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 177.8s. |
| `G20-P4-B5` | `Complete` | This batch commit | The exact 153 assigned obligations execute: 75 Occurrences, 57 reachable Swarm variants, 15 shared Services and six external Adventure outcomes. The runtime also closes 308 supporting choices, 19 Service/Beacon rules and one run-scoped Cosmic Fragment currency initialized to 50. Occurrence pools retain exact `55/14/3/3` membership, require nonzero caller-owned weight bindings, sort by handbook order then stable ID, use only the Occurrence RNG stream and fail closed on missing, duplicate or cross-pool bindings; 60 authored random-outcome policies use stable candidate order and consume no draw for empty work. Services validate all inherited shared identities and parameters, allow only exact authored non-shop prices (including the released reset schedule), debit fragments and increment use counters in one Activity transaction, reject insufficient or stale purchases atomically, and isolate Blessing/Curio offers to Shop RNG. Four beacons retain exact topology contributions. The six Adventure rooms accept only caller-reported Tier1/Tier2/Tier3 results, deliberately exclude action input simulation, validate bounded fragments and known content identities, and settle fragments/Blessings/Curios exactly once per room. Runtime digests are `d3d1a2fe…07bd1`, `71d9b473…b1d80` and `e174154c…762f3`; seeds 41/43/47 freeze the selected occurrence, outcome candidates and shop offers. Six policy boundaries become explicit `VersionedExecutablePolicy`/`ProjectPolicy`; the occurrence-choice and Service/Adventure mechanic rules and formal fixtures remain Pending for `G20-P5-M09`/`G20-P5-M10` and `G20-P5-B1`. Four Occurrence tests, four Service/Adventure tests, 78 entry/lifecycle tests, 89 aggregate Swarm tests and five integration tests pass; no public mode type or re-export is added, and the 903-file/72-re-export source policy passes. Final quick passed in 101.6s with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed the Phase 4 full gate in 180.3s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 119.8s. |
| `G20-P5-M01` | `Complete` | This batch commit | The single frozen ProjectPolicy Profile-entry rule now lowers from its private Sora row into `SwarmProfileRuleExecutor` and a guarded ordinary `ActivityProgramDefinition`; the source trigger, three Activity-owned slots, three ordered review operations, fail-closed behavior and fixture binding are validated exactly before the factory becomes usable. Production execution reuses the existing entry compiler and Trailblaze Bonus runtime rather than adding a second state machine: all five Formal difficulties execute exactly once at the graph entry node, entry counters are snapshot-guarded, Bonus identities `101`–`106` close against the six released rows, accepted Bonus effects and deferred content requests share one transaction, and Bonus `104` rejects before mutation because the initial 50 fragments cannot pay its exact 100-fragment cost. Random content remains deferred to the existing Reward stream, so Profile-rule compilation consumes zero RNG draws. Duplicate compilation is a typed error and reapplication rejects atomically. Runtime digest is `3576fde8…735ec`; seed 0 freezes five difficulty hashes and five accepted-Bonus hashes. The 3-operation/4-fact/12-record formal fixture remains truthfully Pending for `G20-P5-B1` with `PreTerminalProductionParity`. Three focused tests, 81 entry/lifecycle tests, 92 aggregate Swarm tests and five integration tests pass; no public type or re-export is added, and the 906-file/72-re-export source policy passes. The cold quick gate passed in 139.0s and the final cache-hit quick passed in 3.0s, both with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed the final tree in 212.1s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 149.0s. |
| `G20-P5-M02` | `Complete` | This batch commit | All four frozen topology mechanic rules now lower from their private Sora rows into one `SwarmTopologyRuleExecutor` boundary: three remain truthfully versioned ProjectPolicy and topology-event ordering retains its ExactStructured accuracy. The executor validates each exact trigger, Activity-owned slot list, ordered operation/fact sequence, unresolved behavior, fixture binding and `ReferenceOnly` source disposition before the factory becomes usable. Production paths delegate to the existing bounded graph compiler and `MapRuntimeCatalog`: three formal planes retain 48 nodes, 61 edges and visit budget 48; creation retains `[33,84,24]` operations and Graph-only RNG; exact event descriptors precede creation; empty event sets reject without a draw; domain replacement compiles typed node overlays; copy and blanking preserve the target beacon; blanking continues to filter legal routes; invalid graph nodes reject before mutation. No second graph compiler or Activity state machine is added. Runtime digest is `1c035541…ffde8`. The four formal fixtures retain 14 ordered operations, 18 expected facts and 15 source records and remain truthfully Pending for `G20-P5-B1` with `PreTerminalProductionParity`. Three focused tests, 84 entry/lifecycle tests, 95 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 908-file/72-re-export source policy pass. Final cache-hit quick passed in 3.1s with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 251.9s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 184.4s. |
| `G20-P5-M03` | `Complete` | This batch commit | All three frozen ProjectPolicy rules now lower into one private `SwarmDisarrayRuleExecutor`, which validates exact domains, triggers, Activity-owned slots, four-step programs, fixture bindings and fail-closed disposition before factory use. Existing Countdown programs remain authoritative: initial 20, movement -1, exact carry, warning 5, Disarray entry only after an accepted move from zero, uncapped level with level-20 modifier cap and vectors `1=5/4/0`, `5=25/20/0`, `6=35/24/5`, `20/21=275/80/125`. All 15 released Boss Decay contributions remain stable-ID ordered, 27 unproven rows remain disabled, at most one selection per plane threshold is accepted, and plane completion now obtains its exact threshold guards through the rule executor. No second Activity state machine is added. Runtime digest is `01ae0ca5…8cf12`; the three formal fixtures retain 12 operations, 15 facts and five records Pending for `G20-P5-B1`. Three focused tests, 87 entry/lifecycle tests, 98 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 910-file/72-re-export source policy pass. Quick passed in 132.1s with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 253.1s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 182.1s. |
| `G20-P5-M04` | `Complete` | This batch commit | All three frozen ProjectPolicy Audience Die rules now lower into one private `SwarmAudienceDieRuleExecutor`, which validates exact Activity domains, triggers, owned slots, four-step operation/fact programs, fixture bindings and fail-closed source disposition before factory use. Production entry selection, Path/Die initialization, control availability, roll/reroll/cheat/abandon, resolution reads and face activation all pass through this executor while retaining the existing Audience, DiceControl and DiceFace catalogs. Eight Dice and all 42 faces remain exact; eight passive kinds initialize once; roll/reroll each consume one labeled Spawn draw, reroll/cheat consume exactly one charge, cheat consumes no draw, and stale programs reject atomically. Face execution retains stage counts `27/8/7`, target modes `25/12/5`, duration modes `25/2/8/7`, five finite turn durations, stable node-ID candidates and zero-draw committed no-op behavior for empty legal targets. No second Activity state machine is added. Runtime digest is `dc3d9dcd…54111`; the three formal fixtures retain 12 operations, 15 facts and eight source records Pending for `G20-P5-B1`. Three focused tests, 90 entry/lifecycle tests, 101 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 912-file/72-re-export source policy pass. Quick passed in 132.5s with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 236.9s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 170.2s. |
| `G20-P5-M05` | `Complete` | This batch commit | Both frozen ProjectPolicy Communing rules now lower into one private `SwarmCommuningDeviceRuleExecutor`, which validates the exact Activity domains, triggers, owned slots, four-step operation/fact programs, fixture bindings and fail-closed source dispositions before factory use. All choice, point and cabinet reads and accepted programs route through the existing `CommuningRuntimeCatalog` and generic Activity transaction state: 21 choices remain ordered as seven Aeons across each of three story stages, one accepted choice increments only its Aeon counter once with zero RNG, seven persistent dimensions carry exactly and clamp each of 55 authored ordered adjustments at 20, and 31 exact-objective cabinets retain 33 prerequisite edges. Duplicate and stale work rejects atomically; no second Activity state machine is added. Runtime digest is `1386e975…c90a`; the two formal fixtures retain eight operations, ten facts and four source records Pending for `G20-P5-B1`. Three focused tests, 93 entry/lifecycle tests, 104 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 914-file/72-re-export source policy pass. Quick passed in 144.8s with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 262.1s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 192.3s. |
| `G20-P5-M06` | `Complete` | This batch commit | The frozen ExactStructured Communing Trail rule and ProjectPolicy Pathstrider rule now lower into one private `SwarmProgressionRuleExecutor`, which validates their exact CrossBattle/Activity domains, trigger order, mixed Activity/Battle slot ownership, four-step operation/fact programs, fixture bindings and distinct `NotApplicable`/`FailClosed` dispositions before factory use. Entry selection, Trail Activity programs, immutable BattleSpec contributions, external objective routing, FinishWay progress, unlock reads and mechanical chapter availability all pass through the executor while retaining the existing generic Trail, Pathstrider and Activity transaction owners. All 63 Trail nodes, 56 prerequisite edges, 63 effects and 58 battle projections remain exact and draw no RNG; Pathstrider retains 31 objective bindings, 102 FinishWays with exactly 15 enabled, and 110 unlocks with exactly 15 enabled. Progress is nondecreasing, repeated work is idempotent or typed-rejected, and stale programs reject atomically. Runtime digest is `5710db1f…2917`; the two formal fixtures retain eight operations, ten facts and five source records Pending for `G20-P5-B1`. Three focused tests, 96 entry/lifecycle tests, 107 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 917-file/72-re-export source policy pass. The entry facade was responsibility-split to 133 lines without a public re-export. Quick passed in 162.7s with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 256.3s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 182.8s. |
| `G20-P5-M07` | `Complete` | This batch commit | Both frozen ProjectPolicy Path rules now lower into one private `SwarmPathRuleExecutor`, which validates exact Activity/CrossBattle domains, triggers, Activity-owned slots, ordered three/four-step programs, fixture bindings and fail-closed disposition before factory use. Path selection, exact Propagation authorization, boost/Resonance reads and Interplay activation now route through the existing `PathRuntimeCatalog`: all eight Paths, eight boosts, 32 Resonances and 16 Interplays remain exact; Propagation alone requires unlock `1000008`; each Path owns two stable Interplays activated at distinct main/sub blessing counts `3 + 3`, with zero RNG and once-only Activity flags. Repeated work is idempotent and stale programs reject atomically. Runtime digest is `a421ce1b…4cc6`; the two formal fixtures retain seven operations, nine facts and five source records Pending for `G20-P5-B1`. Three focused tests, 99 entry/lifecycle tests, 110 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 919-file/72-re-export source policy pass. Quick passed in 133.9s with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 280.9s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 197.9s. |
| `G20-P5-M08` | `Complete` | This batch commit | The frozen ProjectPolicy Curio-lifecycle rule now lowers into one private `SwarmCurioRuleExecutor`, which validates the exact CrossBattle domain, three triggers, two Activity-owned slots, three ordered operation/fact steps, fixture binding and fail-closed source disposition before factory use. All Curio candidate, selection, acquisition, charge use, source-destruction, repair, replacement and teardown paths obtain the already-validated rule binding and continue to compile ordinary Activity programs through the existing `ContentRuntimeCatalog` and `ActivityTransactionState`; no second inventory or state machine is added. All 66 mode copies remain exact across 53 Normal, seven Negative and six Error Code Curios, including six charged, three source-destroyed, six repair and one replace-all lifecycle contract. Mutations draw no RNG, stale programs reject atomically, and replacement/teardown clears the previous lifecycle state. Runtime digest is `0bd76583…00ac`; the formal 3-operation/4-fact/3-record fixture remains Pending for `G20-P5-B1`. Three focused tests, 102 entry/lifecycle tests, 113 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 921-file/72-re-export source policy pass. The cold quick passed in 136.7s and the final cache-hit quick passed in 3.1s, both with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 235.4s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 168.7s. |
| `G20-P5-M09` | `Complete` | This batch commit | The frozen ExactStructured Occurrence-choice rule now lowers into one private `SwarmOccurrenceRuleExecutor`, which validates its exact Activity domain, `OccurrenceChoiceAccepted` trigger, two Activity-owned slots, four ordered review steps, `NotApplicable` unresolved behavior and fixture binding before factory use. Pool selection, occurrence/variant/choice reads and random-outcome selection route through the validated binding and existing `OccurrenceRuntimeCatalog`; all 75 Occurrences, 57 variants and 308 choices remain exact, including 60 seeded-uniform policies and the formal `110301` variant / `.04` choice pair. Pool candidates remain handbook-order/stable-ID sorted, require caller-owned nonzero weights, fail closed across pools and use only the labeled Occurrence RNG stream; empty work consumes no draw. Rule digest is `65dcb928…79d2` and the existing catalog digest remains `d3d1a2fe…07bd1`; seeds 41/43 retain the selected Occurrence and `[10,30]` outcome vector. The 4-operation/5-fact/2-record formal fixture remains Pending for `G20-P5-B1`. Three focused tests, 105 entry/lifecycle tests, 116 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 923-file/72-re-export source policy pass. The cold quick passed in 131.8s and the final cache-hit quick passed in 3.1s, both with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 242.8s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 172.7s. |
| `G20-P5-M10` | `Complete` | This batch commit | The frozen ProjectPolicy Service/Adventure rule now lowers into one private `SwarmServiceRuleExecutor`, which validates its exact Activity domain, service-purchase and externally offered Adventure triggers, three Activity-owned slots, three ordered review steps, fixture binding and fail-closed disposition before factory use. All Service/Adventure reads, purchases, Shop offers, external settlements and beacon contributions route through the validated binding and existing `ServiceAdventureRuntimeCatalog`; no second Activity state machine or inventory owner is added. Fifteen shared Services retain 15 purchase rules plus four beacon contributions, including five Blessing shops and four Curio shops; six external Adventure outcomes remain split three capture/three destroy rooms and accept only Tier1/Tier2/Tier3 caller-resolved results. Purchases debit exact fragments before ordered grants, insufficient or stale transactions reject atomically, Shop randomness remains isolated, external action simulation remains excluded and settlements apply validated fragments/Blessing/Curio payloads exactly once. Rule digest is `ab5aa4de…76de`; Service/Adventure digests remain `71d9b473…b1d80` and `e174154c…762f3`; seed 47 retains Blessing `[140,34]` and Curio `[1011,1002]` offers. The 3-operation/4-fact/3-record formal fixture remains Pending for `G20-P5-B1`. Three focused tests, 108 entry/lifecycle tests, 119 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 925-file/72-re-export source policy pass. The cold quick passed in 131.6s and the final cache-hit quick passed in 3.0s, both with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 243.0s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 173.2s. |
| `G20-P5-M11` | `Complete` | This batch commit | Both frozen ProjectPolicy boss rules now lower into one private `SwarmBossRuleExecutor`, which validates their exact CrossBattle domains, distinct choice/final-BattleSpec triggers, Activity-owned slots, ordered three/four-step programs, fixture bindings and fail-closed disposition before factory use. Displayed boss reads, explicit per-plane selection and plane completion route through the validated binding and existing `PlaneTransitionRuntimeCatalog`; final inputs continue to reuse the existing Boss Decay, Path/Interplay and Planar Disarray owners. The two released displayed choices remain stable source-ID ordered, consume zero RNG, reject unknown layers/identities before mutation and require an explicit same-plane selection before completion. Fifteen proven Boss Decay rows remain enabled, 27 unproven rows remain disabled, and stale selection/completion rejects atomically. Runtime digest is `2394e630…9161`; the two formal fixtures retain seven operations, nine facts and eight source records Pending for `G20-P5-B1`. Three focused tests, 111 entry/lifecycle tests, 122 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 927-file/72-re-export source policy pass. The cold quick passed in 133.0s and the final cache-hit quick passed in 3.1s, both with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 247.3s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 174.1s. |
| `G20-P5-M12` | `Complete` | This batch commit | The frozen ProjectPolicy Encounter-selection rule now lowers into one private `SwarmEncounterRuleExecutor`, which validates the exact CrossBattle domain, `BattleSpecRequested` trigger, three Activity-owned slots, four ordered review steps, fixture binding and fail-closed disposition before factory use. Its stable digest is `31fd63b9…7697`, and all five formal difficulties bind the same immutable contract. This batch intentionally does not claim real encounter selection: the already-lowered 179 groups, 347 waves, 1,070 enemy slots and 15 boss pools remain inputs to the room/difficulty policy owned by `G20-P6-B1`, while real `BattleSpec` materialization remains owned by `G20-P6-B2`. The formal four-operation/five-fact/six-record fixture remains Pending for `G20-P5-B1`. Three focused tests, 114 entry/lifecycle tests, 125 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 929-file/72-re-export source policy pass. The cold quick gate passed in 135.4s and the final cache-hit quick gate in 3.0s, both with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 243.1s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 175.5s. |
| `G20-P5-B1` | `Complete` | This batch commit | All 23 frozen Goal 09 semantic fixture rows now lower privately from the Sora bundle and bind exactly once to 23 unique production regression names. Their exact payload closes 85 ordered operations, 108 expected facts and 76 source-record bindings while preserving 20 `ProjectPolicy` and three `ExactStructured` labels. Twenty-two fixture families execute through existing production Activity/content/rule tests; the Encounter fixture validates the exact `120001` group, `1200011` wave, three enemy slots and Difficulty 1 first-plane boss pool as a `ProductionCatalogProbe` without claiming the selection owned by `G20-P6-B1` or the `BattleSpec` owned by `G20-P6-B2`. Raw preconditions, inputs, operation programs, facts, source keys, evidence labels and unique regression mappings contribute to digest `1171feaf…f25b`; runtime reads no JSON files and adds no public mode type or `pub use`. Four focused tests, 118 entry/lifecycle tests, 129 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 932-file/72-re-export source policy pass. The cold quick gate passed in 140.6s and the final cache-hit quick gate in 3.0s, both with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 251.0s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 180.2s. |
| `G20-P5-B2` | `Complete` | This batch commit | The production factory now compiles one private fail-closed coverage snapshot after all 12 mechanic executors and the semantic-fixture executor. Forty-two exact Sora coverage categories sum to 6,963 source obligations; all 23 mechanic rule IDs bind exactly one same-family fixture, and all 23 fixture IDs bind the executed semantic digest. Missing, extra or duplicate categories and orphan, duplicate or mismatched rule/fixture identities reject factory loading. The frozen disposition verifier independently proves 6,963 unique source IDs split 6,282 `Integrated`, 652 `SharedIntegrated`, six `ExternalOutcome` and 23 `Metadata`; 23 unique rules split three `ExactStructured`/20 `VersionedProjectPolicy` across 12 exact partitions; and 23 `ProductionSemanticFixture` rows. There are zero gaps, duplicates, orphan rules, admitted native handlers or Swarm ID branches in shared domain crates. Coverage digest is `8aeb60d2…bff4`; runtime retains only category aggregates and exact rule/fixture inputs, reads no JSON files, adds no public mode type and no `pub use`. Four focused tests, 122 entry/lifecycle tests, 133 aggregate Swarm tests and five integration tests pass; Clippy, dependency/source/native-handler policy and the 935-file/72-re-export source policy pass. The cold quick gate passed in 132.9s and the final cache-hit quick gate in 3.0s, both with two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 240.8s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 173.1s. |
| `G20-P6-B1` | `Complete` | This batch commit | One private encounter runtime closes all 179 groups, 347 weighted members, 347 81-series waves, 1,070 ordered enemy slots, 71 distinct enemy variants and the five-difficulty × three-tier Boss-pool matrix. Current typed map domains resolve Combat, Elite, Swarm and plane-specific Boss roles; the unpublished 861-row room join is never inferred from numeric IDs and unresolved/non-combat/wrong-plane inputs fail before RNG. Group selection uses stable numeric source-group order plus one labeled Encounter-stream uniform draw; member selection uses authored positive integer weights in source-member order; singleton work consumes no draw and the whole selection is transactional. All 20 assigned difficulty-segment obligations execute: every schedule validates strictly increasing cuts plus `levels = cuts + 1`, while each Formal entry binds its exact three plane segments and computes the effective level by counting cuts less than or equal to the root-board column. Seed `0x20060301` freezes canonical selection digest `bf1f44de…3ab27`. The rooms policy becomes a terminal `VersionedExecutablePolicy`/`ProjectPolicy`; encounter-selection and encounter-difficulty truthfully remain `InheritedPolicy` until their P6-B3 battle-consequence owner. Six focused tests, 128 entry tests, 139 aggregate Swarm tests and five integration tests pass; no public mode type or re-export is added, and the 938-file/72-re-export source policy passes. The cold quick gate passed in 163.3s with one deferred input, a cache-hit quick passed in 3.1s with three deferred inputs, and the final post-test quick passed in 89.7s with three deferred inputs. Isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed the initial full gate in 264.0s, then the final tree passed full in 163.8s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 110.1s. |
| `G20-P6-B2` | `Complete` | This batch commit | One private enemy composition retains all 71 exact released Swarm identities: 59 reuse native Standard combat definitions and 12 mode-owned aliases retain explicit reviewed behavior donors (11 same-family and one rank-equivalent) without changing the protected shared catalog. At fixture level 54, 24 identities use reviewed runtime stats and 47 visibly use the deterministic minimum fallback `HP 1 / SPD 50 / ATK 0 / DEF 0`; identity and behavior/stat accuracy remain separate claims. The current Activity snapshot compiles owned Blessings, selected shared Path/base Resonance, 60 exactly mapped shared Curios, Disarray's eight-purpose outgoing/mitigation modifiers and speed modifier, while hashing all active Interplays, next-battle Die parameters/effects/duration, all 58 selectable Trail battle effects and all 66 mode Curio lifecycle identities. `materialize_current_battle` transactionally selects the authored encounter, preserves ordered waves/slots and effective level, constructs a real immutable `BattleSpec`, validates it through `Battle::create`, and rolls back the entire Encounter RNG transaction on later assembly failure without mutating Activity. Four focused tests, 132 entry tests, 143 aggregate Swarm tests and five integration tests pass; Clippy, dependency policy and the 944-file/72-re-export source policy pass. The cold quick gate passed in 133.6s with three selected harnesses and zero deferred inputs, and the final cache-hit quick passed in 3.5s with two deferred inputs. Isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed the initial full gate in 242.9s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 175.0s; the final tree passed full in 117.9s with all 34 harnesses in 74.4s. Encounter-selection, encounter-difficulty and Path/Resonance remain truthfully inherited until P6-B3 executes and settles the nested battle. |
| `G20-P6-B3` | `Complete` | This batch commit | The current immutable Swarm materialization now starts through the generic Activity preparation/handoff boundary, executes in `UniverseNestedBattleExecutor`, verifies an eight-field projection and four participant carry rows, then atomically commits result carry plus one ordinary post-battle program. HP/Energy use `CarryClamped`, zero HP becomes defeated/departed, loss enters generic `Failed`, repairing Curios progress only after verified victories, and the released shared Reviver restores alive/present carry while charging exact fragments and use count in the same Activity program. Every boss encounter requires a caller-explicit same-plane choice; published nonempty choice joins are validated and the unpublished second-plane join is not invented. Final input hashes the selected boss plus the exact stable Boss Decay keys/canonical parameters, executes current encounter level, shared Path/base Resonance and Planar Disarray combat rules, and commits boss victory with plane completion atomically. Mode-owned Path/Interplay and Boss Decay descriptors remain explicitly identity/parameter bindings rather than claims of observed numeric parity. Missing boss choice rolls back Activity plus Encounter RNG, tampered result digest leaves state byte-identical, and final victory enters `Completed`. The six P6-B3 policy slices close as versioned executable ProjectPolicy; the policy count also reconciles the already-terminal profile/Trailblaze contracts from P5-M01 and rooms contract from P6-B1, yielding 31/31 terminal. Five focused tests, 137 entry tests, 148 aggregate Swarm tests, five integration tests and 63 Activity/replay tests pass; ordinary/final result digests are `d2eada5e…5655d0` / `0a02bd51…a13d8`, and settled state hashes are `8f787b57…0e423` / `1aec0e58…d8e6a`. Clippy, dependency policy and the 946-file/72-re-export source policy pass. Quick passed in 156.1s with three harnesses and two deferred inputs; isolated Python 3.14.6 plus pinned `openpyxl==3.1.5` passed full in 303.2s with 33 generated checks, four source-cache-only skips and 34 workspace harnesses in 231.5s. |
| `G20-P6-B4` | `Pending` | — | Complete seeded matrix and fresh replay verification. |
| `G20-P7-B1` | `Pending` | — | Component-addressed replay. |
| `G20-P7-B2` | `Pending` | — | Baseline controller. |
| `G20-P7-B3` | `Pending` | — | CLI mode/config/coverage/replay surfaces. |
| `G20-P7-B4` | `Pending` | — | Agent Activity sessions. |
| `G20-P7-B5` | `Pending` | — | Authorized MCP surfaces. |
| `G20-P8-B1` | `Pending` | — | Determinism, corruption and property hardening. |
| `G20-P8-B2` | `Pending` | — | Performance and allocation gates. |
| `G20-P8-B3` | `Pending` | — | Dependency, architecture, drift and clean-checkout audits. |
| `G20-P8-B4` | `Pending` | — | Release evidence, docs and immutable snapshot registration. |

## Decision ledger

| Date | Decision | Reason |
|---|---|---|
| 2026-08-01 | Create Goal 20 as a runtime goal separate from immutable Goal 09 evidence. | Candidate reference completeness is not executable behavior and historical hashes must remain unchanged. |
| 2026-08-01 | Reuse the Goal 14 phase structure while freezing Swarm-specific mechanics and denominators. | Gold and Gears proved the intended generic Activity, battle, replay and adapter extension path. |
| 2026-08-01 | Execute on `codex/goal20-swarm-disaster-runtime` in the current worktree. | The user explicitly requested no new worktree; the prior public-API change was committed and the checkout was clean before Goal 20 authoring. |
| 2026-08-01 | Treat all 31 Candidate policy boundaries as unresolved until P0 assigns terminal runtime owners. | Candidate policy labels do not authorize a Released execution claim. |
| 2026-08-01 | Resolve same-boundary Phase 3 work in five tiers: movement/Countdown, face, explicit map replacement, Communing choice, then cabinet points. | Released data does not expose the full cross-family order. The single-program order is visibly `ProjectPolicy`, preserves ordinary Activity ownership and transactional rollback, and must be revisioned if stronger released evidence replaces it. |

## Policy register

`G20-P0-B2` froze the exact 31-row assignment register from Goal 09. Runtime
resolution evidence does not rewrite that historical assignment artifact.
`G20-P2-B2` resolves `topology_policy`, `G20-P2-B3` resolves `domains` plus
`topology_consequences`, and `G20-P2-B5` resolves `boss_choices`, as explicitly
labeled `VersionedExecutablePolicy` boundaries. Boss choice records the caller's
displayed candidate and requires a separately typed released Boss Decay
threshold, without claiming the unresolved encounter join. The `beacons`
boundary remains `InheritedPolicy` until its second owner `G20-P4-B5`; the other
23 unresolved rows also remain inherited.
`G20-P2-B4` implements the Activity-side Countdown, Disarray and boss-decay
selection boundaries, but their three policy rows accurately remain inherited
until the final-boss BattleSpec and settlement owner `G20-P6-B3` executes.
`G20-P3-B1` resolves the Audience Dice and Audience Path policy boundaries as
explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy` definitions,
unlock authorization and once-only Activity initialization. Their full mechanic
rule execution and semantic fixture remain pending for `G20-P5-M04` and
`G20-P5-B1` without reopening either policy row.
`G20-P3-B2` resolves the six-record dice-roll-controls boundary as an explicitly
labeled `VersionedExecutablePolicy`/`ProjectPolicy` with typed charges, labeled
RNG purposes, exact abandon authorization/reward and fail-closed lifecycle.
The associated mechanic rule and semantic fixture remain pending for
`G20-P5-M04` and `G20-P5-B1` without reopening the policy row.
`G20-P3-B3` resolves the 44-record dice-target boundary as an explicitly
labeled `VersionedExecutablePolicy`/`ProjectPolicy`: each face validates its
typed effect filter, candidates sort by stable domain then node identity, and
an empty legal set commits a no-op with zero additional draws. Its complete
mechanic rule and semantic fixture remain pending for `G20-P5-M04` and
`G20-P5-B1` without reopening the policy row.
`G20-P3-B4` resolves the Communing-choice, Communing-dimension and
Pathstrider-cabinet policy boundaries as explicitly labeled
`VersionedExecutablePolicy`/`ProjectPolicy` contracts: choices increment one
Aeon counter without direct permanent points, Activity-owned dimension points
carry exactly and clamp after every authored ordered increment, and cabinet
unlock edges are inverted into exact objective-gated prerequisites. Their two
mechanic rules and semantic fixtures remain pending for `G20-P5-M05` and
`G20-P5-B1` without reopening the three policy rows.
`G20-P4-B1` resolves the 112-record Communing Trail prerequisite boundary as
an explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`: within each
dimension the released point threshold and stable Talent identity form one
nine-node chain, and each non-root node requires its immediate predecessor.
The exact Trail effect mechanic rule and semantic fixture remain pending for
`G20-P5-M06` and `G20-P5-B1` without reopening the policy row.
`G20-P4-B2` resolves the 33-record Pathstrider-objective boundary as an
explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`: one exact
external quest-completion condition routes to one cabinet transaction after an
accepted Activity operation. The 114-record Pathstrider-unlock boundary stays
`InheritedPolicy` because `G20-P4-B3` still owns its Path/Propagation consumers;
the progress mechanic rule and semantic fixture remain pending for
`G20-P5-M06` and `G20-P5-B1`.
`G20-P4-B3` resolves the mechanical-chapter-locator and Pathstrider-unlock
boundaries as explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`
contracts: existing chapter flags remain Activity-owned, every selected Path
binds its account authorization to a released Pathstrider unlock row, and
Propagation uniquely binds `1000008`. Six random content grants stop at stable
deferred requests until their content-pool owner executes them. The
Path/Resonance boundary therefore remains `InheritedPolicy` for real nested
battle materialization in `G20-P6-B3`, while the shared-pool-weight boundary
remains inherited for `G20-P4-B5`; the two formal Path/Interplay rules and
fixtures remain pending for `G20-P5-M07` and `G20-P5-B1`.
`G20-P4-B5` resolves the abstract-Adventure, beacon, Occurrence-pool,
Occurrence-random-outcome, Service-transaction and shared-pool-weight
boundaries as explicitly labeled `VersionedExecutablePolicy`/`ProjectPolicy`
contracts. Occurrence and Shop randomness use separate labeled streams over
stable candidates; non-shop prices must match exact inherited values; Service
and Adventure settlements remain ordinary atomic Activity programs. Action
minigame input and unavailable tier reward tables remain excluded, so callers
must provide the displayed shop price or validated external Adventure payload.
The two formal mechanic rules and semantic fixtures remain pending for
`G20-P5-M09`, `G20-P5-M10` and `G20-P5-B1` without reopening these policy rows.
`G20-P6-B1` resolves the rooms boundary as an explicitly labeled
`VersionedExecutablePolicy`/`ProjectPolicy`: all 861 released rows retain their
published section membership, zero room/domain/group joins are invented from
numeric IDs, and selection proceeds only after the current Activity map overlay
provides a typed combat domain. The executable encounter selector and all 20
difficulty schedules are now present, but the multi-owner encounter-selection
and encounter-difficulty rows remain truthfully `InheritedPolicy` until
`G20-P6-B3` applies their final battle-consequence and settlement responsibilities.
`G20-P6-B2` materializes the current Activity snapshot into a real immutable,
construction-validated `BattleSpec`: it preserves exact authored encounter
waves and slots, composes all 71 source enemy identities, applies current shared
battle contributions and Planar Disarray modifiers, and hashes mode-owned Path,
Interplay, Die, Trail and Curio contribution identity. It intentionally does not
execute or settle that battle. Encounter-selection, encounter-difficulty and
Path/Resonance therefore remain `InheritedPolicy` until `G20-P6-B3` proves
nested execution, carry, consequence and atomic Activity settlement.
`G20-P6-B3` completes that final owner: selected encounters and their effective
levels execute in the real nested battle, current Disarray and shared
Path/base-Resonance rules affect combat, explicit boss choice plus stable Boss
Decay descriptors are bound into the immutable final input, and verified
results settle carry and post-battle consequences atomically. The mode-owned
Path/Interplay and Boss Decay bindings accurately claim exact identity and
canonical parameter application, not unavailable observed numeric parity. The
six remaining P6-B3 rows are terminal `VersionedExecutablePolicy`/
`ProjectPolicy` boundaries. The current ledger also reconciles the already
terminal profile and Trailblaze contracts proved by `G20-P5-M01` and the rooms
contract proved by `G20-P6-B1`; all 31 policy boundaries are now terminal.

## Terminal checklist

- [ ] Immutable prerequisites and protected roots pass.
- [ ] All 65 Candidate tables load privately and validate.
- [x] 6,963/6,963 source obligations have exact-once runtime dispositions.
- [x] 23/23 mechanic rules have terminal execution evidence/dispositions.
- [x] All 23 semantic fixture families execute against production values.
- [x] All 31 inherited policy boundaries are terminal and accurately labeled.
- [x] Entry, topology, Countdown/Disarray, dice and Communing systems execute.
- [x] Progression, content, services, Adventure and encounters execute.
- [x] Real nested battles use current Activity state and verified projections.
- [ ] The seeded matrix completes and freshly verifies every replay.
- [ ] CLI, baseline AI, agent API and MCP offered-command parity passes.
- [ ] Cross-platform determinism and RNG-isolation goldens pass.
- [ ] Performance, dependency, architecture and generated-drift audits pass.
- [ ] Prior-release compatibility and the full clean-checkout gate pass.
- [ ] `G20-P8-B4` is committed and the completion snapshot is registered.

## Completion record

Not yet released.
