# Goal 18 Launch Prompt — Apocalyptic Shadow Reference Data

Execute or resume Goal 18 in the dedicated
`codex/goal18-apocalyptic-shadow-reference` worktree.

Read completely before mutation:

- `AGENTS.md`;
- `docs/goals/18-apocalyptic-shadow-reference-data.md`;
- `docs/goals/18-apocalyptic-shadow-reference-data-status.md`;
- `docs/18-standard-and-challenge-modes.md`;
- `docs/15-content-data-and-coverage.md`;
- `docs/content-reference/authoring-contract.md`; and
- `docs/sources.md`.

Then repeatedly select the earliest unblocked batch. Keep exactly one batch
`InProgress`, complete its data/tools/evidence/docs together, update the ledger,
run its focused verifier and the change-aware repository gate, create an atomic
Conventional Commit whose subject includes the exact `G18-*` batch ID, push it
and verify the remote tip. Continue through every phase without pausing at a
plan, manifest, workbook, evaluator or phase boundary.

Use only the six Goal 18 artifact roots plus its three goal documents/index.
Never modify runtime crates, another goal's artifacts or shared challenge
semantics. Shared Goal 13/15/17 source rows receive reconciliation receipts,
not duplicated ownership or cross-worktree edits.

Use only released Version 4.4 evidence at the pinned revisions. Freeze active
group `3019` and Tierce `30195` by selector/reachability evidence; keep group
`3020` and historical periods exclusion-only. Reject leaks, previews, story,
rewards, assets and account/calendar behavior.

Do not stop until every terminal gate is evidenced, the immutable Candidate
snapshot is registered, runtime rows equal zero, the final branch is pushed and
the remote commit is verified. If genuinely blocked, record the exact command,
error, safe alternatives attempted and the smallest required external action.
