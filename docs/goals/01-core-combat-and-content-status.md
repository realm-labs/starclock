# Goal 01 Status — Complete Core Combat and Released Character Content

This file is the persistent execution ledger for
[Goal 01](01-core-combat-and-content.md). The executor must update it in the same
commit as every implementation or content batch.

## Goal state

| Field | Value |
|---|---|
| Goal ID | `core-combat-v1` |
| State | `InProgress` |
| Active phase | Phase 7 — Complete released content import |
| Next unblocked batch | `G01-P7-C06` (`InProgress`) |
| Last completed batch | `G01-P7-C05` |
| Last completed commit | This row's containing commit |
| Goal plan baseline | Phase 0 complete; bound to frozen manifests and coverage evidence |
| Content prerequisite | Verified and bound by `G01-P0-B1` |
| Content reference digest | `0dca8ae581b4fa1e9fe8ce0c9e67ac6eb72c251deacbd4831751ce685e45ef5a` |
| Goal manifest digest | `e2188c7844d678253c98d569db017dbad7101541cf502aba4c2eb80c0435bf19` |
| Blocking condition | None |

Allowed goal states are `ReadyToStart`, `InProgress`, `Blocked` and `Complete`.
Use `Blocked` only when no independent batch can progress and record the exact
external decision or evidence required. Phase completion alone never changes the
goal to `Complete`.

## Frozen manifest counters

These totals are populated by `G01-P0-B1` from machine-readable manifests. Do
not manually estimate missing totals.

| Required manifest | Digest | Required | DataReady | Disabled announced | Coverage |
|---|---:|---:|---:|---:|---:|
| Released character combat forms | `d83b6a1621f47ed6964e164b560b317288f1a8a3e53ac0ed3654d5df8ff815e9` | 88 | 46 | 2 announced outside enabled pack | 52.3% runtime data |
| Released Light Cones | `317afdf6480e58d7396d781b5d23b6377d90b03ca1fc24e62b88ffbb3f4b9994` | 165 | 0 | 0 | 0% runtime data |
| `standard-v1` enemies/variants | `a9fdd427940738a712cbf4b796281113a9df3c2f7465c5f51c76356885eeb84b` | 17 exact variants | 17 | 0 | 100% runtime data |
| `standard-v1` encounters | `a9fdd427940738a712cbf4b796281113a9df3c2f7465c5f51c76356885eeb84b` | 6 | 6 | 0 | 100% runtime data |
| `standard-v1` scenarios | `a9fdd427940738a712cbf4b796281113a9df3c2f7465c5f51c76356885eeb84b` | 6 | 6 | 0 | 100% runtime data |

Disabled announced entries are recorded for audit but are never included in a
required-coverage denominator until public release and a new manifest revision.

## Phase ledger

| Phase | State | Exit evidence |
|---|---|---|
| Phase 0 — Freeze scope and evidence | `Complete` | Frozen manifest `e2188c7844d678253c98d569db017dbad7101541cf502aba4c2eb80c0435bf19`; provenance evidence `e629313eee624ccb124036ec6fd4664df9ca761e392d026ce6f2f7c34a184466`; current research evidence `00b0f3054552304f28fd32f3b2d07193eb814a764e8a152967572eee6f04a7ea`; current coverage `eb50a6fe807b7ca3d0180a76cfcccc32910566f77fcbf5b36cc9884976241c42`. All 283 required entries are accounted, 76 entries are `GoldenVerified`/`DataReady`, and two announced forms remain disabled outside the denominator. |
| Phase 1 — Workspace and reproducible data foundation | `Complete` | The nine-crate workspace, locked compiler/dependency/tool inventory, local/CI gates, Sora 0.3.0 capability proof, all 80 schema tables, production `.xlsx` authoring/export/generated-reader path and validated catalog boundary are complete. Production clean regeneration remains at `120fe823…089e`; its 283 disabled identities construct an immutable empty-domain catalog. The separate 13-table representative fixture regenerates twice to bundle `4add1383…b070`, lowers one character/ability/hit plan and is rejected by the production loader. Full repository/source-cache gates pass with 16 generated checks. |
| Phase 2 — Deterministic primitives | `Complete` | Checked numeric domains and formula oracle, typed immutable catalogs, SHA-256-derived ChaCha8 integer RNG, canonical replay framing/streaming hash and fixed-seed shrinking property harness all pass native Windows x64 tests and Windows ARM64 compile-only checks. Cross-platform native execution remains a Phase 8 retained-evidence gate and is not inferred from compilation. |
| Phase 3 — Executable combat vertical slice | `Complete` | The synthetic Standard battle runs to `Won`, replays every command against its authoritative hash, retains the versioned Phase 3 benchmark baseline, and passes fixed-seed command/rollback/replay-corruption properties. B1-B8 are committed through `9564ba2`. |
| Phase 4 — Complete shared combat kernel | `Complete` | B1-B10 establish the typed Rule IR, all shared formula/effect/Toughness/reaction/lifecycle/Elation/enemy mechanics and the audited empty native-handler registry. B11 passes the complete core/probe golden suite, retains the Phase 3 benchmark by digest, adds a genuine five-operation full-kernel workload and five-sample strict report, and resolves all 21 Phase 4 research blockers by six observations plus explicit generic project-policy decisions. Those decisions retain V1B observation gates and do not promote content coverage; the ten later Himeko cases are resolved separately by Phase 7 M01. |
| Phase 5 — Build compiler, Traces, Eidolons and Light Cones | `Complete` | B1-B5 implement the independent pure build catalog/compiler, exact character/ability/Trace/Eidolon/Light Cone selection, generic combat output, source attribution, canonical definition/catalog/build/spec digests, named presets and build locks. B6 adds the versioned explicit-empty relic/planar seam and advances the selected-build digest domain to v2 without importing deferred data. Representative E0/E6 and S1/S5 builds, valid/invalid wearer behavior, canonical order, Windows x64 runtime tests and Windows ARM64 compile-only checks pass; production remains Excel/Sora-only at 0/283 `DataReady`. |
| Phase 6 — Standard orchestration, AI, CLI and replay | `Complete` | B1-B5 establish the minimum Activity/Standard aggregate, deterministic controllers, replay envelope and CLI contracts. B6 imports the exact frozen 17-variant/6-encounter/6-scenario manifest through production Excel/Sora, lowers 63 source-bound enemy abilities and 17 finite AI graphs, validates phase/link/multi-wave topology, and runs every declared seed to `Won` with committed event counts and final state hashes; the CLI emits and verifies production-scenario replays. |
| Phase 7 — Complete released content import | `InProgress` | Generic batches M01-M13 supply production build/rule/hit/lifecycle execution, effective-level formulas, exact event observations, bounded 101-hit actions, named action-resource costs, effect-granted Assist Skills, provider/caller attribution, universal Toughness hits and forced no-cost companion actions. V1B through C05 promote 46 forms through complete statistics, abilities, Techniques, Traces, E1-E6, representative programs and execution goldens. Coverage is 76/283 overall and 46/88 character forms; C06 is next. |
| Phase 8 — Hardening and documentation freeze | `Pending` | None |

Allowed batch/phase states are `Pending`, `InProgress`, `Researching`, `Blocked`,
`Complete` and `NotApplicable`. `NotApplicable` requires a decision-log entry and
may not be used for a required acceptance gate.

## Batch ledger

Add one row per concrete batch. Expand the Phase 7 partition families after the
Phase 0 manifests are frozen.

| Batch | State | Commit | Validation evidence | Notes |
|---|---|---|---|---|
| `G01-P0-B1` | `Complete` | `3c04cb1` | `node tools/content-reference/verify.mjs content-reference/v4.4`; `node tools/goal-manifest/generate.mjs --check`; `node tools/goal-manifest/verify.mjs`; `cargo fmt --all -- --check`; `cargo test`; `git diff --check`. Frozen evidence: [`manifest-index.json`](../../content-manifests/core-combat-v1/manifest-index.json), [`standard-v1.json`](../../content-manifests/core-combat-v1/standard-v1.json), [`partitions.json`](../../content-manifests/core-combat-v1/partitions.json). | Bound reference pack `0dca8ae5…f5a`; froze goal manifest `e2188c78…f19`, 88 forms, 165 Light Cones, 17 exact enemy variants, 6 encounters and 6 scenarios. |
| `G01-P0-B2` | `Complete` | `6654540` | `node tools/goal-provenance/generate.mjs --check`; `node tools/goal-provenance/verify.mjs`; `node tools/content-reference/verify.mjs content-reference/v4.4`; `node tools/goal-manifest/generate.mjs --check`; `node tools/goal-manifest/verify.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `git diff --check`. Evidence: [`evidence-index.json`](../../evidence/core-combat-v1/reference-binding/evidence-index.json), [`source-cache-report.json`](../../evidence/core-combat-v1/reference-binding/source-cache-report.json), [`saber-archer-audit.json`](../../evidence/core-combat-v1/reference-binding/saber-archer-audit.json). | Verified both pinned revisions and all 1,811 source hashes; regenerated all 13 pack JSON files exactly; mapped 283 frozen entries to a 3,085-record closure; all 10 explicit approximations retain evidence hashes and no approximation is unbound. |
| `G01-P0-B3` | `Complete` | `63b1fe1` | `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/content-reference/verify.mjs content-reference/v4.4`; `node tools/goal-manifest/generate.mjs --check`; `node tools/goal-manifest/verify.mjs`; `node tools/goal-provenance/generate.mjs --check`; `node tools/goal-provenance/verify.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `git diff --check`. Evidence: [`evidence-index.json`](../../evidence/core-combat-v1/research-register/evidence-index.json), [`research-cases.json`](../../evidence/core-combat-v1/research-register/research-cases.json), [`fixture-specifications.json`](../../evidence/core-combat-v1/research-register/fixture-specifications.json), [`decision-records.json`](../../evidence/core-combat-v1/research-register/decision-records.json). | Named 19 V1a, 8 cross-kit Elation and 10 Himeko Nova cases; assigned every case to a later batch; bound all cases to prepared text hashes and reproducible observation/golden envelopes; no ambiguity was converted into a convenient default. |
| `G01-P0-B4` | `Complete` | `dd2a2ae` | `node tools/content-reference/verify.mjs content-reference/v4.4`; `node tools/goal-manifest/generate.mjs --check`; `node tools/goal-manifest/verify.mjs`; `node tools/goal-provenance/generate.mjs --check`; `node tools/goal-provenance/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `git diff --check`. Evidence: [`coverage-index.json`](../../evidence/core-combat-v1/coverage/coverage-index.json), [`goal-coverage.json`](../../evidence/core-combat-v1/coverage/goal-coverage.json). | Accounted for 283/283 required entries including the Standard profile; terminal states are 188 `Cataloged`, 85 `Documented`, 10 `Researching`, zero `DataReady` and zero `GoldenVerified`; verified every goal/reference documentation counter and retained two announced forms as disabled audit-only entries. |
| `G01-P1-B1` | `Complete` | `698480c` | `node tools/workspace/verify-dependencies.mjs`; `node tools/content-reference/verify.mjs content-reference/v4.4`; `node tools/goal-manifest/verify.mjs`; `node tools/goal-provenance/verify.mjs`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/verify.mjs`; `cargo check --workspace --all-targets --all-features`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `git diff --check`. Boundary evidence: [`crates/README.md`](../../crates/README.md), [`verify-dependencies.mjs`](../../tools/workspace/verify-dependencies.mjs), [`workspace_boundaries.rs`](../../crates/starclock-cli/tests/workspace_boundaries.rs). | Replaced the placeholder root package with a virtual workspace containing exactly nine in-scope crates; `starclock-combat` has zero dependencies; no external, engine, challenge or universe dependency/crate was introduced. |
| `G01-P1-B2` | `Complete` | `2c0d779` | `node tools/dependency-policy/verify.mjs`; `node tools/workspace/verify-dependencies.mjs`; `node tools/content-reference/verify.mjs content-reference/v4.4`; `node tools/goal-manifest/verify.mjs`; `node tools/goal-provenance/verify.mjs`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/verify.mjs`; `cargo check --workspace --all-targets --all-features`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `git diff --check`. Policy evidence: [`dependency-and-tool-policy.json`](../../policy/dependency-and-tool-policy.json), [`dependency-and-tool-policy.md`](../dependency-and-tool-policy.md), [`rust-toolchain.toml`](../../rust-toolchain.toml). | Pinned Rust/Cargo 1.97.0 and Node 24.15.0; reviewed `fixnum =0.9.5` plus all three transitives and measured a 1,161 ms fresh combat check; added non-zero fixed-width definition/runtime IDs and six-decimal `Scalar`/`Ratio` wrappers without exposing `fixnum`. |
| `G01-P1-B3` | `Complete` | `0a2691f` | `node tools/repository-check/run.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Policy evidence: [`repository-checks.json`](../../policy/repository-checks.json), [`generated-drift.json`](../../policy/generated-drift.json), [`repository-check README`](../../tools/repository-check/README.md). | One pinned command verifies the exact dependency/tool inventory, nine-crate dependency direction and shared-lint inheritance, 12 handwritten Rust files against physical-line/facade limits, two explicit reviewed public re-export declarations, eight clean-checkout generated/integrity checks, formatting, Clippy and all-target/all-feature tests. The optional cache gate also reproduced the prepared pack and bound evidence as a ninth check. |
| `G01-P1-B4` | `Complete` | `e4ea293` | `node tools/sora/install.mjs`; `node tools/sora/verify-golden.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Capability evidence: [`capability-lock.json`](../../config/sora-golden/capability-lock.json), [`expected-manifest.json`](../../config/sora-golden/expected-manifest.json), [`sora-toolchain.json`](../../policy/sora-toolchain.json), [`Sora capability lock`](../sora-0.3.0-capability-lock.md). | Bound crates.io archive `90d37310…763a`, annotated tag `fe12080f…aeeb` / commit `4afadfb4…03dc`; proved check/build/schema-lock, template and preview/write sync, primary-key references, a unique index, union/ordered child-table materialization, Rust codegen/reader, `.sora` and `json-debug`. Thirteen stable artifacts digest to `30687554…5f09`; XLSX drift is semantic. Recorded and regression-proved 0.3.0's Windows formatter probe, bare clean-path, unsigned decoder and generated-reader dependency limitations. |
| `G01-P1-B5` | `Complete` | `cc27087` | `node tools/ci/verify-workflow.mjs`; `node tools/ci/write-evidence.mjs --profile windows-x64-native --output .ci-evidence/local-native.json`; `rustup target add aarch64-pc-windows-msvc --toolchain 1.97.0`; `cargo check --workspace --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/ci/write-evidence.mjs --profile windows-arm64-compile --output .ci-evidence/local-compile.json`; `node tools/repository-check/run.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Contract evidence: [CI workflow](../../.github/workflows/ci.yml), [`ci-matrix.json`](../../policy/ci-matrix.json), [platform matrix](../ci-platform-matrix.md). | Immutable action SHAs and explicit hosted labels define native Windows x64, Linux x64 and macOS ARM64 jobs that install checksum-bound Sora and invoke the shared repository runner. Paired alternate-CPU jobs run only `cargo check`; evidence records actual host/architecture, target, tools, policy hash, Sora golden digest and an explicit execution flag. Local Windows native evidence-writer smoke and Windows ARM64 compile-only check passed; no unexecuted Linux/macOS runtime claim is made here. |
| `G01-P1-B6` | `Complete` | `29a62ab` | `node tools/config-schema/verify-common.mjs --bless`; `node tools/config-schema/verify-common.mjs`; `node tools/repository-check/run.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Golden evidence: [`expected-manifest.json`](../../config/schema-fixtures/common/expected-manifest.json), [`config-schema.json`](../../policy/config-schema.json), [common schema contract](../common-configuration-schema.md). | Shared Sora tables now own global identity/bilingual metadata/version/release/coverage, source/confidence records, named evidence and ordered fact bindings, plus the config manifest. Positive `i32` transport and string-only canonical-decimal policy are mechanical; integer-only boundary tests include signed `i64` extremes. Sora check/build/schema-lock/codegen/binary/diagnostic exports reproduce 24 byte-golden files at `e8811696…14e8`; six Excel template names and negative localization/reference/uniqueness cases pass. Synthetic TOML rows stay disabled fixture evidence; production `.xlsx` and the generated reader remain owned by `G01-P1-B10`. |
| `G01-P1-B7` | `Complete` | `9a0b62c` | `node tools/config-schema/verify-character-build.mjs --bless`; `node tools/config-schema/verify-character-build.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Golden evidence: [`expected-manifest.json`](../../config/schema-fixtures/character-build/expected-manifest.json), [character/build schema contract](../character-build-configuration-schema.md). | Four schema modules own Ability/HitPlan, Character/Trace/Eidolon, the normative ten-variant closed `BuildPatch`, and Light Cone/S1-S5 facts. Typed references and unique ordered children are mechanical; the fixture proves Trace self-references, Technique binding, two-hit millionth-unit ratios, E1-E6 and scalable S1-S5 completeness, constant-rank policy, and negative missing-reference/duplicate-rank cases. Sora check/build/schema-lock/codegen/binary/diagnostic exports reproduce 73 byte hashes at `7dc7667a…2a2a`. All 16 fixture identities remain disabled `ProjectFixture` rows; Rule IR is deferred to `G01-P1-B8`, production `.xlsx` to B10 and domain validation to B11, with zero coverage promotion. |
| `G01-P1-B8` | `Complete` | `d3a882d` | `node tools/config-schema/verify-rule-ir.mjs --bless`; `node tools/config-schema/verify-rule-ir.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Golden evidence: [`expected-manifest.json`](../../config/schema-fixtures/rule-ir/expected-manifest.json), [typed Rule IR schema contract](../rule-ir-configuration-schema.md). | Six schema modules define Rule/Slot/Trigger/EventFilter, Selector, Expression/Condition, finite Program/Operation, Effect and Modifier/Snapshot families. The composed fixture proves 47 tables, ordered slots/predicates/programs, two normal triggers plus a replacement proposal, `If` and bounded `ForEach`, seven operation rows, cause-role filters, stacking/snapshot bindings and disabled static-handler metadata. Sora rejects missing expression/modifier references and duplicate trigger order; the verifier rejects cross-domain children, cyclic programs/expressions and replacement triggers reaching mutations. Check/build/schema-lock/codegen/binary/diagnostic outputs reproduce 182 byte hashes at `ea2ae5d8…7752`. All 24 identities and the handler remain disabled synthetic evidence; runtime evaluation, production `.xlsx`, domain catalogs and Activity handoff remain `G01-P4-B1`/B10/B11/B9 work, with zero coverage promotion. |
| `G01-P1-B9` | `Complete` | `dbc9073` | `node tools/config-schema/verify-standard-encounter.mjs --bless`; `node tools/config-schema/verify-standard-encounter.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Golden evidence: [`expected-manifest.json`](../../config/schema-fixtures/standard-encounter/expected-manifest.json), [enemy/Standard schema contract](../enemy-standard-configuration-schema.md). | Five schema modules define enemy templates/variants/stats/resistance/Toughness/abilities/phases/links, bounded deterministic AI, encounters/waves, the minimum generic Activity/BattleBinding/ResultProjection seam and Standard profiles/scenarios. The composed fixture proves 80 tables, 40 disabled identities, two mechanically distinct variants, a linked summon and Exo-Toughness layer, enemy Ability/HitPlan, mandatory AI fallback, one encounter wave, a bounded one-battle Activity graph, four unique opaque build/spec locks and an exact four-field result projection. Sora rejects missing variant/projection references and duplicate AI order; semantic negatives reject graph cycles/unreachable terminals, missing AI fallback, battle-scoped Activity slots, duplicate/unlocked participants, undeclared Standard result fields and challenge flags. Check/build/schema-lock/codegen/binary/diagnostic outputs reproduce 274 byte hashes at `17c1e816…dfcb`. No challenge, universe, shop, reward, account, season, score or clock table was added; fixture rows grant zero coverage, production `.xlsx` remains B10 and domain catalogs remain B11. |
| `G01-P1-B10` | `Complete` | `0090299` | `node tools/config-production/generate-bootstrap-policy.mjs`; `node tools/config-production/verify.mjs --bless`; `node tools/config-production/verify.mjs`; `cargo fmt --manifest-path tools/workbook-bootstrap/Cargo.toml -- --check`; `cargo clippy --manifest-path tools/workbook-bootstrap/Cargo.toml --locked -- -D warnings`; `cargo test --manifest-path tools/workbook-bootstrap/Cargo.toml --locked`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Golden evidence: [`production-golden.json`](../../config/production-golden.json), [production pipeline](../production-configuration-pipeline.md), [bootstrap dependency inventory](../../policy/workbook-bootstrap-dependencies.json). | Migrated all 80 production table sources to `.xlsx`, committed the sole production Sora project, designer workbooks, schema lock, templates, private generated Rust reader, diagnostic tables and `.sora` bundle. The no-overwrite bootstrap pins calamine 0.35.0/rust_xlsxwriter 0.96.0 in an isolated 47-package lock, verifies both frozen digests, assigns stable transport IDs and preserves all 283 entries as disabled initial `Cataloged`/`Documented`/`Researching` rows with evidence bindings. Two fresh roots export byte-identically; read-only sync and existing-root refusal leave workbooks unchanged. The generated reader compiles through private serde/zstd dependencies, accepts only Sora binary bytes and rejects diagnostic JSON. The 274 stable artifacts digest to `120fe823…089e`; every executable domain table remains empty, enabled count is zero and coverage remains 0/283 DataReady. |
| `G01-P1-B11` | `Complete` | `b89ce62` | `node tools/config-production/verify.mjs`; `node tools/config-catalog/verify.mjs --bless`; `node tools/config-catalog/verify.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [catalog boundary](../catalog-construction-boundary.md), [`golden.json`](../../config/catalog-fixtures/representative/golden.json), [`config.sora`](../../config/catalog-fixtures/representative/config.sora). | The public production loader accepts only real Sora bytes, validates compatibility/identity/provenance/evidence/release state, rejects fixture labels, disabled executable rows and any populated table without reviewed lowering, and returns an immutable `Arc` with no generated types. The current production bundle builds a 283-identity/zero-domain catalog. Two fresh 80-workbook representative roots export byte-identically at `4add1383…b070`; the fixture privately lowers one character, one ability and one exact hit plan through positive typed IDs and integer-only signed-millionth decimals. Coverage remains 0/283 DataReady. |
| `G01-P2-B1` | `Complete` | `2d4486c` | `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `cargo check -p starclock-combat --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [numeric implementation](../numeric-implementation.md), [`numeric_golden.rs`](../../crates/starclock-combat/tests/numeric_golden.rs). | Numeric policy `fixed-i64-6dp-v1` now owns checked `Scalar`/`Ratio`, stat/Speed/ActionGauge/probability and integral HP/shield/damage/healing/Toughness domains. One promoted integer quotient implements Floor/Ceil/toward-zero/away-zero/nearest-away/nearest-even for both signs and exact ties; overflows, zero division, invalid conversion and domain violations are typed. Eight authoritative source roots mechanically reject float types. Native vectors pass and the combat crate compiles for Windows ARM64; no full-workspace cross-target claim is made from this local shell. |
| `G01-P2-B2` | `Complete` | `28718fb` | `cargo check -p starclock-combat --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [catalog implementation](../combat-catalog-implementation.md), [`catalog_contract.rs`](../../crates/starclock-combat/tests/catalog_contract.rs). | Definition and runtime IDs are fixed-width non-zero newtypes and cannot cross type boundaries. The public builder accepts only domain definitions, sorts ten private typed tables, preserves only explicitly authored execution order and rejects invalid identity, duplicates, noncanonical sets, unresolved references and static program cycles. Opposing insertion orders produce identical indexes; Windows ARM64 combat compilation passes. |
| `G01-P2-B3` | `Complete` | `6f4f7b9` | `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `cargo test -p starclock-combat --test rng_golden`; `cargo check -p starclock-combat --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [RNG implementation](../rng-implementation.md), [`rng_golden.rs`](../../crates/starclock-combat/tests/rng_golden.rs), [dependency inventory](../../policy/dependency-and-tool-policy.json). | Exact `rand =0.10.2` (`std`,`chacha`) and `sha2 =0.11.0` (no defaults) plus nine new packages are reviewed; a fresh locked combat check measured 3,500 ms. The live wrapper is non-cloneable, counts every raw `u64`, checks exhaustion before generator mutation, preserves stable candidate order and consumes no draw for empty/all-zero/invalid requests. Native Windows x64 goldens pass and combat compiles for Windows ARM64; no unexecuted-platform runtime claim is made. |
| `G01-P2-B4` | `Complete` | `72adc16` | `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `cargo test -p starclock-replay --test codec_golden`; `cargo check -p starclock-replay --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [codec implementation](../replay-codec-implementation.md), [`codec_golden.rs`](../../crates/starclock-replay/tests/codec_golden.rs). | Replay format/schema 1 binds `SCRP`, compatibility/config/controller/entry/build identities, seed and bounded record count. Closed kinds reserve later battle/Activity payloads without redesign; unknown kinds and malformed version/policy/ID/sequence/length/truncation/trailing bytes fail. One encoder produces exact primitive bytes and streams state hash `ee9b6541…b522`; build-aware replay bytes hash to `4822d0f3…8ef5`. Fresh locked replay check: 4,014 ms. |
| `G01-P2-B5` | `Complete` | `0b3bc8f` | `node tools/repository-check/verify-source-policy.mjs`; `cargo test -p starclock-combat --test numeric_formula_oracle`; `cargo check -p starclock-combat --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [numeric implementation](../numeric-implementation.md), [`numeric_formula_oracle.rs`](../../crates/starclock-combat/tests/numeric_formula_oracle.rs). | Six test-only suites compose the documented derived-stat, damage, DEF/RES/mitigation, sustain, Toughness/Break/Super Break, chance, Energy, Speed and Action Gauge formulas. Exact finalization and fault vectors cover all six rounding modes, both signs, exact ties, endpoints, overflow, division by zero and domain rejection; 4,096 deterministic legal multiply/divide inputs remain within the declared `0.000001` resolution of the non-authoritative `f64` oracle. All authoritative source roots remain float-free and Windows ARM64 combat compilation passes. |
| `G01-P2-B6` | `Complete` | `c4ef072` | `node tools/dependency-policy/verify.mjs`; `node tools/workspace/verify-dependencies.mjs`; `cargo test -p starclock-combat --test property_contract`; `cargo test -p starclock-replay --test property_contract`; `cargo check -p starclock-combat -p starclock-replay --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/repository-check/verify-source-policy.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [property harness](../property-test-harness.md), [`combat property_contract.rs`](../../crates/starclock-combat/tests/property_contract.rs), [`replay property_contract.rs`](../../crates/starclock-replay/tests/property_contract.rs), [dependency inventory](../../policy/dependency-and-tool-policy.json). | Exact dev-only `proptest =1.11.0` with only `std` plus 17 new locked packages are license/determinism reviewed; fresh no-run combat/replay test compilation measured 12,266 ms. Seven fixed-seed 256-case families with source-parallel regression persistence cover fixed arithmetic, RNG mapping/draw policy, catalog ordering, codec streaming/roundtrip, replay byte stability, malformed framing and arbitrary decoder input. Production dependency edges are unchanged; native Windows x64 passes and Windows ARM64 compiles without claiming runtime evidence. |
| `G01-P3-B1` | `Complete` | `b6a874c` | `cargo test -p starclock-combat --test battle_boundary`; `cargo check -p starclock-combat --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [battle aggregate boundary](../battle-aggregate-boundary.md), [`battle_boundary.rs`](../../crates/starclock-combat/tests/battle_boundary.rs). | Private tombstone-capable unit/actor stores retain canonical runtime IDs, source bindings, formations, resources and encounter state behind immutable views. Canonical `BattleSpec` validation completes before allocation; closed decision-bound commands reject stale, forged, unoffered and terminal input without mutation or RNG use. The only accepted vertical slice is explicit Start then offered Concede, avoiding placeholder ability semantics; action execution belongs to B3-B5. |
| `G01-P3-B2` | `Complete` | `325dbeb` | `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `cargo test -p starclock-combat --all-targets --all-features`; `cargo check -p starclock-combat --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [battle transaction boundary](../battle-transaction-boundary.md), [`battle_boundary.rs`](../../crates/starclock-combat/tests/battle_boundary.rs). | Accepted commands prepare one private semantic-copy scratch only after legality, append mutations/allocations/events to a bounded forward journal and swap on commit. Test-only failures at two mutation depths prove byte-identical rollback; commit-fault preserves completed cause-linked facts. Stable fault payloads persist in state. The private versioned codec streams complete state directly into SHA-256; collecting equivalence and fixed initial/Start/Concede hashes pass. Public events carry monotonic command/event identities and all distinct cause roles. |
| `G01-P3-B3` | `Complete` | `6aeb4fc` | `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `cargo test -p starclock-combat --all-targets --all-features`; `cargo check -p starclock-combat --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [action/timeline boundary](../action-timeline-boundary.md), [`battle_boundary.rs`](../../crates/starclock-combat/tests/battle_boundary.rs). | Exact rational AV selection advances only eligible actors with six-decimal floor and a complete side/formation/spawn/unit/actor tie key. Start and every turn expose a canonical pre-action Pass decision; exact offered membership precedes scratch. Executable fixture abilities lower to monotonic action/phase/hit IDs and a synchronous cause-linked lifecycle before resetting AG and selecting the next turn. The private forced-follow-up/Ultimate/extra-action queue has stable insertion/removal and canonical encoding. Fixed initial/Start/Pass/action/Concede hashes pass; target/resource/operation semantics remain B4-B5. |
| `G01-P3-B4` | `Complete` | `64a8446` | `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `cargo test -p starclock-combat --all-targets --all-features`; `cargo check -p starclock-combat --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [target/resource boundary](../target-and-resource-boundary.md), [`action_resources.rs`](../../crates/starclock-combat/tests/action_resources.rs), [`battle_boundary.rs`](../../crates/starclock-combat/tests/battle_boundary.rs). | Typed selectors offer self/Single/Blast/All commands from alive-present formation pools and lower immutable target commitments. Every hit revalidates through explicit cancel/keep/retarget/rebuild/fail policy; retarget uses journaled battle RNG and selectors declare repetition. Basic/Skill/Ultimate definitions own finite hit counts and explicit SP/Energy costs/gains. Affordability controls exact offered commands; Ultimates execute/reopen the interrupt window without consuming the normal turn. Resource facts retain before/after/overflow, personal Energy is canonical fixed-point state, and fixed Ultimate/three-hit Skill plus refreshed B3 hashes pass. |
| `G01-P3-B5` | `Complete` | `0cce897` | `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `cargo test -p starclock-combat --all-targets --all-features`; `cargo check -p starclock-combat --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [damage/lifecycle boundary](../damage-lifecycle-boundary.md), [`damage_lifecycle.rs`](../../crates/starclock-combat/tests/damage_lifecycle.rs), [`sustain.rs`](../../crates/starclock-combat/src/formula/sustain.rs). | Closed authored Damage/Heal hit operations allocate monotonic IDs, journal snapshots and resolve through pure checked fixed-point calculators before HP mutation. Golden ordinary damage is 648/720; healing retains 720 calculated, 600 effective and 120 overheal. Zero HP settles Downed then Defeated with explicit applier credit. One-wave victory, last-player loss and terminal rejection are pinned. Ordered encounter waves enter Reserved, transition only AfterAction through WaveEnded/depart/activate/WaveStarted, and a two-hit fixture proves later hits cannot spill into the next wave. Hashes: sustain `083fb46ca43a0d939bf43a093139a9ce9525dedba5ba1764a7b842baa2eff2aa`; one-wave win `c374675686ecf1f552247e2abcccf30ffc1b8b94cc984cc6573641a5e4dc0322`; wave two `3c0d57f713fd4c6e12341f5edb7830574397d0412681a57b5521e0b0650358b1`; final win `68322bd810a0adaf83ba9bb1cc9f27625c6e29e48197f1934c56c9ea06ae15de`; loss `c5fbd5dd087715c8d285c3dcff37adcaa947a958442734a50012a4ec68c201dd`. Frozen digests remain `0dca8ae581b4fa1e9fe8ce0c9e67ac6eb72c251deacbd4831751ce685e45ef5a` / `e2188c7844d678253c98d569db017dbad7101541cf502aba4c2eb80c0435bf19`; coverage remains 0/283 DataReady. |
| `G01-P3-B6` | `Complete` | `ed96028` | `node tools/dependency-policy/verify.mjs`; `node tools/workspace/verify-dependencies.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `cargo test -p starclock-mode-standard --all-targets --all-features`; `cargo test -p starclock-replay --all-targets --all-features`; `cargo test -p starclock-cli --all-targets --all-features`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [synthetic Standard/replay boundary](../synthetic-standard-replay-boundary.md), [`standard_replay_smoke.rs`](../../crates/starclock-cli/tests/standard_replay_smoke.rs), [`battle.rs`](../../crates/starclock-replay/src/battle.rs). | A smoke-only one-wave Standard profile derives the battle stream from master seed 7 and constructs `Battle` directly without claiming Phase 6 Activity/result-projection semantics. Replay schema 1 now carries versioned closed battle commands paired with authoritative hashes; one-shot verification checks all combat compatibility identities, applies every command once and reports the first rejection/divergence. Two CLI runs reproduce 3 commands, terminal `Won`, 530 bytes, replay SHA-256 `af1623db54a28aa7374ef560bf76824a4896deb922c923dc52e8465a986e4616` and final state hash `697a383ec91618282442011c89c12616736273e897c2846a793abe4d1a55f272`; a mutated first hash fails at command index 0. Frozen digests remain `0dca8ae581b4fa1e9fe8ce0c9e67ac6eb72c251deacbd4831751ce685e45ef5a` / `e2188c7844d678253c98d569db017dbad7101541cf502aba4c2eb80c0435bf19`; coverage remains 0/283 DataReady. |
| `G01-P3-B7` | `Complete` | `ea75019` | `node tools/dependency-policy/verify.mjs`; `node tools/workspace/verify-dependencies.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/benchmark/verify.mjs`; `$env:STARCLOCK_BENCH_RUNNER_ID='starclock-bench-win10-i7-10700f-v1'; node tools/benchmark/verify.mjs --strict --samples 5 --output evidence/core-combat-v1/performance/phase3-baseline-windows-x64.json`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [benchmark baseline contract](../performance-benchmark-baseline.md), [machine policy](../../policy/benchmark-workloads.json), [strict report](../../evidence/core-combat-v1/performance/phase3-baseline-windows-x64.json), [`g01_benchmark.rs`](../../crates/starclock-cli/examples/g01_benchmark.rs). | Workload revision `g01-phase3-benchmark-v1` fixes seed 7 and nine release-mode rows: 1,000 ordinary commands, a transparent eight-hit Phase 3 trigger-heavy proxy, 10,000 stale rejections, 5,000 hashes each over 2/4/8 combatants, 20×100-command and 5×500-command one-shot audits, and 16 isolated 100-command jobs on four workers sharing only one immutable catalog. Pinned dev-only `allocation-counter =0.8.1` plus feature-gated non-authoritative combat instrumentation record latency, commands/second/core, allocation count/bytes, peak bytes/job, semantic-copy bytes and journal/event/operation growth without changing the B6 goldens. The designated i7-10700F strict runner passed five-sample medians: ordinary 180,802 cmd/s/core, 3,657,640 allocated bytes and 6,324 peak bytes/job; 500-command replay 192,034 cmd/s/core and 39,344 peak bytes/job; concurrent total throughput is 3.151× sequential 100-command replay. Streaming hashes and invalid rejection allocate zero. Shared native CI enforces only the broad 30-second/2-GB/128-MiB smoke ceiling. Fresh locked release example build: 21,720 ms. Frozen digests remain `0dca8ae581b4fa1e9fe8ce0c9e67ac6eb72c251deacbd4831751ce685e45ef5a` / `e2188c7844d678253c98d569db017dbad7101541cf502aba4c2eb80c0435bf19`; coverage remains 0/283 DataReady. |
| `G01-P3-B8` | `Complete` | This row's containing commit | `cargo test -p starclock-combat battle::aggregate::tests`; `cargo test -p starclock-replay --test battle_property_contract`; `cargo check -p starclock-combat -p starclock-replay --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/repository-check/verify-source-policy.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [Phase 3 command/replay property contract](../command-replay-property-contract.md), [`aggregate.rs`](../../crates/starclock-combat/src/battle/aggregate.rs), [`battle_property_contract.rs`](../../crates/starclock-replay/tests/battle_property_contract.rs). | Three new fixed-seed ChaCha families run 256 cases and 4,096 shrink iterations each. Seed `636f6d6d616e6431` generates 1-128-step mixed accepted/forged streams and proves every rejection preserves canonical bytes/hash, RNG draws and decision while independent battles reproduce every `Resolution` and command hash. Seed `726f6c6c6261636b` covers valid prefixes 0-64 and proves early/late Rollback injection converges to byte-identical faulted state and collecting/streaming hashes. Seed `626174746c652d31` mutates a verified 64-command replay across truncation, trailing bytes, config identity, command/hash payloads, payload version, record kind and sequence; every case rejects. Native Windows x64 passes and combat/replay test targets compile for Windows ARM64 without claiming target execution. Phase 3 exit gate is complete: the synthetic Standard battle runs to victory, verifies every command hash, has a versioned benchmark baseline and accumulated properties. Frozen digests remain `0dca8ae581b4fa1e9fe8ce0c9e67ac6eb72c251deacbd4831751ce685e45ef5a` / `e2188c7844d678253c98d569db017dbad7101541cf502aba4c2eb80c0435bf19`; coverage remains 0/283 DataReady. |
| `G01-P4-B1` | `Complete` | This row's containing commit | `cargo test -p starclock-combat --test rule_ir_contract`; `cargo test -p starclock-rules --all-targets --all-features`; `cargo check -p starclock-combat -p starclock-rules --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/workspace/verify-dependencies.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [typed Rule IR runtime boundary](../rule-ir-runtime-boundary.md), [`rule_ir_contract.rs`](../../crates/starclock-combat/tests/rule_ir_contract.rs), [`registry_contract.rs`](../../crates/starclock-rules/tests/registry_contract.rs). | Executable catalog rules now retain generic source/digest/tags, six closed value kinds, battle-owned typed slots with bounds/visibility/persistence/reset policy, event filters, checked expressions/conditions, finite `If`/bounded-`ForEach` programs, seven trigger phases, signed priorities and eight once scopes. Catalog validation rejects malformed/mistyped/cyclic/cross-reference/replacement/handler shapes and compiles immutable event/phase trigger groups ordered by priority/source/rule/trigger; complete runtime keys add side/formation/spawn/instance/insertion. Read-only evaluation returns only typed resolver proposals under independent step/emission/iteration budgets; once keys commit only after success. The versioned static registry requires sorted IDs, schema/version/domain match, determinism/owner/removal metadata and an IR-insufficiency decision. Its only handler is test-local synthetic equivalence evidence; zero content handlers and zero V1a rows are admitted. Native Windows x64 passes and Windows ARM64 compiles without a runtime claim. Frozen digests remain `0dca8ae581b4fa1e9fe8ce0c9e67ac6eb72c251deacbd4831751ce685e45ef5a` / `e2188c7844d678253c98d569db017dbad7101541cf502aba4c2eb80c0435bf19`; coverage remains 0/283 DataReady. |
| `G01-P4-B2` | `Complete` | `db7e251` | `node tools/config-probes/verify-asta-modifier.mjs`; `cargo test -p starclock-combat --test modifier_pipeline`; `cargo test -p starclock-data probe_tests`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo check -p starclock-combat -p starclock-rules --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/repository-check/run.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [modifier runtime boundary](../modifier-and-snapshot-runtime-boundary.md), [Asta probe golden](../../config/probes/v1a/asta-modifier/golden.json), [`modifier_pipeline.rs`](../../crates/starclock-combat/tests/modifier_pipeline.rs), [`probe_tests.rs`](../../crates/starclock-data/src/probe_tests.rs). | Replaced the modifier identity placeholder with the catalog-owned immutable registry. Stat Rule IR uses checked fixed-point stages and a read-only recursive `QueryStat` leaf; all nine group policies, source/instance tie-breaks, named caps, context filters, full and partial snapshots, deterministic cycle paths and cache-on/off equality are executable. Pinned Sora 0.3.0 exports the dedicated 80-workbook Asta scope twice to bundle `63b13864…faed`; the production reader lowers six expressions, one bounded 0-5 Charging slot and one dynamic ReplaceGroup aura. One modifier follows 0/1/5/3 stacks at exact level-10 ratio `0.14`; a seeded repeat/Fire-weak sequence yields `1/2/0/1` credit. Both B2 research cases are `Observed`; four disabled `ProjectFixture` identities are rejected by production loading and grant zero coverage. Native Windows x64 tests and Windows ARM64 combat/rules compile-only evidence pass; `starclock-data` is not claimed for ARM64 from this shell because its existing `zstd-sys` build requires unavailable `cl.exe`. Research digest is `7c1f907f…f19c`, coverage is `99774c47…22e9`, and DataReady remains 0/283. |
| `G01-P4-B3` | `Complete` | This row's containing commit | `node tools/config-probes/verify-firefly-damage.mjs`; `cargo test -p starclock-combat --test damage_sustain_pipeline --all-features`; `cargo test -p starclock-combat --test damage_lifecycle --all-features`; `cargo test -p starclock-data probe_tests --all-features`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo check -p starclock-combat -p starclock-rules --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/repository-check/run.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [damage/sustain runtime boundary](../damage-sustain-and-hp-runtime-boundary.md), [Firefly probe golden](../../config/probes/v1a/firefly-damage/golden.json), [`damage_sustain_pipeline.rs`](../../crates/starclock-combat/tests/damage_sustain_pipeline.rs), [`damage_lifecycle.rs`](../../crates/starclock-combat/tests/damage_lifecycle.rs), [`probe_tests.rs`](../../crates/starclock-data/src/probe_tests.rs). | Ordinary direct/DoT/additional damage now retains mixed scaling, CRIT eligibility/result, additive DMG Boost, Weaken, actual/level DEF, clamped RES/PEN, additive vulnerability, multiplicative mitigation and broken state as named checked stages with one integral finalization. Healing, shield creation and HP consumption retain raw/effective/overflow outcomes. The battle owns monotonic separately encoded shield instances; concurrent-largest and additive policies are deterministic, and damage journals/emits every shield mutation before HP overflow. Rule IR and the generated-reader boundary lower typed damage, true-damage, heal, shield, HP-consume, Energy, effect, advance and countdown proposals. Pinned Sora 0.3.0 exports five disabled Firefly fixtures twice to bundle `260363d9…b2b`; source payload `edd2cf12…f19c` binds ordered 40% Max HP/floor-1, 60% Max Energy, 200% ATK damage and Ultimate countdown/RedMode/advance/Energy operations. Both B3 cases are `Observed`; production rejects the probe and coverage remains 0/283. Native Windows x64 tests and Windows ARM64 combat/rules compile-only evidence pass. Research digest is `973339d1…96f1`; coverage is `3725d863…8a00`. |
| `G01-P4-B4` | `Complete` | This row's containing commit | `node tools/config-probes/verify-firefly-damage.mjs`; `cargo test -p starclock-combat --test toughness_formula`; `cargo test -p starclock-combat --test damage_lifecycle`; `cargo test -p starclock-data probe_tests`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo check -p starclock-combat -p starclock-rules --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/repository-check/run.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [Toughness/Break runtime boundary](../toughness-break-runtime-boundary.md), [Firefly probe golden](../../config/probes/v1a/firefly-damage/golden.json), [`toughness_formula.rs`](../../crates/starclock-combat/tests/toughness_formula.rs), [`damage_lifecycle.rs`](../../crates/starclock-combat/tests/damage_lifecycle.rs), [`probe_tests.rs`](../../crates/starclock-data/src/probe_tests.rs). | Ordered ordinary/Exo/sequential/shared layers retain eligibility, locks, broken routing, recovery, depletion behavior and typed event-source credit; reduction never spills implicitly. Checked fixed-point goldens pin attempted/effective Toughness, initial Break and Super Break plus all seven base effects. Timed weakness, retained DoT/delay/speed/skip/stack effects, turn-start lethal settlement and canonical views/codec are executable without character branches. Rule IR/generated lowering covers weakness, Toughness, Break, Super Break and layer proposals. Pinned Sora 0.3.0 exports eight disabled Firefly fixtures twice to bundle `62d6b0ef…73ba`; enhanced Skill orders two-turn Fire weakness before 90 raw reduction and the 50% branch uses effective 50/40/0 ordinary/Exo/empty samples. Both B4 cases are `Observed`; production rejects the probe. Native Windows x64 and Windows ARM64 combat/rules compile-only gates pass. Research is `d167ec70…d87b`, coverage is `f3fd70f8…55f4`, and DataReady remains 0/283. |
| `G01-P4-B5` | `Complete` | This row's containing commit | `node tools/config-probes/verify-asta-modifier.mjs`; `node tools/config-probes/verify-kafka-dot.mjs`; `cargo test -p starclock-combat --test effect_resource_pipeline`; `cargo test -p starclock-data probe_tests`; `node tools/benchmark/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo check -p starclock-combat -p starclock-rules --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [effect/DoT/resource runtime boundary](../effect-dot-and-resource-runtime-boundary.md), [Asta probe golden](../../config/probes/v1a/asta-modifier/golden.json), [Kafka probe golden](../../config/probes/v1a/kafka-dot/golden.json), [`effect_resource_pipeline.rs`](../../crates/starclock-combat/tests/effect_resource_pipeline.rs), [`probe_tests.rs`](../../crates/starclock-data/src/probe_tests.rs). | Authoritative effect instances implement all eight stack policies, seven duration clocks, deterministic tick phases, crowd-control masks, cleanse/dispel queries and canonical teardown/view/codec state. Chance, Energy, Skill Points, named character resources and bounded typed rule slots use checked fixed point; aggro weighted selection preserves authored order and consumes no draw for a zero pool. DoT tick/detonation stays target-local/tagged and retains original applier/source/snapshot/duration/stack attribution. Rule IR/Sora lowering adds effect apply/remove/detonate, resource and state-slot mutation without HP shortcuts or character-ID branches. Pinned Sora 0.3.0 exports the extended Asta scope to bundle `a534b817…672d` and the new Kafka scope to `a84065da…e1`; Kafka's combined prepared source payload is `1e581889…59e3`. The probes constrain implementation but do not claim the still-unobserved Asta decay/order and Kafka mixed-category/dynamic-snapshot envelopes; all 13 remaining cases stay `Researching` for B6/B11. Production rejects both probes and coverage remains 0/283. Canonical state additions rebless the nine deterministic Phase 3 benchmark hashes without changing workload shape, replay sizes or budgets. Source policy passes after private build/effect lowering splits keep every handwritten file below 1,200 lines. Research remains `d167ec70…d87b`; coverage remains `f3fd70f8…55f4`. |
| `G01-P4-B6` | `Complete` | This row's containing commit | `node tools/config-probes/verify-clara-counter.mjs`; `cargo test -p starclock-combat --test reaction_scheduler --all-features`; `cargo test -p starclock-data probe_tests --all-features`; `node tools/benchmark/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo check -p starclock-combat -p starclock-rules --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/repository-check/verify-source-policy.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [reaction/extra-action runtime boundary](../reaction-and-extra-action-runtime-boundary.md), [Clara probe golden](../../config/probes/v1a/clara-counter/golden.json), [`reaction_scheduler.rs`](../../crates/starclock-combat/tests/reaction_scheduler.rs), [`probe_tests.rs`](../../crates/starclock-data/src/probe_tests.rs). | A private transaction-local reaction queue drains `AfterHit`, `AfterPhase`, `AfterAction` and `BeforeTimeline` with a complete boundary/priority/side/formation/spawn/source/rule/instance/trigger/actor/ability/insertion order and a transaction-wide 256-reaction budget. Follow-up, counter, extra-turn, extra-action and delayed-action families share one automatic action envelope without owning the normal turn; control and target invalidation explicitly cancel before declaration without RNG or implicit fallback, while the queued cause retains the attacked ally and attacker independently from the reacting actor. Rule IR and generated Sora lowering add queue/delay/extra-turn emissions and real trigger/condition/phase/once/priority rows, rejecting unsupported points rather than dropping semantics. Pinned Sora 0.3.0 exports six disabled Clara fixture identities across 80 tables to bundle `e5bcde8a…96e6`; the Talent/Ultimate/Skill source hashes are retained and production rejects the probe. Its executable target/cause/charge evidence is partial only, so all four Clara cases and all 13 remaining V1a cases stay `Researching`; coverage remains 0/283. Reaction scratch and its insertion counter are not canonical state, preserving the existing hash revision while requiring an empty queue at commit. Research is `cb220b29…abda`; coverage is `10c1718f…b9b`. Source policy passes after queue-action lowering moved into a responsibility-bounded private module. |
| `G01-P4-B7` | `Complete` | This row's containing commit | `node tools/config-probes/verify-aglaea-memosprite.mjs`; `node tools/config-probes/verify-firefly-transform.mjs`; `cargo test -p starclock-combat --test linked_lifecycle --all-features`; `cargo test -p starclock-data probe_tests --all-features`; `node tools/benchmark/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo check -p starclock-combat -p starclock-rules --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/repository-check/verify-source-policy.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [lifecycle runtime boundary](../summon-memosprite-and-transformation-runtime-boundary.md), [Aglaea probe golden](../../config/probes/v1a/aglaea-memosprite/golden.json), [Firefly transform probe golden](../../config/probes/v1a/firefly-transform/golden.json), [`linked_lifecycle.rs`](../../crates/starclock-combat/tests/linked_lifecycle.rs), [`probe_tests.rs`](../../crates/starclock-data/src/probe_tests.rs). | Linked combatants retain distinct owner/unit/timeline identities and explicit source attribution; countdowns remain timeline-only. Canonical links own defeat/departure/wave policies, active primary participants alone determine terminal state, and summon/transform/revive/despawn mutations remain journaled and rollback-safe. Transformation atomically replaces form/abilities/presence, optionally owns a countdown, and restores original state exactly once. Goldens prove memosprite automatic-action attribution, countdown teardown, owner-defeat-before-revive settlement and `WaveEnded`/link settlement/`WaveStarted` order. Pinned Sora 0.3.0 exports the six-identity Aglaea scope twice to bundle `610e72a2…f6c` and five-identity Firefly transform scope to `dcc3f7bc…910`; both pass generated lowering, are rejected by production loading and grant zero coverage. Their five live-observation envelopes remain `Researching`; no prepared ambiguity was promoted. Canonical state growth reblesses the nine Phase 3 benchmark hashes and synthetic Standard replay hash without changing workloads, replay sizes or budgets. Native Windows x64 and Windows ARM64 combat/rules compile-only gates pass. Research is `8af99455…11c`, coverage is `36fe3a46…465`, and DataReady remains 0/283. |
| `G01-P4-B8` | `Complete` | This row's containing commit | `node tools/config-probes/verify-elation-probes.mjs`; `cargo test -p starclock-combat --test elation_subsystem --all-features`; `cargo test -p starclock-data probe_tests --all-features`; `node tools/benchmark/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo check -p starclock-combat -p starclock-rules --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/repository-check/verify-source-policy.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `rg -n -i "trailblazer\|yao.?guang\|sparxie\|silver.?wolf\|punchline\|banger\|aha" crates/starclock-combat/src`; `git diff --check`. Evidence: [shared Elation runtime boundary](../shared-elation-runtime-boundary.md), [Trailblazer Elation probe golden](../../config/probes/v1a/trailblazer-elation/golden.json), [Yao Guang Elation probe golden](../../config/probes/v1a/yao-guang-elation/golden.json), [`elation_subsystem.rs`](../../crates/starclock-combat/tests/elation_subsystem.rs), [`probe_tests.rs`](../../crates/starclock-data/src/probe_tests.rs). | Ability tags and `DamageClass::Elation` are independent typed facts. Canonically encoded keyed team resources have checked gain/spend/set, cap/overflow events and explicit persist/reset/clear wave policy. Forced Skill envelopes retain distinct provider/actor/owner/target, explicit ordinary/suppressed/substitute payment and attempted/payer/effective SP facts; invalid work mutates no resource or RNG. A generic linked `SharedActor` is selected by kind and does not gain an implicit timeline action. Rule IR/Sora lowering retains team-resource keys plus forced-use owner/payment fields and accepts a normal-turn Skill only when explicitly tagged. Pinned Sora 0.3.0 exports disabled Trailblazer and Yao Guang scopes across all 80 tables twice to bundles `c9063919…864e` and `cb8b01cc…72d0`; production rejects both and coverage remains 0/283. The eight Elation cases retain partial executable evidence but stay `Researching`; no live-observation envelope or four-form audit was overclaimed. Production generation is `fd276d81…fa1e`, research is `1de5d782…21f1`, and coverage is `32085c1c…7b17`. Canonical state growth reblesses the nine Phase 3 benchmark hashes and 530-byte synthetic Standard replay without changing workload shape or budgets. No released-form/kit name occurs in `starclock-combat/src`; source policy passes at 147 handwritten files. Native Windows x64 and Windows ARM64 combat/rules compile-only gates pass; `starclock-data` ARM64 is not claimed because this shell lacks the existing `zstd-sys` C toolchain. |
| `G01-P4-B9` | `Complete` | This row's containing commit | `cargo test -p starclock-ai`; `cargo test -p starclock-combat --test catalog_contract --all-features`; `cargo test -p starclock-combat --test damage_lifecycle --all-features`; `cargo test -p starclock-combat --test enemy_orchestration --all-features`; `node tools/benchmark/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo check -p starclock-combat -p starclock-ai -p starclock-rules --all-targets --all-features --target aarch64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs`; `git diff --check`. Evidence: [enemy AI/encounter runtime boundary](../enemy-ai-and-encounter-runtime-boundary.md), [`catalog_contract.rs`](../../crates/starclock-combat/tests/catalog_contract.rs), [`damage_lifecycle.rs`](../../crates/starclock-combat/tests/damage_lifecycle.rs), [`enemy_orchestration.rs`](../../crates/starclock-combat/tests/enemy_orchestration.rs). | Catalog-owned finite AI graphs validate canonical candidates/transitions, mandatory fallbacks, references, reachability and automatic cycles. `starclock-ai` owns isolated graph/state/turn cursors plus seeded behavior draws and returns exact offered commands under explicit no-target policy. Authored waves bind exact enemy/formation/initial-phase occurrences and transition only at `AfterHit`, `AfterPhase`, `AfterAction` or `Explicit`; transactional carry covers HP, Energy, Skill Points, effects, Action Gauge and keyed resources. Boss-phase operations apply HP/gauge/effect/Toughness/link carry, targetability, same-unit or linked-variant replacement and typed facts without enemy-ID branches. Canonical initial resources and enemy orchestration advance the documented state layout to `sha256-v2`; replay/benchmark goldens are reblessed without changing workload shape, replay framing or budgets. Native Windows x64 gates and Windows ARM64 combat/AI/rules compile-only checks pass. The universal CLI `config validate`/`catalog coverage` commands remain intentionally unavailable until owned `G01-P6-B5`; their current usage rejection was recorded rather than treated as a B9 implementation requirement. Research remains `1de5d782…21f1`, coverage remains `32085c1c…7b17`, and DataReady remains 0/283. |
| `G01-P4-B10` | `Complete` | This row's containing commit | `node tools/repository-check/verify-native-handlers.mjs`; `cargo test -p starclock-rules --all-targets --all-features`; `cargo test -p starclock-combat --test rule_ir_contract --all-features`; `cargo test -p starclock-data probe_tests --all-features`; `cargo check -p starclock-combat -p starclock-rules --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/benchmark/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [native-handler/content-branch audit](../native-handler-and-content-branch-audit.md), [machine audit](../../policy/native-handler-audit.json), [`registry_contract.rs`](../../crates/starclock-rules/tests/registry_contract.rs). | All eight V1a probe scopes are reviewed as typed-IR sufficient, so `native-registry-v1` deliberately admits zero production handlers. Sora lowering audits every enabled row against exact static stable-key/domain/version/schema/determinism/owner/IR-insufficiency/removal metadata and rejects unregistered rule/operation references. The universal repository gate exactly inventories V1a scopes and scans 97 combat/rules source files for content types, kit symbols and hard-coded numeric definition-ID control flow. Registry mismatch/fault-shape and IR-equivalence contracts pass. Native Windows x64 gates and Windows ARM64 combat/rules compile-only checks pass. The Phase 6 CLI commands remain unavailable by their owning schedule. Reference pack remains `0dca8ae…ef5a`, research `1de5d782…21f1`, coverage `32085c1c…7b17`, and DataReady remains 0/283. |
| `G01-P4-B11` | `Complete` | This row's containing commit | `node tools/core-kernel/verify-phase4.mjs`; `$env:STARCLOCK_BENCH_RUNNER_ID='starclock-bench-win10-i7-10700f-v1'; node tools/benchmark/verify.mjs --strict --samples 5 --output evidence/core-combat-v1/performance/phase4-provisional-windows-x64.json`; `node tools/benchmark/review-phase4.mjs --check`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [Phase 4 strict report](../../evidence/core-combat-v1/performance/phase4-provisional-windows-x64.json), [growth review](../../evidence/core-combat-v1/performance/phase4-growth-review.json), [benchmark contract](../performance-benchmark-baseline.md), [`research-cases.json`](../../evidence/core-combat-v1/research-register/research-cases.json), [`decision-records.json`](../../evidence/core-combat-v1/research-register/decision-records.json). | The complete formula/lifecycle/rule/probe suite passes. Workload `g01-phase4-full-kernel-v1` retains the immutable Phase 3 baseline digest and adds 500 commands with five checked operation families per command; its five-sample stable-runner median is 4.98 ms, 100,462 commands/s/core, 6,515,760 allocated bytes and 37,784 peak bytes/job under provisional limits. Nine comparable workload growth rows are retained. Six V1a cases are `Observed`; the remaining 21 Phase 4 architecture blockers are `ResolvedProjectPolicy` through five decision families and exact regression fixtures, while retaining their V1B public-observation/stronger-source gate before production `DataReady`. The ten Himeko Nova cases remain `Researching` for `G01-P7-M01`. Research is `448a7571…374`; coverage is `463d8833…c26db` and remains 0/283 `DataReady`. |
| `G01-P5-B1` | `Complete` | This row's containing commit | `cargo test -p starclock-build --all-targets --all-features`; `cargo check -p starclock-build --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/workspace/verify-dependencies.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [build catalog/compiler boundary](../build-catalog-and-compilation-boundary.md), [`build_boundary.rs`](../../crates/starclock-build/tests/build_boundary.rs). | `starclock-build` owns an immutable catalog, exact minimal build spec, pure compiler and typed ordered validation report. Catalog construction canonicalizes form rows, rejects duplicates and invalid cross-domain bindings, and captures the exact compatible combat revision+digest. Compilation revalidates both immutable catalogs, emits no partial value on failure and returns only `ResolvedCombatantSpec` on success. The fixed resolved row is synthetic B1 evidence, not production data; curves/Traces/Eidolons/Light Cones/digest encoding/presets/relics remain B2-B6. No build type or dependency enters combat. Native Windows x64 tests and Windows ARM64 compile-only checks pass. Research remains `448a7571…374`; coverage remains `463d8833…c26db` and 0/283 `DataReady`. |
| `G01-P5-B2` | `Complete` | This row's containing commit | `cargo test -p starclock-build --all-targets --all-features`; `cargo check -p starclock-build --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/workspace/verify-dependencies.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [ability/Trace compilation boundary](../ability-level-and-trace-compilation.md), [`ability_trace_compilation.rs`](../../crates/starclock-build/tests/ability_trace_compilation.rs). | Exact level/promotion rows and complete contiguous effective-level tables select validated combat ability variants with no clamp. Form-owned Trace graphs reject duplicate/unresolved prerequisites and cycles, then use Kahn order with `TraceNodeId` tie-breaking. The B2 patch subset adds abilities, rule bundles, stat modifiers and checked signed ability bonuses/caps; missing prerequisites, promotion, investments, curve rows and cross-catalog targets fail before output. Reversing stat/table/node/investment/Trace input produces an equal report and `ResolvedCombatantSpec`. Production remains Excel/Sora-only and unchanged: this is synthetic compiler evidence, research stays `448a7571…374`, coverage stays `463d8833…c26db` and 0/283 `DataReady`. Native Windows x64 tests and Windows ARM64 compile-only checks pass. |
| `G01-P5-B3` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo clippy -p starclock-build --all-targets --all-features -- -D warnings`; `cargo test -p starclock-build --all-targets --all-features`; `cargo check -p starclock-build --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/workspace/verify-dependencies.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [Eidolon compilation boundary](../eidolon-compilation.md), [`eidolon_compilation.rs`](../../crates/starclock-build/tests/eidolon_compilation.rs). | Exact E0-E6 selection requires one canonical definition for every E1-E6 rank, applies ranks ascending and preserves explicit within-rank patch order. The executable build patch language now adds/removes rule bundles, adds/replaces abilities, adds modifiers and applies checked ability-level adjustments; catalog construction rejects unresolved references, missing replacement/removal targets, duplicate additions and fixed full-rank conflicts. Trace/Eidolon binding collisions fail at the typed Eidolon report stage without partial output. Production remains Excel/Sora-only and unchanged: this is synthetic compiler evidence, research stays `448a7571…374`, coverage stays `463d8833…c26db` and 0/283 `DataReady`. Native Windows x64 tests and Windows ARM64 compile-only checks pass. |
| `G01-P5-B4` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo clippy -p starclock-build --all-targets --all-features -- -D warnings`; `cargo test -p starclock-build --all-targets --all-features`; `cargo check -p starclock-build --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/workspace/verify-dependencies.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [Light Cone compilation boundary](../light-cone-compilation.md), [`light_cone_compilation.rs`](../../crates/starclock-build/tests/light_cone_compilation.rs). | Exact typed level 1-80/promotion rows compose HP with checked integer arithmetic and ATK/DEF with checked fixed-point arithmetic into generic combat fields. Every definition has exactly one canonical S1-S5 row set; selecting a rank applies that row only. `MatchingPath`, `Always` and `BaseStatsOnly` evaluate passive eligibility independently from equipped base stats, so an invalid wearer remains a valid base-stat fixture with no accidental passive. Catalogs reject duplicate definitions/rows, incomplete ranks and unresolved or duplicate passive bindings; compilation rejects missing rows and existing-binding conflicts at the typed Light Cone stage without partial output. Production remains Excel/Sora-only and unchanged: this is synthetic compiler evidence, research stays `448a7571…374`, coverage stays `463d8833…c26db` and 0/283 `DataReady`. Native Windows x64 tests and Windows ARM64 compile-only checks pass. |
| `G01-P5-B5` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo clippy -p starclock-build --all-targets --all-features -- -D warnings`; `cargo test -p starclock-build --all-targets --all-features`; `cargo check -p starclock-build --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/workspace/verify-dependencies.mjs`; `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [build identity, attribution, presets and locks](../build-identity-and-locking.md), [`build_identity.rs`](../../crates/starclock-build/tests/build_identity.rs). | Versioned length-prefixed big-endian SHA-256 encoders pin distinct character/Light Cone definition, build-catalog, normalized selected-build and generic resolved-combatant scopes; reverse catalog/definition input produces the same goldens. Selected character/Trace/Eidolon/Light Cone `RuleSource` bindings cross only as canonical combat-owned source IDs/classes/tags/digests, while the report retains detailed owners. Named presets expand through the normal compiler and catalog construction rejects duplicate names/IDs, invalid selections and stale expected digests. Locks bind catalog revision+digest, build digest and independent combatant digest with typed mismatch results. The already reviewed pinned `sha2 = 0.11.0` package is reused privately with no new registry package. Production remains Excel/Sora-only and unchanged: research stays `448a7571…374`, coverage stays `463d8833…c26db` and 0/283 `DataReady`. Native Windows x64 tests and Windows ARM64 compile-only checks pass. |
| `G01-P5-B6` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo clippy -p starclock-build --all-targets --all-features -- -D warnings`; `cargo test -p starclock-build --all-targets --all-features`; `cargo check -p starclock-build --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/workspace/verify-dependencies.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [deferred relic/planar boundary](../deferred-relic-planar-boundary.md), [`relic_boundary.rs`](../../crates/starclock-build/tests/relic_boundary.rs), [`build_identity.rs`](../../crates/starclock-build/tests/build_identity.rs). | Every normalized build carries `relic-planar-deferred-empty-v1` with an exact zero piece count. A closed six-slot Cavern/Planar mapping and incompatible-revision rejection protect the future seam without set, affix, roll, value, source or runtime definitions. The explicit empty marker advances only the normalized selected-build domain to `starclock-combatant-build-v2`; its golden is re-pinned while definition/catalog/resolved-spec domains remain unchanged. No relic/planar type crosses into combat and workspace dependency direction remains mechanical. No deferred row is imported or claimed; research stays `448a7571…374`, coverage stays `463d8833…c26db` and 0/283 `DataReady`. Native Windows x64 tests and Windows ARM64 compile-only checks pass. |
| `G01-P6-B1` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo clippy -p starclock-activity --all-targets --all-features -- -D warnings`; `cargo test -p starclock-activity --all-targets --all-features`; `cargo check -p starclock-activity --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/workspace/verify-dependencies.mjs`; `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [one-Battle Activity boundary](../activity-one-battle-boundary.md), [`activity_boundary.rs`](../../crates/starclock-activity/tests/activity_boundary.rs). | The command-atomic aggregate owns exactly one `Activity -> Section -> Node -> Attempt -> Battle` path and three typed terminal routes. Closed typed slots reject owner/reset/type/bounds drift; participant rows retain only formation identity, catalog revision and opaque build/resolved-spec digests under a verified canonical lock. Start returns one immutable `BattleSpec` plus purpose-derived seed and exact result identity. Submission verifies every scope/config/lock/spec/seed field, recomputes the result digest, matches the predeclared ordered projection and enforces terminal-fault consistency before mutation. Golden hashes cover initial, handoff and terminal states; rejected commands preserve the full hash. Combat exposes only opaque `BattleStateHash` reconstruction and retains no Activity dependency. The already reviewed pinned SHA-256 primitive is reused privately with no new registry package. No challenge, universe, fork/join, route, reward or shop type exists. Production remains Excel/Sora-only and unchanged: research stays `448a7571…374`, coverage stays `463d8833…c26db` and 0/283 `DataReady`. Native Windows x64 tests and Windows ARM64 compile-only checks pass. |
| `G01-P6-B2` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo clippy -p starclock-mode-standard --all-targets --all-features -- -D warnings`; `cargo test -p starclock-mode-standard --all-targets --all-features`; `cargo check -p starclock-activity -p starclock-mode-standard --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/workspace/verify-dependencies.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [Standard profile construction](../standard-profile-construction.md), [`standard_profile.rs`](../../crates/starclock-mode-standard/tests/standard_profile.rs). | `StandardProfile` structurally owns one player team, a checked 1-4 party bound and one ordinary combat wave-transition default; clock, score and seasonal-rule fields do not exist. It accepts only a matching Activity identity, one-team participant policy and the exact frozen Outcome/FinalStateHash/EventDigest/TerminalFault projection. `StandardActivityBinding` resolves the authored binding ID around an already validated opaque ActivitySpec. `StandardScenario` parses the exact 16-hex seed, resolves all links and instantiates the generic aggregate; `StandardActivity` adds only profile/scenario/binding identity plus typed expected-terminal verification. The Phase 3 synthetic/benchmark fixtures remain unchanged. No alternate commands, hashing, result protocol, controller, replay, CLI, production import or challenge/universe/fork/shop semantics are introduced. Production remains Excel/Sora-only and unchanged: research stays `448a7571…374`, coverage stays `463d8833…c26db` and 0/283 `DataReady`. Native Windows x64 tests and Windows ARM64 compile-only checks pass. |
| `G01-P6-B3` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo clippy -p starclock-ai --all-targets --all-features -- -D warnings`; `cargo test -p starclock-ai --all-targets --all-features`; `cargo check -p starclock-combat -p starclock-ai --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/workspace/verify-dependencies.mjs`; `node tools/repository-check/verify-native-handlers.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [deterministic baseline controller execution](../baseline-controller-execution.md), [`baseline.rs`](../../crates/starclock-ai/src/baseline.rs), [`tests.rs`](../../crates/starclock-ai/src/tests.rs). | `baseline-battle-controller-v1` consumes only `BattleView`, the immutable DecisionPoint and canonical bounded hints. Separate survival, mandatory/interrupt, Skill and Basic tiers plus authored priority/survival/break/target/resource/synergy components produce ordered `i64` diagnostics; stable command/target identity breaks ties. Missing/duplicate hints, invisible or wrong-side actors/targets and interrupt-class drift fail rather than inventing policy. An authored immediate-survival action outranks every losing alternative; Concede stays below ordinary offers. Hint and offered-command row reversal cannot alter selection or diagnostics. The existing seeded enemy graph controller continues to settle bounded automatic transitions, canonical candidates, exact fallbacks and purpose-labelled draws, and now explicitly proves reversed offer storage cannot change its selected command/candidate/draw. No character/enemy/mode ID branch or optimality claim is added. Controller diagnostics remain nonauthoritative and replay/CLI integration stays B4/B5. Production remains Excel/Sora-only and unchanged: research stays `448a7571…374`, coverage stays `463d8833…c26db` and 0/283 `DataReady`. Native Windows x64 tests and Windows ARM64 compile-only checks pass. |
| `G01-P6-B4` | `Complete` | This row's containing commit | `cargo test -p starclock-replay`; `cargo test -p starclock-activity`; `cargo clippy -p starclock-replay --all-targets -- -D warnings`; `cargo check -p starclock-replay -p starclock-activity -p starclock-combat --all-targets --all-features --target aarch64-pc-windows-msvc`; `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/run.mjs`; `git diff --check`. Evidence: [Activity replay and controller diagnostics](../activity-replay-and-controller-diagnostics.md), [`activity_replay.rs`](../../crates/starclock-replay/tests/activity_replay.rs). | The version-1 envelope now encodes full accepted Activity commands and declared BattleResult projections, exact nested start identities/end digests and optional bounded integer controller score diagnostics. One-shot verification reconstructs a fresh aggregate, validates configuration/profile/definition/spec identity before execution, applies every accepted command once, compares each state boundary immediately and reports the first state or nested-boundary divergence. The golden complete-Activity fixture pins replay bytes and covers both controller diagnostics, identity rejection and corrupted state/start/end payloads; an internal codec fixture covers every projection/metric family and stable BattleFault reconstruction. The handwritten module is 1,068 lines, within policy. Native Windows x64 gates and Windows ARM64 compile-only checks pass. Repository integrity remains reference pack `0dca8ae…ef5a`, research `448a7571…374`, coverage `463d8833…c26db`, and 0/283 DataReady. |
| `G01-P6-B5` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo test -p starclock-data --all-targets --all-features`; `cargo test -p starclock-cli --all-targets --all-features`; `cargo run -q -p starclock-cli -- config validate`; `cargo run -q -p starclock-cli -- catalog coverage --goal core-combat-v1`; `node tools/workspace/verify-dependencies.mjs`; `node tools/dependency-policy/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs`; `git diff --check`. Evidence: [headless CLI contract](../headless-cli-contract.md), [`cli_contract.rs`](../../crates/starclock-cli/tests/cli_contract.rs), [`standard_replay_smoke.rs`](../../crates/starclock-cli/tests/standard_replay_smoke.rs). | `config validate` loads the embedded production bundle or exact `--bundle` bytes through the generated Sora reader and full production catalog conversion, rejects diagnostic JSON and reports SHA-256 `d4369701…d763`. `catalog coverage --goal core-combat-v1` derives the frozen 88/165/17/6/6/1 denominator from validated Sora identities, filters canonical categories and correctly reports 0 enabled/DataReady/GoldenVerified without counting support rows. The smoke-only synthetic scenario now executes `baseline-battle-controller-v1`; its exact authored hint descriptor is SHA-256-bound into the header, and deterministic human/`starclock-cli-v1` JSON output plus replay bytes are golden. Replay verification reconstructs the compatible entry, compares every hash and retains first-divergence exit class 4. Usage/config/replay/scenario/simulation/internal failures have distinct 2-7 codes. Production Standard/Activity entry resolution remains B6. Source policy passes at 519 CLI lines, 246 coverage lines and 1,199 catalog lines. Native Windows x64 and the complete repository suite pass. The attempted local Windows ARM64 data/CLI check reached `zstd-sys` but this shell has no ARM64 MSVC `cl.exe`; the committed `windows-arm64-compile` CI profile remains the compile-only evidence owner, and no cross-platform execution is claimed here. Research remains `448a7571…374`, coverage remains `463d8833…c26db`, and 0/283 DataReady. |
| `G01-P6-B6` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-standard-v1.py --check`; `node tools/config-production/verify.mjs`; `node tools/config-schema/verify-standard-encounter.mjs`; `cargo test -p starclock-data --all-targets --all-features`; `cargo test -p starclock-cli --all-targets --all-features`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Production bundle `f5cb9eba…b1c8`; seeded terminal hashes are committed in `starclock-cli::standard_v1`. | Imported 17 exact variants, 63 source-bound abilities, 17 finite first-legal AI graphs, 6 exact encounters, one ordinary profile and 6 build-locked scenarios. The public-data topology covers single/multi-wave, elite adds, summons, phase changes and three-layer Toughness; all six declared seeds reach `Won`, record exact event counts/final hashes, and round-trip canonical CLI replays. Character-specific compiled behavior remains owned by Phase 7 and receives no character coverage credit. |
| `G01-P7-V1B` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `node tools/config-production/verify.mjs`; `node tools/config-catalog/verify.mjs`; `cargo test -p starclock-data --all-targets --all-features`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [representative V1B production import](../representative-character-v1b-production.md), [`catalog_tests.rs`](../../crates/starclock-data/src/catalog_tests.rs), [production golden](../../config/production-golden.json). | Production bundle `68be1951…9c87` contains six forms, 45 abilities, 1,407 exact level parameters, 116 Traces and 36 Eidolons. Six-form E0/E6 builds, direct/Blast/bounce formulas, promoted Asta/Clara/Kafka/Firefly/Aglaea programs and Silver Wolf's 34/34/32+final enhanced Basic, six-hit Elation Skill, 300 Hidden MMR cap, 15-point gain and explicit SP semantics are golden-verified. Prepared mechanics remain `ExactStructured`; retained project-policy cases are not relabelled `Observed`. Character coverage reaches 6/88 and total coverage 36/283. |
| `G01-P7-C01` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C01 --check`; `node tools/config-production/verify.mjs`; `node tools/config-catalog/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [C01 production import](../character-partition-c01-production.md), [`catalog_tests.rs`](../../crates/starclock-data/src/catalog_tests.rs), [production golden](../../config/production-golden.json). | Production bundle `77e8ac15…18de` adds 8 forms, 52 abilities, 1,417 level parameters, 144 Traces and 48 Eidolons. All 14 production forms compile at E0/E6; C01 goldens pin Anaxa bounce, Aventurine DEF follow-up and Arlan SP semantics. Character coverage reaches 14/88 and total coverage 44/283. |
| `G01-P7-C02` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C01 --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C02 --check`; `node tools/config-production/verify.mjs`; `node tools/config-catalog/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo test -p starclock-data --all-targets --all-features`; `cargo test -p starclock-cli --test cli_contract --all-features`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [C02 production import](../character-partition-c02-production.md), [named character-resource action-cost boundary](../named-character-resource-action-cost-boundary.md), [`catalog_tests.rs`](../../crates/starclock-data/src/catalog_tests.rs), [production golden](../../config/production-golden.json). | Production bundle `2af0d528…41fac` adds 8 forms, 53 abilities, 1,807 level parameters, 151 Traces and 48 Eidolons. All 22 production forms compile at E0/E6; C02 goldens pin Blade's HP-scaling enhanced Basic and Castorice's zero-Energy, 100-Newbud Ultimate cost. Character coverage reaches 22/88 and total coverage 52/283. |
| `G01-P7-C03` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C01 --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C02 --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C03 --check`; `node tools/config-production/verify.mjs`; `node tools/config-catalog/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo test -p starclock-data --all-targets --all-features`; `cargo test -p starclock-cli --test cli_contract --all-features`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [C03 production import](../character-partition-c03-production.md), [named character-resource action-cost boundary](../named-character-resource-action-cost-boundary.md), [`catalog_tests.rs`](../../crates/starclock-data/src/catalog_tests.rs), [production golden](../../config/production-golden.json). | Production bundle `362552fd…2ef51` adds 8 forms, 55 abilities, 1,637 level parameters, 147 Traces and 48 Eidolons. All 30 production forms compile at E0/E6; C03 goldens pin Dan Heng • Imbibitor Lunae's three-stage enhanced Basic SP costs, Evanescia's Elation damage class and Feixiao's zero-Energy, 6-Flying-Aureus Ultimate cost. Character coverage reaches 30/88 and total coverage 60/283. |
| `G01-P7-C04` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C01 --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C02 --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C03 --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C04 --check`; `node tools/config-production/verify.mjs`; `node tools/config-catalog/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo test -p starclock-data --all-targets --all-features`; `cargo test -p starclock-cli --test cli_contract --all-features`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [C04 production import](../character-partition-c04-production.md), [Assist/companion boundary](../assist-skill-and-companion-protocol-boundary.md), [`catalog_tests.rs`](../../crates/starclock-data/src/catalog_tests.rs), [production golden](../../config/production-golden.json). | Production bundle `d3e137ab…ae05` adds 8 forms, 55 abilities, 1,694 level parameters, 144 Traces, 48 Eidolons and the first production effect-granted Assist envelope. All 38 production forms compile at E0/E6; C04 goldens pin named Assist payment, universal Toughness, three canonical grants, Skill/Source Energy programs and Orbital's 3-Source-Energy cost. Project-policy provenance remains distinct from observed fact. Character coverage reaches 38/88 and total coverage 68/283. |
| `G01-P7-C05` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C01 --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C02 --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C03 --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C04 --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C05 --check`; `node tools/config-production/verify.mjs`; `node tools/config-catalog/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo test -p starclock-data --all-targets --all-features`; `cargo test -p starclock-cli --test cli_contract --all-features`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [C05 production import](../character-partition-c05-production.md), [`catalog_tests.rs`](../../crates/starclock-data/src/catalog_tests.rs), [production golden](../../config/production-golden.json). | Production bundle `749e338c…1d31d` adds 8 forms, 49 abilities, 1,713 level parameters, 147 Traces and 48 Eidolons. All 46 production forms compile at E0/E6; C05 goldens pin Jade's FollowUp action and Charge cap, Jing Yuan's Summon action and Lightning-Lord hit counter, and Jingliu's zero-SP enhanced Skill with named Syzygy payment. Character coverage reaches 46/88 and total coverage 76/283. |
| `G01-P7-C06` | `InProgress` | — | — | 8 forms; frozen membership below. |
| `G01-P7-C07` | `Pending` | — | — | 8 forms; frozen membership below. |
| `G01-P7-C08` | `Pending` | — | — | 8 forms; frozen membership below. |
| `G01-P7-C09` | `Pending` | — | — | 8 forms; frozen membership below. |
| `G01-P7-C10` | `Pending` | — | — | 8 forms; frozen membership below. |
| `G01-P7-C11` | `Pending` | — | — | 2 forms; frozen membership below. |
| `G01-P7-L01` | `Pending` | — | — | 16 Light Cones; frozen membership below. |
| `G01-P7-L02` | `Pending` | — | — | 16 Light Cones; frozen membership below. |
| `G01-P7-L03` | `Pending` | — | — | 16 Light Cones; frozen membership below. |
| `G01-P7-L04` | `Pending` | — | — | 16 Light Cones; frozen membership below. |
| `G01-P7-L05` | `Pending` | — | — | 16 Light Cones; frozen membership below. |
| `G01-P7-L06` | `Pending` | — | — | 16 Light Cones; frozen membership below. |
| `G01-P7-L07` | `Pending` | — | — | 16 Light Cones; frozen membership below. |
| `G01-P7-L08` | `Pending` | — | — | 16 Light Cones; frozen membership below. |
| `G01-P7-L09` | `Pending` | — | — | 16 Light Cones; frozen membership below. |
| `G01-P7-L10` | `Pending` | — | — | 16 Light Cones; frozen membership below. |
| `G01-P7-L11` | `Pending` | — | — | 5 Light Cones; frozen membership below. |
| `G01-P7-M01` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo test -p starclock-combat --all-targets --all-features`; `cargo test -p starclock-data --all-targets --all-features`; `cargo clippy -p starclock-combat -p starclock-data --all-targets --all-features -- -D warnings`; `node tools/config-production/verify.mjs`; `node tools/goal-research/generate.mjs --check`; `node tools/goal-research/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [Assist/companion boundary](../assist-skill-and-companion-protocol-boundary.md), [`assist_skill_subsystem.rs`](../../crates/starclock-combat/tests/assist_skill_subsystem.rs), [`elation_subsystem.rs`](../../crates/starclock-combat/tests/elation_subsystem.rs), [`research-cases.json`](../../evidence/core-combat-v1/research-register/research-cases.json). | Effect instances now grant/revoke canonical abilities; Assist remains an independent semantic tag on Skill actions. Named team resources gate and pay ordinary shared uses, while explicitly suppressed forced envelopes preserve all costs. Provider ownership/scaling remains distinct from the caller actor, per-hit universal Toughness does not mutate weaknesses, and the existing generic shared-actor link covers Starblazer. Ten source-bound Himeko Nova cases are `ResolvedProjectPolicy` under a deterministic released-text envelope with every observation/replacement gate retained. |
| `G01-P7-M02` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo test -p starclock-combat --test modifier_pipeline --all-features`; `cargo test -p starclock-build --all-targets --all-features`; `cargo test -p starclock-data --all-targets --all-features`; `cargo clippy -p starclock-combat -p starclock-data --all-targets --all-features -- -D warnings`; `node tools/workspace/verify-dependencies.mjs`; `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [`domain_catalog.rs`](../../crates/starclock-data/src/domain_catalog.rs), [`modifier_pipeline.rs`](../../crates/starclock-combat/tests/modifier_pipeline.rs), [dependency inventory](../../policy/dependency-and-tool-policy.json). | Validated Sora character rows now compile into revision-compatible combat/build catalogs with exact input-bundle SHA-256 attribution, level-1 family identity, deterministic higher-level variants, base innate rules, stat curves, Trace graphs and exact E1-E6 sets. The bridge lowers all executable rule/ability/modifier patch forms and proves E0/E6 compilation. Uncapped non-stat pipeline modifiers are accepted without weakening cap-stage validation. The reviewed existing SHA-256 package is reused privately by `starclock-data`; no registry package was added. |
| `G01-P7-M03` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo test -p starclock-combat --all-targets --all-features`; `cargo test -p starclock-data --all-targets --all-features`; `cargo clippy -p starclock-combat -p starclock-data --all-targets --all-features -- -D warnings`; `node tools/workspace/verify-dependencies.mjs`; `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [target and action-resource boundary](../target-and-resource-boundary.md), [`production_hit_plans.rs`](../../crates/starclock-data/tests/production_hit_plans.rs). | Production Sora `AbilityHitPlanBinding` rows now survive validation and compile into immutable action hits with exact target group, damage/Toughness shares and CRIT policy. Resolver hit targeting projects primary, adjacent, selected/all, self and journaled bounce-draw groups without content branches. The finite generic action bound is 100, matching the validated authoring schema and largest released plan rather than truncating at the earlier synthetic limit. |
| `G01-P7-M04` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo test -p starclock-combat --all-targets --all-features`; `cargo test -p starclock-data --all-targets --all-features`; `cargo clippy -p starclock-combat -p starclock-data --all-targets --all-features -- -D warnings`; `node tools/workspace/verify-dependencies.mjs`; `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [`ability_program_execution.rs`](../../crates/starclock-combat/tests/ability_program_execution.rs), [`production_hit_plans.rs`](../../crates/starclock-data/tests/production_hit_plans.rs). | Generated Selector rows now compile into immutable typed origin/side/life/presence/reference/order/count/choice/empty-pool plans. Authored ability phase timing survives compilation; entry, before-hit, per-hit, after-hit and resolved programs evaluate inside the authoritative transaction against one deterministic selector snapshot. Per-hit programs consume the exact M03 damage/Toughness shares, supported emissions route through ordinary operations, action shifts and resources, and evaluation or unsupported-emission failures prove the standard rollback/Faulted policy without partial mutation. |
| `G01-P7-M05` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo test --workspace --all-targets --all-features`; `cargo clippy -p starclock-combat -p starclock-build -p starclock-cli --all-targets --all-features -- -D warnings`; `node tools/workspace/verify-dependencies.mjs`; `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [authoritative build-stat runtime boundary](../authoritative-build-stat-runtime-boundary.md), [`ability_program_execution.rs`](../../crates/starclock-combat/tests/ability_program_execution.rs), [`eidolon_compilation.rs`](../../crates/starclock-build/tests/eidolon_compilation.rs). | Resolved combatants bind every selected modifier to its exact Character, Trace, Eidolon or Light Cone source and battles instantiate those bindings as canonically ordered authoritative modifier state. Complete HP/ATK/DEF/SPD bases and modifier instances now survive views, rollback and canonical hashing. Ability programs and selected rule bundles share one checked stat resolver; the generic Hit Ended trigger dispatcher supplies exact cause/occurrence/selector context, enforces once-ledger scopes and routes supported emissions through the ordinary transaction services without content-ID branches. |
| `G01-P7-M06` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo clippy -p starclock-combat --all-targets --all-features -- -D warnings`; `cargo test -p starclock-combat --all-targets --all-features`; `cargo test -p starclock-data --all-targets --all-features`; `cargo test --workspace --all-targets --all-features`; `node tools/benchmark/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `git diff --check`. Evidence: [rule-emission lifecycle runtime boundary](../rule-emission-lifecycle-runtime-boundary.md), [`ability_program_execution.rs`](../../crates/starclock-combat/tests/ability_program_execution.rs), [`rule_ir_contract.rs`](../../crates/starclock-combat/tests/rule_ir_contract.rs). | Exact active rule instances own slot updates; authored effect chance purposes and exact-definition removals use the effect store; selector-resolved counters enter the deterministic reaction queue with full rule ordering; catalog-owned linked-unit and countdown definitions use ordinary lifecycle/link services. Hit lifecycle causes retain explicit appliers, missing lifecycle definitions fail catalog construction and a single regression proves all representative mutations plus rollback-safe execution without content branches. |
| `G01-P7-M07` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-standard-v1.py --check`; `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `node tools/config-production/verify.mjs`; `node tools/config-schema/verify-rule-ir.mjs`; `cargo fmt --all -- --check`; `cargo test --workspace --all-targets --all-features`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `git diff --check`. Evidence: [rule-emission lifecycle runtime boundary](../rule-emission-lifecycle-runtime-boundary.md), [`ability_program_execution.rs`](../../crates/starclock-combat/tests/ability_program_execution.rs), [`linked_lifecycle.rs`](../../crates/starclock-combat/tests/linked_lifecycle.rs), [`catalog_tests.rs`](../../crates/starclock-data/src/catalog_tests.rs). | Catalog-owned stable keys initialize form-scoped character resources and resolve team-resource payment to explicit source IDs. Effects instantiate and tear down source-bound modifier/rule attachments at every lifecycle boundary. Linked units and countdowns compile from production Sora, owner scaling and transformation exit execute authoritatively, queued reactions retain an explicit boundary, and informational rule emissions become observable typed signals. All 16 operation families reachable from current production/probe programs lower without content branches; unsupported future variants remain fail-closed. Canonical state/replay identity advances to `sha256-v3`; production regeneration is deterministic and coverage remains 30/283 `DataReady` pending V1B goldens. |
| `G01-P7-M08` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-standard-v1.py --check`; `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `node tools/config-production/verify.mjs`; `node tools/config-schema/verify-rule-ir.mjs`; `cargo fmt --all -- --check`; `cargo test --workspace --all-targets --all-features`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `node tools/benchmark/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/workspace/verify-dependencies.mjs`; `node tools/dependency-policy/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [ability-level parameter runtime boundary](../ability-level-parameter-runtime-boundary.md), [`rule_ir_contract.rs`](../../crates/starclock-combat/tests/rule_ir_contract.rs), [`catalog_tests.rs`](../../crates/starclock-data/src/catalog_tests.rs). | Every production `AbilityLevelParameter` row now compiles into an immutable map on its exact level-1 family or checked higher-level variant ID. The typed `AbilityParameter` Rule IR leaf carries its declared result kind, reads only the current occurrence's resolved ability and faults closed for a missing occurrence, reader, ability or key. Production minimum/maximum variants execute one checked formula to exact distinct results `2.0` and `5.6`; all dependent schema, fixture, probe, benchmark and production goldens regenerate deterministically. No content receives readiness credit: coverage remains 30/283 pending V1B execution goldens. |
| `G01-P7-M09` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-standard-v1.py --check`; `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `node tools/config-production/verify.mjs`; `node tools/config-schema/verify-character-build.mjs`; `node tools/config-schema/verify-rule-ir.mjs`; `cargo test -p starclock-combat --test damage_lifecycle --all-features`; `cargo test -p starclock-data --all-targets --all-features`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `node tools/benchmark/verify.mjs`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [authored hit formula runtime boundary](../authored-hit-formula-runtime-boundary.md), [`damage_lifecycle.rs`](../../crates/starclock-combat/tests/damage_lifecycle.rs), [`catalog_tests.rs`](../../crates/starclock-data/src/catalog_tests.rs). | `AbilityHitPlanBinding` now owns an all-or-nothing typed damage payload plus an independent base-Toughness payload. Catalog compilation resolves each coefficient against the exact level-1 family or higher-level variant, applies canonical per-hit shares and emits ordered live-stat damage/Toughness operations; missing parameters and partial bindings fail closed. The runtime reads the actor's modifier-resolved stat before ordinary damage. Production Asta Basic proves exact level-1/10 coefficients `0.5`/`1.4`, 2,000-ATK results `1,000`/`2,800` and 30 Toughness; coverage remains 30/283 pending V1B goldens. |
| `G01-P7-M10` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-standard-v1.py --check`; `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `node tools/config-production/verify.mjs`; `node tools/config-schema/verify-character-build.mjs`; `node tools/config-schema/verify-rule-ir.mjs`; `cargo test -p starclock-data --all-targets --all-features`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/benchmark/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [per-hit payload override boundary](../per-hit-payload-override-boundary.md), [`catalog_tests.rs`](../../crates/starclock-data/src/catalog_tests.rs). | Structural hit ratios remain normalized metadata while optional executable overrides select a per-hit parameter key, coefficient ratio and absolute Toughness. Overrides without a complete binding or element fail closed. Production Asta Skill proves five repeated full coefficients (`0.25`/`0.625`) and 30 Toughness at levels 1/15; Kafka Skill proves distinct primary/adjacent coefficients (`0.8`/`0.3`, then `2.0`/`0.75`) and exact 60/30 Toughness. Coverage remains 30/283 pending V1B goldens. |
| `G01-P7-M11` | `Complete` | This row's containing commit | `uv run --with openpyxl python tools/config-production/author-standard-v1.py --check`; `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `node tools/config-production/verify.mjs`; `node tools/config-catalog/verify.mjs`; `node tools/config-schema/verify-rule-ir.mjs`; `node tools/config-probes/verify-clara-counter.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/verify-source-policy.mjs`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [rule event observation boundary](../rule-event-observation-runtime-boundary.md), [`rule_ir_contract.rs`](../../crates/starclock-combat/tests/rule_ir_contract.rs), [`ability_program_execution.rs`](../../crates/starclock-combat/tests/ability_program_execution.rs), [`probe_tests.rs`](../../crates/starclock-data/src/probe_tests.rs). | Exact schema event points now survive lowering and dispatch only on their committed battle event. Generic filters retain selector-relative cause roles, source/action/ability/damage/resource facts and ancestry; expressions read exact event values, Energy, Skill Points and named character/team resources; contextual conditions query life/presence, effects, weakness, broken state, resource bounds and selector cardinality against immutable snapshots. Event payloads retain character-resource identity and executable damage element, absent facts fail closed, and observed event roles remain distinct from the bound rule owner. All schema, production, representative and probe bundles regenerate deterministically; coverage remains 30/283 pending V1B execution goldens. |
| `G01-P7-M12` | `Complete` | This row's containing commit | `cargo fmt --all -- --check`; `cargo test -p starclock-combat --test catalog_contract --all-features`; `cargo clippy -p starclock-combat --all-targets --all-features -- -D warnings`; `git diff --check`. Evidence: [target and action-resource boundary](../target-and-resource-boundary.md), [`catalog_contract.rs`](../../crates/starclock-combat/tests/catalog_contract.rs). | The immutable action aggregate accepts one to 256 hits while each Sora plan remains independently limited to 100. The 101-hit released envelope and exact 256 boundary compile, 257 fails closed, and existing finite resolver budgets remain unchanged. |
| `G01-P7-M13` | `Complete` | This row's containing commit | `cargo test -p starclock-combat --test action_resources --all-features`; `cargo test -p starclock-data domain_catalog::tests::zero_energy_ultimate_lowers_action_started_character_resource_cost --all-features`; `uv run --with openpyxl python tools/config-production/author-character-v1b.py --check`; `uv run --with openpyxl python tools/config-production/author-character-partition.py C01 --check`; `node tools/config-production/verify.mjs`; `node tools/config-catalog/verify.mjs`; `node tools/goal-coverage/generate.mjs --check`; `node tools/goal-coverage/verify.mjs`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-targets --all-features`; `node tools/repository-check/run.mjs --with-source-cache`; `git diff --check`. Evidence: [named character-resource action-cost boundary](../named-character-resource-action-cost-boundary.md), [`action_resources.rs`](../../crates/starclock-combat/tests/action_resources.rs), [`domain_catalog.rs`](../../crates/starclock-data/src/domain_catalog.rs). | Action policies now carry canonical positive named character-resource costs. Legality checks exact actor-owned balances; action-start payment is checked, journaled and emits the existing typed character-resource event inside the rollback transaction. Zero-Energy Ultimates with an authored named spend pass catalog validation and data lowering no longer synthesizes Energy for them. Production remains at C01 digest `cd448583…b0e0f` and coverage `44/283`. |
| `G01-P7-M14+` | `Pending` | — | — | Add later-discovered shared primitive or reviewed native-handler prerequisites before their dependent content partitions. |
| `G01-P7-R1` | `Pending` | — | — | Clean catalog and coverage regeneration. |
| `G01-P7-R2` | `Pending` | — | — | Manifest-wide build compilation. |
| `G01-P8-B1` through `G01-P8-B7` | `Pending` | — | — | Audits, established CI matrix, fuzz expansion, performance gate and freeze. Expand before Phase 8 starts. |

For a completed row, validation evidence must include commands and a link to a
committed report or fixture when applicable. A commit hash alone is insufficient.

## Required content partitions

### Character partitions

Frozen by `G01-P0-B1` in stable manifest-ID order. `G01-P7-V1B` owns the six
representative probe forms: `character.aglaea`, `character.asta`,
`character.clara`, `character.firefly`, `character.kafka`, and
`character.silver-wolf-lv-999`.

| Batch | Stable manifest IDs |
|---|---|
| `G01-P7-C01` | `character.acheron`, `character.anaxa`, `character.archer`, `character.argenti`, `character.arlan`, `character.ashveil`, `character.aventurine`, `character.bailu` |
| `G01-P7-C02` | `character.black-swan`, `character.blade`, `character.boothill`, `character.bronya`, `character.castorice`, `character.cerydra`, `character.cipher`, `character.cyrene` |
| `G01-P7-C03` | `character.dan-heng`, `character.dan-heng-imbibitor-lunae`, `character.dan-heng-permansor-terrae`, `character.dr-ratio`, `character.evanescia`, `character.evernight`, `character.feixiao`, `character.fu-xuan` |
| `G01-P7-C04` | `character.fugue`, `character.gallagher`, `character.gepard`, `character.guinaifen`, `character.hanya`, `character.herta`, `character.himeko`, `character.himeko-nova` |
| `G01-P7-C05` | `character.hook`, `character.huohuo`, `character.hyacine`, `character.hysilens`, `character.jade`, `character.jiaoqiu`, `character.jing-yuan`, `character.jingliu` |
| `G01-P7-C06` | `character.lingsha`, `character.luka`, `character.luocha`, `character.lynx`, `character.march-7th.preservation`, `character.march-7th.the-hunt`, `character.misha`, `character.mortenax-blade` |
| `G01-P7-C07` | `character.moze`, `character.mydei`, `character.natasha`, `character.pela`, `character.phainon`, `character.qingque`, `character.rappa`, `character.robin` |
| `G01-P7-C08` | `character.ruan-mei`, `character.saber`, `character.sampo`, `character.seele`, `character.serval`, `character.silver-wolf`, `character.sparkle`, `character.sparxie` |
| `G01-P7-C09` | `character.sunday`, `character.sushang`, `character.the-dahlia`, `character.the-herta`, `character.tingyun`, `character.topaz-numby`, `character.trailblazer.destruction`, `character.trailblazer.elation` |
| `G01-P7-C10` | `character.trailblazer.harmony`, `character.trailblazer.preservation`, `character.trailblazer.remembrance`, `character.tribbie`, `character.welt`, `character.xueyi`, `character.yanqing`, `character.yao-guang` |
| `G01-P7-C11` | `character.yukong`, `character.yunli` |

### Light Cone partitions

Frozen by `G01-P0-B1` in stable manifest-ID order.

| Batch | Stable manifest IDs |
|---|---|
| `G01-P7-L01` | `light-cone.a-dream-scented-in-wheat`, `light-cone.a-grounded-ascent`, `light-cone.a-secret-vow`, `light-cone.a-star-that-lights-the-night`, `light-cone.a-thankless-coronation`, `light-cone.a-trail-of-bygone-blood`, `light-cone.adversarial`, `light-cone.after-the-charmony-fall`, `light-cone.along-the-passing-shore`, `light-cone.amber`, `light-cone.an-instant-before-a-gaze`, `light-cone.arrows`, `light-cone.baptism-of-pure-thought`, `light-cone.before-dawn`, `light-cone.before-the-tutorial-mission-starts`, `light-cone.boundless-choreo` |
| `G01-P7-L02` | `light-cone.brighter-than-the-sun`, `light-cone.but-the-battle-isnt-over`, `light-cone.carve-the-moon-weave-the-clouds`, `light-cone.chorus`, `light-cone.collapsing-sky`, `light-cone.concert-for-two`, `light-cone.cornucopia`, `light-cone.cruising-in-the-stellar-sea`, `light-cone.dance-at-sunset`, `light-cone.dance-dance-dance`, `light-cone.darting-arrow`, `light-cone.data-bank`, `light-cone.day-one-of-my-new-life`, `light-cone.dazzled-by-a-flowery-world`, `light-cone.defense`, `light-cone.destinys-threads-forewoven` |
| `G01-P7-L03` | `light-cone.dreams-montage`, `light-cone.dreamville-adventure`, `light-cone.earthly-escapade`, `light-cone.echoes-of-the-coffin`, `light-cone.elation-brimming-with-blessings`, `light-cone.epoch-etched-in-golden-blood`, `light-cone.eternal-calculus`, `light-cone.eyes-of-the-prey`, `light-cone.fermata`, `light-cone.final-victor`, `light-cone.fine-fruit`, `light-cone.flame-of-blood-blaze-my-path`, `light-cone.flames-afar`, `light-cone.flickering-stars`, `light-cone.flowing-nightglow`, `light-cone.fly-into-a-pink-tomorrow` |
| `G01-P7-L04` | `light-cone.for-tomorrows-journey`, `light-cone.geniuses-greetings`, `light-cone.geniuses-repose`, `light-cone.good-night-and-sleep-well`, `light-cone.hey-over-here`, `light-cone.hidden-shadow`, `light-cone.holiday-thermae-escapade`, `light-cone.i-am-as-you-behold`, `light-cone.i-shall-be-my-own-sword`, `light-cone.i-venture-forth-to-hunt`, `light-cone.if-time-were-a-flower`, `light-cone.in-pursuit-of-the-wind`, `light-cone.in-the-name-of-the-world`, `light-cone.in-the-night`, `light-cone.incessant-rain`, `light-cone.indelible-promise` |
| `G01-P7-L05` | `light-cone.inherently-unjust-destiny`, `light-cone.into-the-unreachable-veil`, `light-cone.its-showtime`, `light-cone.journey-forever-peaceful`, `light-cone.landaus-choice`, `light-cone.lies-dance-on-the-breeze`, `light-cone.life-should-be-cast-to-flames`, `light-cone.lingering-tear`, `light-cone.long-may-rainbows-adorn-the-sky`, `light-cone.long-road-leads-home`, `light-cone.loop`, `light-cone.make-farewells-more-beautiful`, `light-cone.make-the-world-clamor`, `light-cone.mediation`, `light-cone.memories-of-the-past`, `light-cone.memorys-curtain-never-falls` |
| `G01-P7-L06` | `light-cone.meshing-cogs`, `light-cone.moment-of-victory`, `light-cone.multiplication`, `light-cone.mushy-shroomys-adventures`, `light-cone.mutual-demise`, `light-cone.never-forget-her-flame`, `light-cone.night-of-fright`, `light-cone.night-on-the-milky-way`, `light-cone.ninja-record-sound-hunt`, `light-cone.ninjutsu-inscription-dazzling-evilbreaker`, `light-cone.nowhere-to-run`, `light-cone.on-the-fall-of-an-aeon`, `light-cone.only-silence-remains`, `light-cone.passkey`, `light-cone.past-and-future`, `light-cone.past-self-in-mirror` |
| `G01-P7-L07` | `light-cone.patience-is-all-you-need`, `light-cone.perfect-timing`, `light-cone.pioneering`, `light-cone.planetary-rendezvous`, `light-cone.poised-to-bloom`, `light-cone.post-op-conversation`, `light-cone.quid-pro-quo`, `light-cone.reforged-in-hellfire`, `light-cone.reforged-remembrance`, `light-cone.reminiscence`, `light-cone.resolution-shines-as-pearls-of-sweat`, `light-cone.return-to-darkness`, `light-cone.river-flows-in-spring`, `light-cone.sagacity`, `light-cone.sailing-towards-a-second-life`, `light-cone.scent-alone-stays-true` |
| `G01-P7-L08` | `light-cone.see-you-at-the-end`, `light-cone.shadowburn`, `light-cone.shadowed-by-night`, `light-cone.shared-feeling`, `light-cone.shattered-home`, `light-cone.she-already-shut-her-eyes`, `light-cone.sleep-like-the-dead`, `light-cone.sneering`, `light-cone.solitary-healing`, `light-cone.something-irreplaceable`, `light-cone.subscribe-for-more`, `light-cone.sweat-now-cry-less`, `light-cone.swordplay`, `light-cone.texture-of-memories`, `light-cone.the-birth-of-the-self`, `light-cone.the-day-the-cosmos-fell` |
| `G01-P7-L09` | `light-cone.the-finale-of-a-lie`, `light-cone.the-flower-remembers`, `light-cone.the-forever-victual`, `light-cone.the-great-cosmic-enterprise`, `light-cone.the-hell-where-ideals-burn`, `light-cone.the-moles-welcome-you`, `light-cone.the-seriousness-of-breakfast`, `light-cone.the-storys-next-page`, `light-cone.the-unreachable-side`, `light-cone.this-is-me`, `light-cone.this-love-forever`, `light-cone.those-many-springs`, `light-cone.though-worlds-apart`, `light-cone.thus-burns-the-dawn`, `light-cone.time-waits-for-no-one`, `light-cone.time-woven-into-gold` |
| `G01-P7-L10` | `light-cone.to-evernights-stars`, `light-cone.today-is-another-peaceful-day`, `light-cone.todays-good-luck`, `light-cone.tomorrow-together`, `light-cone.trend-of-the-universal-market`, `light-cone.under-the-blue-sky`, `light-cone.until-the-flowers-bloom-again`, `light-cone.unto-tomorrows-morrow`, `light-cone.victory-in-a-blink`, `light-cone.void`, `light-cone.warmth-shortens-cold-nights`, `light-cone.we-are-wildfire`, `light-cone.we-will-meet-again`, `light-cone.welcome-to-the-cosmic-city`, `light-cone.what-is-real`, `light-cone.when-she-decided-to-see` |
| `G01-P7-L11` | `light-cone.whereabouts-should-dreams-rest`, `light-cone.why-does-the-ocean-sing`, `light-cone.woof-walk-time`, `light-cone.worrisome-blissful`, `light-cone.yet-hope-is-priceless` |

### Standard battle partitions

Frozen by `G01-P0-B1`. The exact 17 enemy variants and six four-build scenario
bindings are in [`standard-v1.json`](../../content-manifests/core-combat-v1/standard-v1.json).

| Encounter | Required archetype coverage | Seeded scenario |
|---|---|---|
| `encounter.cocoon.0001` | Basic single wave | `scenario.standard-v1.basic-single-wave` |
| `encounter.mainline.0541` | Three waves, summoner/replaceable entity, DoT, revival/return, invalidation, AfterAction advancement | `scenario.standard-v1.multi-wave-dot-revival` |
| `encounter.farmelement.0008` | Elite with adds, crowd control, invalidation | `scenario.standard-v1.elite-control-counter` |
| `encounter.mainline.0276` | Multi-phase boss and crowd control | `scenario.standard-v1.cocolia-phase-change` |
| `encounter.mainline.0755` | Three-layer Toughness routing and boss phases | `scenario.standard-v1.layered-toughness` |
| `encounter.mainline.1253` | Three waves, elites/adds, summoning, CC/DoT, revival/return, self-destruction/invalidation, AfterAction advancement | `scenario.standard-v1.target-invalidation-and-return` |

## Research and blockers

| ID | State | Question or blocker | Evidence required | Owner/batch |
|---|---|---|---|---|
| `G01-R-SABER-ARCHER-SOURCE` | `Complete` | Verified source IDs `1014` and `1015` are absent from pinned 4.4 `AvatarConfig`; their pinned fallback identity, skill/Trace/Eidolon IDs and released-text hashes match. Both retain `ExactPreviousRelease`. | [`saber-archer-audit.json`](../../evidence/core-combat-v1/reference-binding/saber-archer-audit.json) | `G01-P0-B2` |
| `G01-R-HIMEKO-NOVA-APPROX` | `ResolvedProjectPolicy` | Ten named `ApproximateFromReleasedText` cases now bind the generic implementation to effect-granted Assist Skills, named shared uses, distinct provider/caller attribution, universal Toughness hits, typed thresholds, forced no-cost actions and a shared linked actor. The M01 decision closes the C04 architecture prerequisite without claiming an observed game fact; every recorded observation and stronger-source replacement gate remains active. | [`research-cases.json`](../../evidence/core-combat-v1/research-register/research-cases.json), [`fixture-specifications.json`](../../evidence/core-combat-v1/research-register/fixture-specifications.json), [`decision-records.json`](../../evidence/core-combat-v1/research-register/decision-records.json), [Assist/companion boundary](../assist-skill-and-companion-protocol-boundary.md), [`assist_skill_subsystem.rs`](../../crates/starclock-combat/tests/assist_skill_subsystem.rs) | `G01-P7-M01`; deterministic policy input for `G01-P7-C04` |
| `G01-R-ELATION-SEMANTICS` | `ResolvedProjectPolicy` | Eight cases now bind the generic implementation to independent ability/damage tags, checked keyed resources, explicit forced-use payment/attribution and typed shared actors. B11's decision record and executable Trailblazer/Yao Guang/four-form audit fixtures close the Phase 4 architecture blocker without claiming an unrecorded game observation. Every case retains its exact V1B public-observation or stronger-source gate before affected content may become `DataReady`. | [`research-cases.json`](../../evidence/core-combat-v1/research-register/research-cases.json), [`fixture-specifications.json`](../../evidence/core-combat-v1/research-register/fixture-specifications.json), [`decision-records.json`](../../evidence/core-combat-v1/research-register/decision-records.json), [Trailblazer Elation probe golden](../../config/probes/v1a/trailblazer-elation/golden.json), [Yao Guang Elation probe golden](../../config/probes/v1a/yao-guang-elation/golden.json), [`elation_subsystem.rs`](../../crates/starclock-combat/tests/elation_subsystem.rs) | `G01-P4-B11`; content observation retained by `G01-P7-V1B` |
| `G01-R-V1A-PROBES` | `ResolvedProjectPolicy` | Six source-bound Asta/Firefly cases are `Observed`. The other 13 Asta/Kafka/Clara/Firefly/Aglaea cases are closed as Phase 4 architecture blockers by explicit policy decisions plus their dedicated Sora/lowering/runtime regression fixtures. They are not game-fact observations and each retains its recorded public-observation or stronger-source gate before production `DataReady`. | [`research-cases.json`](../../evidence/core-combat-v1/research-register/research-cases.json), [`fixture-specifications.json`](../../evidence/core-combat-v1/research-register/fixture-specifications.json), [`decision-records.json`](../../evidence/core-combat-v1/research-register/decision-records.json), [Asta probe golden](../../config/probes/v1a/asta-modifier/golden.json), [Kafka probe golden](../../config/probes/v1a/kafka-dot/golden.json), [Firefly damage probe golden](../../config/probes/v1a/firefly-damage/golden.json), [Firefly transform probe golden](../../config/probes/v1a/firefly-transform/golden.json), [Clara probe golden](../../config/probes/v1a/clara-counter/golden.json), [Aglaea probe golden](../../config/probes/v1a/aglaea-memosprite/golden.json) | `G01-P4-B11`; content observation retained by owning Phase 7 batches |

An unresolved research case may not be converted into a default implementation
without a documented project-policy decision and regression fixture.

## Decision log

| Date | Decision | Reason |
|---|---|---|
| 2026-07-20 | Resolve all ten Himeko Nova approximation cases through the generic M01 Assist/companion envelope while retaining their observation gates. | Bounded source research did not expose hidden operation order. Effect grants, named shared uses, provider-owned scaling, weakness-independent Toughness, typed thresholds, explicit forced no-cost queues and shared linked actors provide a deterministic, character-agnostic C04 policy without overstating it as an observed fact. |
| 2026-07-20 | Register `G01-P7-M12` ahead of V1B for the aggregate action-hit bound. | Silver Wolf LV.999's released enhanced Basic is three resumable plans totaling 100 bounces followed by a separate final hit. The existing 100-hit aggregate limit rejected the exact 101-hit production action even though every authored Sora plan stayed within its validated 100-row bound. |
| 2026-07-20 | Register `G01-P7-M08` ahead of V1B for effective-level parameter execution. | The V1B completeness audit found that all authored `AbilityLevelParameter` rows reach the build catalog but generated combat variants currently reuse one literal program, so selected minimum and maximum effective levels cannot produce distinct behavior. Content cannot receive `DataReady` until this generic bridge is executable and golden-tested. |
| 2026-07-20 | Register `G01-P7-M09` ahead of V1B for executable hit formulas. | The post-M08 V1B audit found that production `HitPlanHit` rows preserve target and split metadata but compile with empty operation lists. Most authored abilities therefore emit hit lifecycle events without damage or Toughness mutation. M09 must bind explicit coefficient keys, character elements and per-hit shares to typed checked operations before any representative form can receive readiness credit. |
| 2026-07-20 | Register `G01-P7-M10` ahead of V1B for exact per-hit payload overrides. | M09 proves one shared coefficient/base-Toughness payload, but V1B includes Blast actions with distinct primary/adjacent parameter keys and bounce actions whose coefficient and Toughness repeat in full on every hit. Structural ratios must remain normalized metadata, so exact executable payloads need independent optional per-hit overrides rather than distorted hit plans. |
| 2026-07-20 | Register `G01-P7-M11` ahead of V1B for exact rule-event observations. | The post-M10 six-form audit found that the committed schema already authors exact action/turn/effect points, selector-relative cause roles, ability tags, event values, resource reads and battlefield predicates, while current lowering accepts only Hit Ended plus literal/comparison conditions and direct stable-ID filters. Asta decay/unique-target credit, Clara attack-received counters, Kafka ally-Basic follow-ups and Silver Wolf Skill-Point reactions cannot be represented exactly until this generic contract reaches authoritative Rule IR evaluation. |
| 2026-07-20 | Register `G01-P7-M07` ahead of V1B for production attachment/identity completion and the remaining reachable emission mappings. | M06 closes the representative runtime path but the compiler still lacks catalog-owned linked/countdown authoring, effect-bound rules/modifiers, canonical named-resource keys and explicit queue boundaries; fail-closed emissions cannot be promoted as executable content. |
| 2026-07-20 | Register `G01-P7-M06` ahead of V1B for the remaining typed emission and lifecycle bridge. | M05 proves source-bound rule dispatch and stat queries, but representative programs still require queued actions, effect application, linked summon/countdown creation and rule-slot mutation; those generic services must execute authoritatively before any dependent content receives `DataReady`. |
| 2026-07-20 | Register `G01-P7-M03` through `M05` ahead of V1B for exact hit-plan lowering, ability-program execution and authoritative build-stat/modifier integration. | V1B audit proved that hit-plan bindings and phase programs were validated but discarded and that combatant ATK/DEF plus selected modifiers never entered authoritative state; count-only E0/E6 compilation cannot be accepted as executable content. |
| 2026-07-20 | Register `G01-P7-M02` ahead of V1B for production combat/build catalog compilation and executable progression modifiers. | The first real character import proved that count-only data definitions cannot satisfy E0/E6 compilation and that minor Trace damage-stage modifiers exceed the previously exercised stat-only registry boundary; the generic prerequisite must land before dependent content receives `DataReady`. |
| 2026-07-20 | Keep one frozen Standard profile/activity with scenario-selected Battle bindings, and bind pre-Phase-7 participant locks to released `ContentIdentity` rows rather than nonexistent executable `Character` rows. | Six encounter-specific scenarios must share the ordinary profile without duplicating coverage identities or importing partial character definitions; each binding still owns an exact encounter, four build/spec digests, seed stream and result projection. |
| 2026-07-20 | Author the frozen Standard-v1 rows with the approved `uv`/`openpyxl` adapter, retain checksum-bound Sora as the only validator/exporter, and keep bootstrap reproducibility distinct from the evolving production golden. | Designer workbooks remain deterministic and reviewable while the original 283-identity empty bootstrap can still reproduce twice without being mistaken for the current executable production bundle. |
| 2026-07-17 | Goal 01 targets complete core battle plus released character forms, Traces, Techniques, Eidolons and Light Cones. | Establish the first independently playable milestone. |
| 2026-07-17 | Universe families and all three recurring challenge families are excluded. | Prevent activity-specific systems from delaying the core battle milestone. |
| 2026-07-17 | Full relic/planar and public enemy catalogs are excluded; future boundaries remain protected. | They are not part of the requested first content batch. Standard battle uses a frozen representative public-data manifest. |
| 2026-07-17 | Content completeness is manifest-based and requires `DataReady`; behavioral profiles and placeholders do not count. | Make completion auditable and prevent scope inflation. |
| 2026-07-17 | Every batch is committed separately and updates this ledger. | Preserve reviewability, resumption and deterministic progress tracking. |
| 2026-07-17 | Goal 01 binds the prepared Version 4.4 content-reference pack before implementation. | Prevent compact profiles, memory, or ad-hoc websites from becoming the Excel source of truth. |
| 2026-07-17 | Excel workbooks plus pinned Sora output remain the only authoritative production authoring/runtime-data chain; prepared JSON is bootstrap evidence, not a runtime shortcut. | Preserve the formal editable and validated configuration workflow selected for Starclock. |
| 2026-07-17 | Phase 4 interleaves shared-kernel batches with non-production V1a probes compiled from a dedicated Excel/Sora scope. | Make complex released mechanics constrain the Rule IR and lifecycle before bulk import without misreporting partial content as DataReady. |
| 2026-07-17 | Cross-platform CI, property-test scaffolding and performance measurement begin before hardening. | Make Phase 8 consume accumulated evidence instead of creating its prerequisites at the end. |
| 2026-07-17 | Freeze Goal 01 manifest `e2188c7844d678253c98d569db017dbad7101541cf502aba4c2eb80c0435bf19` against reference pack `0dca8ae581b4fa1e9fe8ce0c9e67ac6eb72c251deacbd4831751ce685e45ef5a`. | Establish an immutable machine-readable completeness oracle before workspace or content implementation. |
| 2026-07-17 | Select Silver Wolf LV.999 as the Elation-focused production form in `G01-P7-V1B`. | Reuse the prepared Elation review fixture while Phase 4 still requires at least one additional released form to constrain shared semantics. |
| 2026-07-17 | Freeze six public encounter records, 17 exact variants, and six seeded four-build scenarios as `standard-v1`. | Cover every Goal 01 section 4.4 archetype without claiming or importing the full public enemy catalog. |
| 2026-07-17 | Retain Saber and Archer as `ExactPreviousRelease` records bound to pinned StarRailRes revision `7b349e39ee0f6f3bf814567995829b99c95e7a93`. | Their source IDs are absent from the pinned 4.4 structured AvatarConfig; the audit proves identity and released-text hashes without fabricating 4.4 provenance. |
| 2026-07-17 | Carry the 10 source-bound Himeko Nova mechanism approximations into a named Phase 0 research case. | Evidence hashes validate the released text, but exact operation/target/timing semantics require observations or explicit fixture decisions before production import. |
| 2026-07-17 | Freeze 37 named research cases and reproducible fixture envelopes for V1a, shared Elation and Himeko Nova; retain their state as `Researching`. | Phase 0 must eliminate unnamed ambiguity without pretending that a fixture specification is a completed live observation. |
| 2026-07-17 | Resolve the two B2 Asta research cases to `Observed` only through the disabled dedicated Excel/Sora probe and production reader/domain boundary. | Structured OnStack/weakness evidence plus executable query/credit fixtures now answer those bounded questions, while incomplete Asta content remains excluded from production and DataReady coverage. |
| 2026-07-17 | Resolve the two B3 Firefly research cases through the disabled Excel/Sora probe while making shield instances authoritative canonical state. | The pinned source payload and executable Rule IR program fix HP/Energy/damage and Ultimate operation order; separately retained shields are required for concurrent decay, overflow, rollback, replay hashing and later content without a character branch. |
| 2026-07-17 | Resolve the two B4 Firefly cases through the extended disabled Excel/Sora probe and make layered Toughness policy explicit. | Source-bound operation order proves weakness before reduction and Super Break's effective per-target sample; ordinary, Exo and future provider layers must author routing, recovery, depletion and event-source credit instead of inheriting a single-bar shortcut. |
| 2026-07-17 | Make Rule IR `QueryStat` the sole recursive stat-expression leaf and keep query caches outside authoritative state. | Modifier expressions reuse checked Rule IR arithmetic, cycle faults retain ordered query keys, and cache eviction cannot change results or hashes. |
| 2026-07-17 | Register `G01-P7-M01` as the prerequisite for Himeko Nova's generic Assist Skill/shared-use/companion-protocol mechanism. | The prepared source contains exact numbers/text hashes but no bound configuration operations for ten abilities; the shared primitive and observation goldens must precede production batch `G01-P7-C04`. |
| 2026-07-20 | Make generic effect instances and owner-scoped typed rule slots authoritative canonical battle state; retain Asta/Kafka cases as `Researching` when a prepared probe does not satisfy the recorded live-observation envelope. | Duration, stack, control, DoT attribution and content resources must survive rollback/replay without character branches, while executable source-bound probe rows are design evidence rather than permission to overstate unobserved ordering or snapshot behavior. |
| 2026-07-20 | Keep the reaction queue and its insertion counter transaction-local, and retain Clara's four cases as `Researching` despite partial executable probe evidence. | Every scheduled reaction must drain or fault within one command, so persisting scheduler scratch would change canonical hashes without adding replay semantics; source-bound Clara rows constrain cause, target and charge plumbing but do not replace the registered live-observation envelopes. |
| 2026-07-20 | Represent summons, memosprites and countdowns through one canonical typed owner-link store, and keep transformation as reversible state on the existing unit. | Distinct owner/unit/timeline identities preserve attribution without scheduler exceptions; explicit defeat/departure/wave policies prevent orphan actors, while atomic saved form/ability/presence state makes teardown deterministic and character-agnostic. Dedicated Aglaea/Firefly probes remain non-production partial evidence, so their five live-observation cases stay `Researching`. |
| 2026-07-20 | Represent shared Elation behavior with independent typed ability/damage tags, generic keyed team resources, explicit queued owner/payment policy and a linked shared-actor kind; retain all eight Elation cases as `Researching`. | Common event and state envelopes can express both released-form probes without content IDs or resolver branches. The two source-bound probes constrain architecture but do not satisfy the recorded live cross-kit observations or four-form compilation audit. |
| 2026-07-20 | Keep enemy graph/state/turn cursors and behavior RNG in `starclock-ai`, while `starclock-combat` retains only exact hostile occurrence/phase bootstrap state and accepts controller choices solely as offered commands. | Controller policy and random behavior remain isolated from authoritative mutation; command replay can verify combat without rerunning an engine/controller, and graph transitions remain deterministic and auditable. |
| 2026-07-20 | Advance canonical battle hashing to `sha256-v2` for initial team-resource and enemy orchestration state. | Wave resets and boss/controller bootstrap depend on values absent from the v1 byte layout; an explicit revision prevents old replay verifiers from silently interpreting the new canonical stream. |
| 2026-07-20 | Admit zero production native handlers after reviewing every V1a probe, and require exact compiled/Sora metadata agreement plus a repository-wide content-branch audit for any future admission. | Completed typed IR and shared Phase 4 mechanics express all reviewed probe envelopes. An explicit empty registry prevents absence from being mistaken for unfinished work, while mechanical scope inventory and ID-branch checks stop exceptional content code from leaking into shared resolvers. |
| 2026-07-20 | Resolve 21 remaining V1a/Elation Phase 4 architecture blockers by explicit generic policy decisions and regression fixtures while retaining every content-observation gate. | Phase 4 must freeze deterministic engine behavior without converting partial probes into invented game facts; production rows still require their recorded public observation or stronger source before `DataReady`. |
| 2026-07-20 | Preserve the Phase 3 benchmark report by SHA-256 and add a genuine five-operation full-kernel workload with provisional stable-runner budgets. | Phase 4 growth remains reviewable against the original transaction/replay baseline, while damage, healing, HP consumption, effects and keyed resources now have a representative strict workload before content scale-up. |
| 2026-07-20 | Make `starclock-build` an upstream pure compiler whose catalog captures exact combat revision+digest compatibility and whose success value is only `ResolvedCombatantSpec`. | Build legality and diagnostics must remain outside authoritative combat state, while immutable catalog identity prevents a build validated against one combat catalog from compiling against another with the same revision label. |
| 2026-07-20 | Compile ability levels by selecting validated combat-definition variants and compile Trace effects through a closed four-patch build language in canonical topological order. | Effective-level data stays outside the resolver, minor stats remain generic modifiers, and Trace unlock/passive behavior reaches combat without mutable catalog edits, row-order dependence or content-specific branches. |
| 2026-07-20 | Require exactly six canonical Eidolon ranks and lower deterministic binding patches in ascending rank and explicit sequence order, rejecting missing targets and every unspecified overwrite. | E0-E6 selection stays total and input-order independent, replacement semantics cannot silently discard an active binding, and Trace/Eidolon conflicts fail before a partial combat spec can escape. |
| 2026-07-20 | Treat Light Cone base-stat composition and passive applicability as separate compile decisions, and make S1-S5 exact alternative passive rows rather than cumulative item definitions. | Matching-path failures still equip the authored HP/ATK/DEF row without activating the passive, while explicit `Always` and `BaseStatsOnly` policies remain data-driven and no path or item branch enters combat. |
| 2026-07-20 | Use distinct versioned canonical digest domains for build definitions, the build catalog, normalized build selections and resolved combatants; keep preset expected digests outside the catalog hash and bind all scopes in one build lock. | Definition/catalog changes, player selection changes and generic combat-output changes must be independently diagnosable without recursive hashes, platform layout dependence or caller-supplied digest bytes. |
| 2026-07-20 | Carry a versioned explicit-zero relic/planar boundary in normalized builds and advance the selected-build digest domain to v2 when adding that marker. | The deferred seam must fail unknown revisions and reserve Cavern/Planar slot ownership without importing sets/affixes or silently changing the v1 canonical byte layout. |
| 2026-07-17 | Freeze initial Goal 01 coverage `05f3539927e298d570a6d128e2b27613f3639620260d16509ab0deb8cda03d69` at 283/283 accounted and 0 `DataReady`. | Prepared reference data, provenance evidence and research fixtures are valuable prerequisites but are not production Excel/Sora runtime content. |
| 2026-07-17 | Replace the root facade with a nine-member virtual Cargo workspace and enforce the exact local graph in both a direct verifier and workspace tests. | Responsibility boundaries must fail mechanically if combat gains peripheral dependencies or an out-of-scope crate appears. |
| 2026-07-17 | Pin Rust/Cargo 1.97.0, Node 24.15.0 and `fixnum =0.9.5`; record every direct/transitive package and tool in the machine-readable dependency policy. | Exact tool/package resolution, license review, deterministic impact and compile cost must be auditable before numeric implementation expands. |
| 2026-07-17 | Make `node tools/repository-check/run.mjs` the single local and future-CI gate, with generated/vendor exclusions and public re-exports allowed only through exact reviewed policy entries. | Prevent local/CI command drift and ensure file-size, visibility, dependency and generated-artifact rules fail mechanically as the workspace grows. |
| 2026-07-17 | Bind `sora-cli 0.3.0` to crates.io archive `90d373102de6a0d7969ebdee51d4ac01ba25c7f2c34661cf581a2c8ead57763a` and golden output digest `3068755483a02de85271dd531c5707a6b8e0e08270a782dd60a34a2b3e965f09`. | Production schema work may rely only on command/type/codegen/export behavior executed by the checksum-bound release. |
| 2026-07-17 | Under Sora 0.3.0, use project paths with a parent, Sora `format = "never"` plus pinned rustfmt, range-validated positive `i32` transport IDs/order fields, and an isolated locked generated-reader fixture. | Executed regression probes show bare clean paths and Windows formatter discovery fail, unsigned Sora Rust fields do not compile, and emitted readers require serde/zstd; domain IDs and production dependency ownership remain protected. |
| 2026-07-17 | Compare generated Excel templates semantically rather than hashing raw `.xlsx` archives. | Sora 0.3.0 workbook ZIP metadata varies across invocations; workbook lists plus read-only no-change synchronization prove schema projection, while schema lock, Rust, diagnostic JSON and `.sora` stay byte-golden. |
| 2026-07-17 | Bind native CI to `windows-2025` x64, `ubuntu-24.04` x64 and `macos-15` ARM64, with paired alternate-CPU `cargo check` profiles and immutable action commits. | The full repository runner and Sora golden execute only on the actual host; compile-only evidence remains mechanically distinct and cannot be promoted into an unexecuted runtime claim. |
| 2026-07-17 | Centralize shared metadata in `ContentIdentity`, bind facts through `SourceRecord`/`EvidenceRecord`, and carry bundle compatibility in singleton `ConfigManifest`. | Later content families can use typed primary-key references while localization and provenance remain non-executable metadata and generated Sora rows stay behind the data boundary. |
| 2026-07-17 | Canonical decimal source fields are strings ending `_decimal`, normalized without redundant zeroes, limited to six fractional digits and checked into signed `i64` millionths without a float conversion. | One machine-readable grammar and integer-only verifier prevent Excel/JSON/locale rounding before Phase 2 domain arithmetic exists. |
| 2026-07-17 | Keep the common-schema TOML rows as disabled `SyntheticFixture` golden input only; defer all production table-source migration and `.xlsx` authoring to `G01-P1-B10`. | B6 can prove Sora structure, references, templates and exporters without creating a non-Excel production runtime shortcut or misreporting partial content. |
| 2026-07-17 | Represent character/build authoring as typed identities plus unique ordered child rows, with one closed Trace/Eidolon patch union and explicit E1-E6 and S1-S5 policies. | Sora can enforce references, bounds and uniqueness now while B8 owns executable Rule IR, B10 owns production Excel, and B11 owns whole-catalog graph/program/completeness validation. |
| 2026-07-17 | Encode Rule IR as referenced relational nodes and closed tagged unions, with finite `If`/bounded-`ForEach` programs, explicit replacement proposals and no recursive cell-local language. | Sora can prove row shape/references while deterministic graph/type/domain validation remains a separately tested catalog-construction responsibility; no JSON/script shortcut or resolver mutation API is introduced. |
| 2026-07-17 | Represent trigger events as a typed event-family union rather than one large enum validation list. | Sora 0.3.0's Excel template writer rejects inline validation lists over Excel's 255-character limit; the family union preserves typed event semantics and the committed golden regression-proves template generation. |
| 2026-07-17 | Keep Standard orchestration as a bounded one-battle Activity graph with typed participant/loadout locks, opaque compiled-build and resolved-spec digests, and an allowlisted result projection. | This protects the generic Activity/BattleSpec handoff without leaking build equipment into combat state or inventing challenge, universe, score, clock, reward or account semantics. |
| 2026-07-17 | Represent enemy behavior as mechanically distinct variants bound to finite AI graphs, explicit mandatory fallbacks and data-authored phase/link/wave lifecycle policy. | Deterministic catalog validation can reject unreachable or cyclic control graphs while the resolver remains generic and free of enemy-ID branches. |
| 2026-07-17 | Make the shared schema modules production-`.xlsx` native and adapt legacy TOML schema goldens only inside their ignored verifier copies. | One committed schema projection must govern production authoring; synthetic fixtures remain reproducible without becoming an alternate production source format. |
| 2026-07-17 | Bootstrap all 283 frozen goal identities as released but disabled Excel rows, retaining initial coverage states and evidence bindings while leaving executable tables empty. | Stable transport IDs and review staging are useful now, but identity-only rows must not become runtime definitions or receive DataReady credit. |
| 2026-07-17 | Keep calamine/rust_xlsxwriter in a standalone locked authoring tool and add private serde/zstd only to `starclock-data` for unmodified Sora readers. | Workbook dependencies never enter runtime crates, generated transport types never cross domain APIs, and diagnostic JSON cannot become a second loader. |
| 2026-07-17 | Keep the Phase 1 `SimulationCatalog` surface limited to immutable compatibility metadata and counts while its representative definitions remain private; require every populated table to have an explicit lowering. | B11 proves the generated-row/domain boundary without preempting the final public combat catalogs and indexes owned by `G01-P2-B2`, and future content cannot be silently discarded. |
| 2026-07-17 | Use disabled `ProjectFixture` rows only in a separately committed real Sora bundle and make the public production loader reject that same bundle. | The representative workbook/Sora/catalog path is executable evidence but cannot become production content, an alternate runtime path or DataReady coverage. |
| 2026-07-17 | Keep `fixnum` as private six-decimal storage and checked exact-operation backend, and implement all precision loss through one project-owned `i128` quotient primitive covering the six normative rounding modes. | Pinned fixnum 0.9.5 exposes only Floor/Ceil/nearest-away; the promoted product/numerator range is proven for `i64` raw inputs, and one audited sign/remainder/tie implementation avoids scattered incomplete rounding. |
| 2026-07-17 | Reject Rust `f32`/`f64` type tokens mechanically in eight authoritative runtime `src` roots while leaving test/tool roots available for the later numeric oracle. | Authoritative state and formulas cannot acquire a float shortcut, but `G01-P2-B5` can still compare legal fixed-point vectors with a non-authoritative research implementation. |
| 2026-07-17 | Store each combat-definition family in one private ID-sorted boxed table; require set-like reference fields to arrive strictly sorted/unique while preserving authored program-call, rule-bundle and encounter-enemy order. | Catalog construction is insertion-order invariant without erasing executable order or exposing a container backend, and invalid generated/domain lowering fails before a battle can observe it. |
| 2026-07-17 | Separate all catalog definition IDs from battle-local runtime IDs as fixed-width non-zero newtypes and return a canonical closed path for static program cycles. | Compile-time type separation prevents cross-domain aliases; deterministic diagnostics make the same invalid catalog fail identically across platforms and insertion orders. |
| 2026-07-17 | Pin `rand =0.10.2` to only `std`/`chacha` and `sha2 =0.11.0` without defaults; confine dependency types to the RNG engine/derivation modules and make generic/system random APIs fail policy review. | Named ChaCha8 and SHA-256 primitives avoid handwritten crypto while Starclock retains ownership of every replay-sensitive byte, mapping and draw rule. |
| 2026-07-17 | Derive streams from a domain-separated, length-prefixed, big-endian SHA-256 tuple and make zero-candidate, all-zero-weight and pre-draw validation failures consume no random word. | Activity coordinates and ASCII labels isolate future graph/spawn/battle streams, while explicit no-draw cases prevent failed/empty selection from shifting later combat rolls. |
| 2026-07-17 | Freeze replay format/schema version 1 as `SCRP` plus typed compatibility/controller/entry identity and zero-based `(kind, sequence, length, payload)` records; reject every unknown kind. | Phase 3/6 command, state, nested-battle and controller payloads can fill reserved kinds without changing framing, while old verifiers never guess how to skip authoritative records. |
| 2026-07-17 | Make one project-owned canonical encoder target both collecting bytes and a private streaming SHA-256 sink; decode record framing twice so all declared lengths/sequences/trailing boundaries validate before allocating the bounded record-reference table. | Golden/debug and production hash paths cannot drift, production state hashing avoids a full byte vector, and hostile replay lengths fail before replay-declared collection allocation. |
| 2026-07-17 | Pin dev-only Proptest 1.11.0 with only `std`; give each property family an explicit ChaCha seed, case/shrink limits and source-parallel regression persistence. | Structural generation and shrinking are reproducible and extensible through Phases 3/8 without changing authoritative RNG, production dependencies or replay state; minimized failures become committed evidence rather than transient random failures. |

## Terminal acceptance checklist

Change an item to `[x]` only with evidence in this file.

- [x] Required workspace crates compile with enforced dependency direction.
- [x] Pinned dependencies/tools have purpose, license, deterministic-impact and
      compile-cost records; the Sora 0.3.0 golden project proves every relied-on
      command, schema and export capability before production schemas.
- [ ] Fixed-point, RNG, canonical codec and state hashing pass cross-platform
      golden vectors.
- [ ] Canonical hashing streams through the shared encoder, and accepted-command
      transactions reuse bounded battle-local scratch only after legality
      validation while preserving rollback/fault hashes.
- [x] Core formula, timeline, effect, Toughness, lifecycle and rule suites pass.
- [x] Asta, Kafka, Clara, Firefly, Aglaea and cross-kit Elation V1a probes pass
      through the production Excel/Sora-to-domain boundary without entering
      production coverage.
- [ ] Standard single-wave, multi-wave, elite and multi-phase boss scenarios run
      from build selection to terminal battle result.
- [ ] CLI configuration validation, coverage, battle run and replay verification
      pass from a clean checkout.
- [ ] Released character combat-form manifest is 100% `DataReady`, including
      abilities, Techniques, Traces and E1-E6.
- [ ] Released Light Cone manifest is 100% `DataReady`, including levels,
      promotions and S1-S5.
- [ ] `standard-v1` enemy, encounter and scenario manifests are 100% `DataReady`.
- [ ] All required bilingual fields, provenance and evidence hashes validate.
- [x] Sora/Excel export and generated outputs reproduce without drift.
- [ ] Manifest-wide E0/S1 and E6/S5 build compilation passes.
- [ ] Baseline controller decisions and replay hashes are deterministic.
- [x] Committed Windows/Linux/macOS CI workflows distinguish native execution
      from compile-only CPU coverage and retain golden evidence.
- [ ] Versioned Standard/server-verification workloads satisfy the reviewed
      stable-runner budgets for incremental latency, 100/500-command one-shot
      replay throughput, commands/second/core, concurrent isolated jobs, peak
      bytes/job, allocations, state-copy/hash cost and journal growth.
- [ ] Formatting, clippy, workspace tests, source-size and public-API audits pass.
- [ ] No excluded universe, challenge, UI, account or full relic/enemy dataset is
      claimed as part of Goal 01.
- [ ] Clean-checkout acceptance report is committed and linked here.

## Completion record

| Field | Value |
|---|---|
| Final state | Not complete |
| Completion commit | — |
| Catalog digest | — |
| Clean-checkout report | — |
| Cross-platform report | — |
| Remaining required work | All execution phases |
