# Goal 07 Service Partition S06

`G07-P4-M14-S06` completes the frozen service milestone. It owns Standard
Trailblaze Bonus ID `6` and Divergent Universe IDs `518`–`530`, for 14 service
records and 14 mechanic-rule bindings.

## Standard enhanced Curio bonus

ID `6` is the third enhanced entry option. It selects one eligible, unowned
Curio from the complete Standard pool using canonical candidate order and one
labeled Reward RNG draw. The checked Activity transaction grants ownership,
initializes the Curio's active state and charge tuple, records its acquisition
boundary and consumes no Cosmic Fragments.

The option appears only when the Ability Tree projects the enhanced
Trailblaze Bonus capability. Empty eligibility fails without state or RNG
mutation, and replay v3 records the same opaque Activity choice used by CLI,
Agent and MCP surfaces.

## Expansion profile boundary

IDs `518`–`530` retain formal `DivergentUniverse` ownership,
`EvidenceOnly` mode ownership and no Standard payload or offer membership.
They close as `ProfileExcluded` and fail with `ProfileUnavailable` before
payload validation, RNG or authoritative mutation.

ID `6` and its rule finish as `ExecutableRuleIr` with `ExactPublic`
accuracy. The other 13 records and rules finish as `ProfileExcluded` with
exact profile ownership. No native handler or numeric approximation is
admitted.

With this partition, all 94 frozen service, shop, respite, roster,
interactable and Trailblaze Bonus records and rules have terminal runtime and
accuracy dispositions.
