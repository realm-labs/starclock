# Goal 07 Occurrence Partition S01

`G07-P4-M13-S01` owns 32 frozen records: four Occurrences, four variants and
24 choices covering Stephen Lloyd's Idea, Aha Stuffed Toy and the first
I.O.U. Dispenser rows.

## Executable outcomes

The shared Occurrence compiler now lowers target-free `Special` outcomes to an
explicit transition operation instead of a deferred-effect counter. Exact
Blessing and Curio parameter references resolve through catalog stable keys;
the selected Curio then enters the ordinary checked acquisition transaction,
including ownership, initial lifecycle state, charges and acquisition events.

The partition adds exact public outcome facts for:

- Stephen Lloyd's 150-Cosmic-Fragment option;
- the I.O.U. Dispenser's named Angel-type I.O.U. Dispenser grants;
- the I.O.U. Dispenser's 100-Cosmic-Fragment decline options.

Aha Stuffed Toy's normal inputs do not change the result, so those choice
records execute as graph transitions without inventing a resource effect.
The reviewed public facts and captured source digests are recorded in
`evidence/standard-universe-mechanics-complete-v1/source-reviews/G07-P4-M13-S01.json`.

## Explicit external results

Project-policy choices that select one Blessing or Curio are expanded into
candidate-specific external outcomes. Each outcome owns a concrete handler
payload and stable topology identity. Submitting it performs no hidden
Activity RNG draw, while invalid state hashes preserve exact state and RNG
bytes. Stephen Lloyd's random-Blessing option therefore exposes 162 explicit
results rather than resolving a deferred policy inside the handler.

The focused runtime test executes the exact fragment grants, named Curio
lifecycle, transition-only choices and an explicit Blessing result. The
partition closes with 32 terminal runtime/accuracy dispositions, no deferred
counter for its choices, no native-handler admission and no numeric
approximation.
