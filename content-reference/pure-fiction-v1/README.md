# Pure Fiction normalized reference pack

Goal 15 owns this isolated normalized research/debug pack. Excel workbooks and
the Sora 0.3.0 export under `config/pure-fiction` are the Candidate authoring
authority; this JSON root is never a runtime input.

