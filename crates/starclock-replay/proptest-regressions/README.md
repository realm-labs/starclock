# Proptest regression corpus

Replay properties use fixed ChaCha seeds and source-parallel failure
persistence. Commit every minimized failure file generated below this
directory unchanged so decoder and battle-replay mutations remain permanent
corpus inputs.
