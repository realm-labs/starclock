# Divergent Universe encounter boundary

This checkpoint accounts for all 877 source obligations: 28 current area
entries, 848 conservative Tourn2 room candidates and the shared StageConfig
root. It grants no room, encounter or StageConfig reachability from a table
prefix, matching name, ID range or reverse occurrence.

The Version 4.4 snapshot has no `Tourn3` room rows, no layer-room rows for the
11 selected layers and no forward current area-to-group or room-to-stage
selector. Fourteen area map-entry values are populated, but none equals a
StageConfig `StageID`; the other 14 current areas omit the value. The 459
combat-capable Tourn2 room candidates therefore remain
`UnresolvedNoCurrentSelector`; 389 non-combat candidates have no wave
expansion.

`RogueTournWeeklyChallenge` publishes enemy display groups, but no fixed-table
selector binds a `ChallengeID` to `Tourn3`, activity module `6002201`, a
current area or a released schedule. Exact forward traversal nevertheless
audits 618 display-pool bindings through 43 exact display groups and 118 StageConfig candidates
(`RogueMonster`/StageConfig pairs), with 176 unique
waves and 385 unique enemy slots. Every
slot binds an inherited Version 4.4 enemy variant and preserves stage/enemy
ability references.

The result contains zero reachable encounter groups. All 43 groups, 118
StageConfig candidates, 176 waves, 385 enemy slots and 618 display-pool
bindings are `Cataloged` shared candidates. Their module, area and difficulty
bindings stay empty and their fallback is fail-closed until released
structured evidence supplies the missing current-weekly selector. Protocol 3
therefore retains its published boss-identity-change rule without inventing
the unnamed replacement identities.
