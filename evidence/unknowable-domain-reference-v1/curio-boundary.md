# Unknowable Domain Version 4.4 Curio boundary

The explicit type `260` selector reaches 60 shared Curio handbook identities.
Those identities resolve to all 81 `RogueMagicMiracle` copy/state rows and all
47 `RogueMagicMiracleGroup` rows. The groups contain 691 exact weighted edges;
their union covers every mode copy exactly, with weights limited to 1, 2 and 4.

Three shared identities have multiple mode copies: Absolute Failure
Prescription has eight, Fission Cuckoo Clock has four and Tawot Cards has
twelve. These are distinct effect-bearing copies, not an inferred ordered
upgrade or repair sequence. The other 57 identities each resolve to one copy.

Released localized mechanics prove 20 finite-use destruction counters, three
conditional destruction boundaries, one Curio repair effect and one
all-possessed-Curio replacement effect. Exact parameter indexes and values are
retained. Hidden random probabilities, repair target order, replacement
candidate pools, group consumers, eligibility, draw order and fallback remain
`Unspecified`.

Account rewards are excluded. Effect rows and bilingual text are retained as
row-level evidence and digests, but are not lowered into runtime handlers.
Table prefixes, matching names and numeric ID shape do not grant membership or
establish lifecycle transitions.
